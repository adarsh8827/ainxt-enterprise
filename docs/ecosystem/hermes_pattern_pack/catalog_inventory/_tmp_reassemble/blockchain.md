## Blockchain

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| evm | skill | blockchain | A read-only, dependency-free Python CLI skill for querying wallet balances, tokens, transactions, gas, ENS, and security data across eight EVM-compatible chains with USD pricing. | Python 3.8+ stdlib, public RPC endpoints (overridable), free public pricing/ENS/decoding APIs | none | N/A | optional-skills/blockchain/evm/SKILL.md | official | MIT (repo) | - | terminal toolset, script path under the skill's install directory | HIGH | OPTIONAL - handy read-only blockchain data tool, but niche/vertical-specific | - Use whenever a user asks for EVM wallet portfolios, token/contract info, gas prices, ENS lookups, whale transfers, or approval/security checks across 8 chains (Ethereum, BSC, Base, Arbitrum, Polygon, Optimism, Avalanche, zkSync)<br>- Run the bundled stdlib-only Python script with a subcommand (wallet, multichain, compare, tx, decode, ens, allowance, contract, whale, gas) rather than calling any external SDK<br>- No credentials are required; pricing/ENS/decoding rely on free public APIs that are rate-limited and have no fallback if down<br>- Watch for known limits: known-token list only covers ~30 tokens per chain, L2 gas estimates omit L1 data-posting fees, and address casing is not checksum-validated<br>- Verify functionality by running the network stats command and an ENS resolution smoke test<br>- Recommend a private RPC endpoint for heavier or production use to avoid public rate limiting |
| hyperliquid | skill | blockchain | A stdlib-only Python CLI skill for read-only Hyperliquid perpetual/spot market data, account positions, fills/orders history, and heuristic post-trade review. | Python stdlib, Hyperliquid public info API, optional env defaults for API URL and user address | none | N/A | optional-skills/blockchain/hyperliquid/SKILL.md | official | MIT (repo) | - | terminal toolset, home-directory .env convention, script path under the skill's install directory | HIGH | OPTIONAL - vertical-specific trading data tool for Hyperliquid users | - Use when a user wants Hyperliquid market data (markets, candles, funding, order book) or to review their own perp/spot account activity and trade quality<br>- Twelve subcommands cover discovery, historical data pulls, live book depth, account state/balances, fills/orders, a heuristic trade review, and a normalized data export for backtesting prep<br>- Configure default network and wallet address via environment variables so most commands can run without repeating the address<br>- Be aware fills/orders endpoints only expose a recent rolling window, not full archive history, and the review command is heuristic, not a precise reconstruction of trader intent<br>- For deep analysis, chain commands: start with review to find problem periods, then pull fills/orders/candles/funding for that window<br>- Verify by running the markets command and confirming top markets by volume print correctly |
| solana | skill | blockchain | A stdlib-only Python CLI skill for querying Solana wallet portfolios, SPL token/NFT holdings, transactions, whale transfers, and network stats with USD pricing. | Python stdlib, Solana public RPC endpoint (overridable), free public pricing API | none | N/A | optional-skills/blockchain/solana/SKILL.md | official | MIT (repo) | - | terminal toolset, script path under the skill's install directory | HIGH | OPTIONAL - vertical-specific blockchain data tool, parallel to the EVM skill | - Use for Solana wallet/token/NFT portfolio lookups, transaction inspection, recent activity, whale detection, or SOL/SPL token price checks<br>- Eight subcommands run against a public RPC endpoint with optional price enrichment; no API keys needed<br>- NFT detection is a heuristic (amount=1, decimals=0) so compressed NFTs and newer token-standard NFTs will be missed<br>- Whale detection only scans the most recent block (a point-in-time snapshot) and public RPC transaction history is limited to about two days<br>- Use a private RPC endpoint for production-grade or high-volume use to avoid public rate limits<br>- Verify by running the network stats command and confirming current slot, TPS, and SOL price are returned |
