## MCP

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| fastmcp | skill | mcp | A build-test-deploy workflow guide for creating Python MCP servers with the FastMCP framework, from scaffolding a minimal tool surface through local validation to client installation and HTTP deployment. | Python, the FastMCP pip package, optionally an HTTP client library for API-wrapper templates, the FastMCP CLI | none (framework itself); depends on whatever the wrapped API/service needs | N/A | optional-skills/mcp/fastmcp/SKILL.md | official | MIT (repo) | - | references a sibling native-MCP-configuration skill/config file for wiring a finished server into the platform, otherwise generic | HIGH | DEFAULT - broadly useful, generic developer workflow for anyone building custom MCP servers | - Use when the task is to create a brand-new Python MCP server (wrapping an API, database, or file-processing workflow) rather than connect to an existing one<br>- Start from the narrowest useful template (API wrapper, read-only database, or file processor) and scaffold it rather than building from scratch<br>- Implement tools first with concrete verb-based names, typed parameters, and JSON-safe return values before adding optional resources or prompts<br>- Validate locally using the framework's CLI to inspect, list, and call each tool - including over HTTP transport - before wiring the server into any client<br>- Install into supported MCP clients only after local validation passes, and prepare a clean repo (dependency file, documented env vars) before any hosted deployment<br>- Verify readiness with a checklist: the server imports cleanly, CLI inspect/list succeed, every tool has at least one real successful call, and env vars are documented |
| mcp-oauth-remote-gateway | skill | mcp | A detailed manual-OAuth playbook for authorizing remote HTTP MCP servers on a headless gateway deployment where the built-in localhost-callback OAuth flow cannot work, including writing tokens into the platform's exact token-storage format. | curl/python for PKCE and Dynamic Client Registration, a remote gateway host, the MCP server's OAuth metadata endpoints, optionally a web dashboard | OAuth (manual PKCE + Dynamic Client Registration) | N/A | optional-skills/mcp/mcp-oauth-remote-gateway/SKILL.md | official | MIT (repo) | - | extremely tight - platform's internal token-storage file schema and paths, internal OAuth-client module, MCP server config block, reload command, dashboard public-URL discovery | LOW | SKIP - the OAuth/PKCE technique is standard but the deliverable (matching this platform's internal token file format) has no meaning on a different platform | - Load only when a remote/headless gateway deployment needs to authorize an OAuth-protected remote MCP server, since the built-in local-callback flow assumes browser and server are on the same machine<br>- Before manual token surgery, try the built-in flow's own fallbacks (paste-back over an interactive terminal, or an SSH port-forward) and prefer any web dashboard that can complete OAuth in the user's own browser<br>- Manually perform OAuth 2.1 with PKCE and Dynamic Client Registration: discover metadata endpoints, register a public client, build an authorize URL with resource indicators, and have the user paste back the failed-redirect URL<br>- Exchange the authorization code for tokens and write both a token file and a client-info file into the exact schema and location the platform's OAuth token storage expects, so a reload finds cached credentials<br>- Smoke-test the new token with a direct protocol handshake request before telling the user to reload, and know the difference between a simple expired-token refresh, a tripped internal circuit breaker requiring a full restart, and a server-side session revocation requiring full re-authorization<br>- Verify success via the reload command showing the server reconnected, and treat a revoked refresh token or repeatedly revoked session as a sign to fall back to a static API key when available |
| mcporter | skill | mcp | A terminal CLI for discovering, calling, authenticating, and managing MCP servers/tools ad-hoc, without needing them pre-configured in the platform. | Node.js/npx, the mcporter npm package, optional OAuth browser flow for auth | OAuth (per-server, via CLI login) / none for unauthenticated servers | N/A | optional-skills/mcp/mcporter/SKILL.md | official | MIT (repo) | - | minimal - recommends JSON output for easier downstream parsing and a pseudo-terminal for interactive OAuth, otherwise generic | HIGH | DEFAULT - lightweight, dependency-light way to reach any MCP server ad-hoc without platform-specific configuration | - Use for one-off or ad-hoc MCP server/tool access - listing, calling, or authenticating against a server - without requiring it to be registered in the platform's own config<br>- Run via npx with no install needed, or install globally; auto-discovers MCP servers already configured by other clients on the same machine<br>- Connect to new servers without config by passing an HTTP URL or a stdio launch command directly on the command line<br>- Call tools using either key=value shorthand, function-call syntax, or a raw JSON payload, and always request JSON output when another process needs to parse the result<br>- Use the CLI's OAuth login command for servers requiring authentication, running it under a pseudo-terminal since it may need an interactive browser flow<br>- Verify a new server works by listing its tools with schema details, then making one real test call and confirming a JSON response comes back |
