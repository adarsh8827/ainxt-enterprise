## Email

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| agentmail | skill | email | Enables an agent to own and operate its own email inbox (send, receive, reply, OTP flows, webhooks/websockets for inbound mail) via a dedicated email-for-agents CLI. | Node.js/npm, the email-service CLI package, an API key, terminal toolset | API key | N/A | optional-skills/email/agentmail/SKILL.md | official | MIT (repo) | - | terminal toolset; prefers CLI over MCP/REST, generic pattern otherwise | HIGH | OPTIONAL - useful when an agent needs its own persistent email identity (OTP flows, inbound loops), not needed for general use | - Use when the task requires an agent-owned mailbox rather than access to a human's existing IMAP/SMTP account, e.g. completing an email OTP flow or running an inbound-email-triggered loop<br>- Install the CLI globally and authenticate via an API key exported as an environment variable; a documented self-signup path exists if no key is available yet<br>- Prefer the CLI over the MCP or REST integrations, falling back only when the host platform mandates MCP tools or the CLI is missing an operation<br>- Always request JSON-formatted output when another step needs to consume IDs programmatically, and prefer extracted plain-text/HTML fields for feeding message content to an LLM<br>- Use webhooks or websockets only when simple polling for inbound mail isn't sufficient<br>- Verify setup by listing inboxes in JSON format and confirming the call succeeds without exposing the API key in logs or prompts |
