## DevOps

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| actual-setup | skill | devops | Sets up a third-party self-hosted-hardware inference provider as an agent inference backend, covering both a hosted encrypted relay and a fully local on-device daemon, including known gotchas. | curl, a provider account/API key (relay) or a locally installed and device-authorized daemon (local), the platform's built-in provider abstraction | API key (relay) / none-on-loopback (local) | N/A | optional-skills/devops/actual-setup/SKILL.md | official | MIT (repo) | - | built-in provider id/aliases, provider/model config-set commands, secrets file, reasoning-effort override config, chat verification pattern | LOW | OPTIONAL - narrow provider-integration setup skill, only relevant to this platform's inference routing | - Use to wire the platform to a self-hosted-hardware inference provider either via a hosted encrypted relay (needs an API key) or a fully local on-device daemon (needs human browser-based device authorization, which the agent must never fake)<br>- For relay mode, store the key in the secrets file, discover available models via a REST call, then set the provider and default model in config<br>- For local mode, have the human authorize the daemon first, then download a model with an explicit quantization tag, load it by its installed name, and point the platform at the local loopback URL<br>- Know the common failure modes: models with small context windows overflow the platform's default tool schema and produce an empty-stream error unless the toolset is trimmed or a larger-context model is chosen; unsupported reasoning-effort values will error<br>- Never create a manually configured custom provider block with the same name as the built-in provider - it owns that name and conflicting custom config is ignored<br>- Verify by sending a one-shot chat request through the configured provider/model and confirming the exact expected reply string comes back |
| docker-management | skill | devops | A comprehensive command reference for managing Docker containers, images, volumes, networks, and Compose stacks, plus troubleshooting and Dockerfile optimization guidance. | Docker Engine, Docker Compose v2, terminal toolset | none | N/A | optional-skills/devops/docker-management/SKILL.md | official | MIT (repo) | - | terminal toolset only | HIGH | DEFAULT - broadly useful, low-risk, generic DevOps reference for any agent platform with shell access | - Use for any container lifecycle, image, Compose, volume/network, or Docker troubleshooting request<br>- Organize work by domain: container lifecycle and interaction, image build/tag/push, Compose up/down/logs/exec, and volume/network management<br>- Always run a diagnostic (disk usage, ps, logs) before destructive cleanup commands and get explicit user confirmation before pruning with volumes<br>- Cover common failure patterns like a container that exits immediately, a busy port, or a loopback-vs-all-interfaces binding mismatch<br>- Recommend Dockerfile best practices: multi-stage builds, dependency-before-source layer ordering, an ignore file, pinned base images, and non-root users<br>- Verify results per-operation: container status via ps, clean logs, port reachability via curl, image presence, and Compose service health |
| hermes-s6-container-supervision | skill | devops | A deep architecture and debugging reference for how this platform's Docker image uses s6-overlay to supervise per-profile gateways and boot-time initialization, including why the main process is unsupervised by design. | s6-overlay v3, Docker, Linux-only container environment, platform-specific service-manager and container-boot code | none | N/A | optional-skills/devops/hermes-s6-container-supervision/SKILL.md | official | MIT (repo) | - | extremely tight - platform Dockerfile, entrypoint scripts, service manager class, boot-reconciliation module, gateway CLI dispatch | LOW | SKIP - purely internal container-implementation documentation for this specific codebase, no standalone value elsewhere | - Load only when modifying or debugging this platform's Docker image process supervision (static services, per-profile gateway supervision, or boot-time initialization scripts)<br>- Understand the layered boot architecture: one-time init scripts run as root for setup, static supervised services back a no-op main slot and an optional dashboard, and per-profile services are dynamically registered under a supervised runtime directory<br>- Know why the main process runs as the container's exec'd command rather than as a supervised service: the init system's setup scripts can't see command-line args, and its halt mechanism can't propagate a real exit code<br>- Use documented recipes to check the supervisor is PID 1, inspect or toggle a specific service, and read the boot reconciliation log<br>- Common pitfalls include commands not found because a binary path isn't on the exec PATH, profile directories becoming root-owned and breaking the boot reconciler, and confusing a normal crash-loop-until-configured service with a bug<br>- Verify changes by running the project's own Docker-based test harness against the built image |
| inference-sh-cli | skill | devops | A wrapper skill for running any of 150+ cloud AI apps (image/video/LLM/search generation) through a single aggregator CLI without managing individual provider APIs. | the aggregator CLI (installed via curl script), an account/API key with the service, terminal toolset | API key | N/A | optional-skills/devops/inference-sh-cli/SKILL.md | official | MIT (repo) | - | terminal toolset; an inline-media-URL display convention | MEDIUM | OPTIONAL - convenient aggregator for many generative AI providers, but a third-party paid service dependency | - Use when a user wants to generate images, video, run search, or access other AI app categories without wiring up each provider's own API<br>- Always search the app catalog by keyword first rather than guessing an app id, since ids change frequently<br>- Run apps with the JSON output flag for structured, parseable results, and pass local file paths directly since the CLI auto-uploads them<br>- Present returned media URLs using the platform's inline-media convention so images/video render for the user<br>- Warn users that video generation can take 30-120 seconds and check authentication first if a call fails<br>- Verify success by checking authentication status before use and confirming the JSON response contains a valid output URL |
| pinggy-tunnel | skill | devops | A zero-install skill for exposing a local service to the public internet via an SSH reverse tunnel, with recipes for webhooks, MCP servers, LLM endpoints, and access-controlled dev-server sharing. | ssh client only, optional paid-tier token, terminal toolset (background mode) | none (free tier) / token (paid tier) | N/A | optional-skills/devops/pinggy-tunnel/SKILL.md | official | MIT (repo) | - | background-process/kill-by-session-id pattern, scratch-cache paths for logs/pids | HIGH | DEFAULT - zero-install, broadly useful networking utility with no vendor lock-in beyond a free SSH-based service | - Use whenever a local port needs a temporary public URL, e.g. sharing a dev server, receiving a webhook callback, or exposing an MCP/LLM endpoint<br>- Confirm the local origin is already listening, then launch the SSH reverse-tunnel command as a background process with output redirected to a log file so the public URL can be parsed out<br>- Stack access-control keywords into the SSH username (basic auth, bearer token, IP whitelist, CORS, forced HTTPS) for anything not meant to be fully public<br>- Remember the free tier caps sessions at 60 minutes, generates a random subdomain each restart, and allows only one tunnel per source IP; a paid token removes these limits<br>- Tear down cleanly by killing the background SSH process (or the platform's process-kill primitive) when the share is no longer needed<br>- Verify by curling the returned public URL and confirming it returns the same response as the local origin (a 502 means the local service isn't actually up yet) |
| setup-wizard-generator | skill | devops | Generates an interactive bash wizard script that walks a human step-by-step through a tedious manual setup or credential-provisioning procedure, capturing values and writing them to the right destinations. | bash, optional GitHub CLI (for CI secrets), a bundled shell script template | none | N/A | optional-skills/devops/setup-wizard-generator/SKILL.md | official | MIT (repo) | - | light - references env-file/config conventions and scratch/script paths, but the technique is generic | HIGH | OPTIONAL - useful niche generator for onboarding/credential-provisioning tasks, not needed for most conversations | - Use when a procedure needs a human to click through third-party dashboards or type in credentials that would be tedious to re-explain every time, such as service provisioning or CI secret setup<br>- First scope every manual step and the exact value each one produces by reading the repo's env files, docs, and CI workflows rather than guessing<br>- For each step, document the precise navigation path (site, menu, button) the human should follow to find the value<br>- Build the wizard from a reusable script template, writing one function-driven "stage" per step, using helpers to open URLs, prompt for secret vs public values, write them to environment files, and set CI secrets only for ones actually referenced<br>- Never execute the generated wizard yourself since it blocks on human input and opens a browser; validate it statically with a shell syntax check and by tracing that every captured value reaches its intended destination<br>- Verify by confirming the stage list matches what the user agreed on and that every CI secret name set by the script matches an actual reference in the CI configuration |
| watchers | skill | devops | Provides three ready-made polling scripts (RSS/Atom, GitHub, generic JSON API) with a shared watermark-based deduplication helper so an agent can be notified only of new items, typically wired into a scheduled job. | Python, terminal toolset, optional GitHub token, a writable state directory, a scheduler to invoke on interval | none (public feeds/APIs) / token optional (to avoid rate limits) | N/A | optional-skills/devops/watchers/SKILL.md | official | MIT (repo) | - | platform-home-directory paths for scripts and watermark state, scheduled-job integration pattern, terminal toolset | MEDIUM | DEFAULT - simple, dependency-light utility broadly useful for building "notify me when X changes" automations | - Use when a user wants to be notified only of new items from an RSS feed, a GitHub repo's issues/PRs/releases/commits, or an arbitrary JSON API<br>- Each watcher script fetches data, diffs it against a small stored watermark file of previously-seen IDs, writes the updated watermark, and prints only new items (nothing on no-change)<br>- The first run of any watcher always establishes a baseline and intentionally emits nothing, so don't expect an initial digest without special handling<br>- Wire the script into a recurring scheduled job that treats empty output as "stay silent" and non-empty output as "summarize and deliver"<br>- Keep the watermark's tracked-ID set bounded so it doesn't grow unbounded on high-churn feeds, and ensure the state directory is actually writable by the execution sandbox<br>- Verify by running a watcher once to confirm baseline behavior, then deleting its state file to force a controlled replay and confirm new items now surface correctly |
