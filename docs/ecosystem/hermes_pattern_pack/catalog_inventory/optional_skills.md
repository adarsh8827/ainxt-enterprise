## Autonomous AI Agents

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| agent-merge-conflict-arbiter | skill | autonomous-ai-agents | Provides a neutral third-party agent procedure for classifying and resolving git merge conflicts between two peer agents' branches without favoring either side. | terminal tool (git), read_file/patch/write_file/search_files, optionally kanban tool and delegate_task | none | N/A | optional-skills/autonomous-ai-agents/agent-merge-conflict-arbiter/SKILL.md | official | MIT (repo) | - | delegate_task, kanban_create/kanban_complete, terminal tool, patch/write_file/search_files tools | MEDIUM | OPTIONAL - valuable only for teams running parallel multi-agent git campaigns | - Use when two independent agent branches collide in a merge and neither original author should self-judge the conflict.<br>- Gather both sides' diffs and stated intent (commit messages, task/PR descriptions) before touching any file.<br>- Classify every conflicting hunk as disjoint (keep both), same-question-different-answer (pick one and say why), or superseded (keep the surviving side).<br>- Edit strictly within conflict markers, stage resolved files, then run the project's build/tests to confirm both intents survive.<br>- Main pitfall is unconscious bias toward whichever side spawned the arbiter, or "splitting the difference" into an untested hybrid; instead pick one answer and disclose it.<br>- Success is verified by a clean merge commit, no leftover conflict markers, passing tests, and a hand-back summary naming every hunk's resolution rationale. |
| antigravity-cli | skill | autonomous-ai-agents | Reference/operator guide for driving the Antigravity CLI ("agy") coding agent through Hermes' terminal tool, covering one-shot runs, PTY sessions, plugins, sandboxing and auth. | agy binary on PATH, terminal tool, tmux (for interactive sessions), Google account for OAuth login | OAuth | N/A | optional-skills/autonomous-ai-agents/antigravity-cli/SKILL.md | official | MIT (repo) | - | terminal tool (pty, background), process tool (poll/log/kill), read_file for inspecting agy's own config/log files | MEDIUM | OPTIONAL - only relevant when the operator has the Antigravity CLI installed as an alternate coding backend | - Use when you need Antigravity ("agy") as a worker/reviewer coding backend, or need to install/debug its auth, sandbox, or plugins.<br>- Distinguish shell wrapper commands (agy help/install/plugin/update) from in-session TUI slash commands, which only exist inside a running session.<br>- Prefer non-interactive `agy -p/--print` for one-shot tasks and plain-text output; use pty+tmux only for multi-turn interactive sessions.<br>- Background long runs with the terminal tool's background mode and poll/log via the process tool; bound run time with `--print-timeout` since there is no `--max-turns`.<br>- Key pitfall: `agy help` doesn't show slash commands, and `agy -p` returns plain text with no JSON envelope unlike Claude Code.<br>- Verify install/auth by checking `agy --version`, `agy plugin list`, and reading the settings/log files under `~/.gemini/antigravity-cli/`. |
| blackbox | skill | autonomous-ai-agents | Delegates coding tasks to the Blackbox AI CLI, a multi-model agent that runs a task across several LLMs and uses a judge to pick the best implementation. | Node.js 20+, npm package @blackbox_ai/blackbox-cli, Blackbox API key/account, terminal tool with pty | API key | N/A | optional-skills/autonomous-ai-agents/blackbox/SKILL.md | official | MIT (repo) | wraps Blackbox AI CLI (commercial, credit-based SaaS) | terminal(pty=true, background), process tool (poll/log/submit/kill) | MEDIUM | OPTIONAL - useful only if the user has a paid Blackbox account | - Use to fan work out to Blackbox's multi-model "chairman/judge" mode when you want several LLMs to attempt a task and auto-pick the best output.<br>- Install the CLI via npm, run `blackbox configure` to store the API key and pick providers, always invoke with `pty=true` since it's a full-screen TUI app.<br>- For long jobs, run in background mode and monitor/submit input through the process tool rather than killing slow sessions.<br>- Use checkpoints (`--resume-checkpoint`) to continue previous runs, and clone-to-temp for safe PR reviews.<br>- Key pitfall: credits/cost scale with multi-model mode, and the CLI will hang without a PTY.<br>- Verify by checking CLI install, confirming successful configuration, and reviewing the diff/output Blackbox produced after a run. |
| dynamic-workflow | skill | autonomous-ai-agents | A large-scale fan-out orchestration pattern combining a deterministic scripted pre-pass, batches of isolated subagents, adversarial claim verification, and multi-wave campaign integration for codebase-sized work. | delegate_task, execute_code sandbox, terminal tool, git worktrees, optionally kanban/cronjob for durability | N/A | N/A | optional-skills/autonomous-ai-agents/dynamic-workflow/SKILL.md | official | MIT (repo) | - | delegate_task concurrency limits, execute_code sandbox tool restrictions, kanban swarm, cronjob, HERMES_HOME/tmp conventions | LOW | DEFAULT - core reusable playbook for any large multi-agent fan-out or campaign-style task | - Use when a task has a clear repeatable unit (file/endpoint/record) and too many units for one context window; skip for small (under 10 unit) or serially-dependent work.<br>- Split work into a deterministic "Layer A" script (fetch/parse/merge, no LLM judgment) and a "Layer B" batch of isolated LLM subagent calls for the judgment-heavy step only.<br>- For quality-critical findings, run adversarial convergence: several independently-framed attempts producing falsifiable claims, then a second wave of refuters trying to break each claim, keeping only survivors.<br>- For codebase-wide campaigns, decompose into a shared brief, one file-cluster per worker, commit-per-verified-step, periodic integration rounds with full-suite verification, and a live QA wave distinct from unit tests.<br>- Key pitfalls: calling the delegation primitive from inside the sandboxed script, trying to read fan-out results in the same turn they were dispatched, and trusting a "completed" status label instead of checking the filesystem/commits.<br>- Success is verified per-round by manifest/output-file counts matching expected units, zero unmerged commits per branch, and full test-suite plus live-QA passes on the integrated tree. |
| grok | skill | autonomous-ai-agents | Orchestration guide for delegating coding work to xAI's Grok Build CLI via headless one-shot runs or interactive PTY/tmux sessions, including parallel worktree batches and PR review flows. | grok CLI (npm @xai-official/grok), SuperGrok/X Premium+ subscription (or XAI_API_KEY fallback), terminal tool, tmux | OAuth | N/A | optional-skills/autonomous-ai-agents/grok/SKILL.md | official | MIT (repo) | wraps xAI Grok Build CLI (commercial, subscription-gated) | terminal(pty, background), process tool, tmux capture-pane pattern | MEDIUM | OPTIONAL - alternative coding backend for users on xAI's subscription | - Use for feature work, refactors, PR review, or batch issue fixing when the user wants Grok specifically instead of Codex/Claude Code.<br>- Prefer headless `grok -p` one-shots for scripted tasks with `--output-format json`; use pty+tmux for interactive multi-turn sessions.<br>- Manage sessions by UUID (`--session-id` starts new, `--resume`/`--continue` resumes); background long runs and poll via the process tool.<br>- For parallel batches, create one git worktree per issue and launch independent headless Grok runs, then collect/push/PR after completion.<br>- Key pitfalls: Grok's login is subscription-gated and separate from Hermes' own xAI OAuth for `x_search`; always pass `--no-auto-update`; `--always-approve` is required for autonomous writes but should be omitted for read-only review.<br>- Verify with a cheap smoke test (`grok -p "Say ok."`) and by checking `~/.grok/auth.json` before relying on the CLI. |
| honcho | skill | autonomous-ai-agents | Sets up and tunes the Honcho cross-session memory service, giving each Hermes profile a distinct AI peer identity while sharing one accumulated model of the user. | honcho-ai pip package, Honcho cloud account (or self-hosted server), hermes CLI honcho subcommands | API key | N/A | optional-skills/autonomous-ai-agents/honcho/SKILL.md | official | MIT (repo) | - | honcho.json config file, HERMES_HOME, 5 honcho_* tools (profile/search/context/reasoning/conclude), hermes profile --clone flow | LOW | DEFAULT - memory quality materially affects every conversation; worth configuring well for any persistent deployment | - Use when setting up cross-session user memory, debugging memory not persisting, or configuring multiple profiles that should share user context but keep separate AI identities.<br>- Run `hermes memory setup honcho` to pick cloud or self-hosted, then verify with `hermes honcho status`.<br>- Tune three independent knobs - cadence (how often), depth (how many reasoning rounds), and level (how hard each round tries) - plus recall mode (hybrid/context/tools) and observation toggles per peer.<br>- Use the 5 tools deliberately: cheap no-LLM calls (profile/search/context) for routine lookups, and the LLM-backed `reasoning` tool only when synthesis is truly needed, since auto-injection already refreshes context each turn.<br>- Key pitfalls: forgetting `--clone` when creating a profile (it won't get its own AI peer), and expecting dashboard-changed observation settings to apply without a new session.<br>- Verify via `hermes honcho status`/`peers` output and confirming memory persists across a new session after a `honcho_conclude` write. |
| openhands | skill | autonomous-ai-agents | Wraps the model-agnostic, LiteLLM-backed OpenHands CLI for headless batch coding delegation to virtually any LLM provider, including local/open-source models. | uv, Python 3.12+, openhands CLI (uv tool install), LLM_API_KEY/LLM_MODEL/LLM_BASE_URL env vars, terminal tool | API key | N/A | optional-skills/autonomous-ai-agents/openhands/SKILL.md | official | MIT (repo) | - | terminal tool (background + timeout), process tool poll/log | MEDIUM | OPTIONAL - best when the user needs a non-Anthropic/non-OpenAI coding backend | - Use when a coding task should run on a specific non-Anthropic/non-OpenAI model (DeepSeek, Qwen, Ollama, vLLM, etc.) via LiteLLM, rather than the vendor-locked Claude Code/Codex siblings.<br>- Install with `uv tool install openhands --python 3.12`, set `LLM_MODEL`/`LLM_API_KEY`/`LLM_BASE_URL`, and always pass `--headless --json --override-with-envs --exit-without-confirmation`.<br>- Parse the JSONL event stream (MessageEvent/ActionEvent/ObservationEvent/FinishAction), filtering only lines starting with `{`; ignore LiteLLM/Authlib stderr noise.<br>- Resume a prior run using the dashed-UUID form printed at the end of the previous run, not the no-dash Conversation ID.<br>- Key pitfalls: without `--override-with-envs` it silently reads `~/.openhands/settings.json` instead of your env vars; the model slug format differs between native and OpenRouter routing.<br>- Verify by running a trivial prompt and confirming the JSONL stream ends in a `FinishAction` whose message echoes back an expected marker string. |

## Blockchain

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| evm | skill | blockchain | A zero-dependency, read-only CLI client for querying wallets, tokens, gas, ENS, contracts, and whale activity across 8 EVM chains with USD pricing. | Python 3.8+ stdlib only, public RPC endpoints, CoinGecko/ensideas.com/4byte.directory public APIs | none | N/A | optional-skills/blockchain/evm/SKILL.md | official | MIT (repo) | - | terminal tool, helper script under ~/.hermes/skills/blockchain/evm/scripts/, EVM_RPC_URL env override | HIGH | OPTIONAL - handy for users who want on-chain lookups, not needed by default | - Use for read-only queries: wallet portfolios, token metadata/price, transaction details/decoding, gas comparison, ENS lookups, allowance/security checks, contract inspection, and whale detection across 8 EVM chains.<br>- Invoke a single stdlib Python helper script with a subcommand (wallet, multichain, compare, tx, decode, gas, allowance, contract, ens, whale) and chain flag.<br>- Pricing comes from CoinGecko's free rate-limited API; ENS and 4byte decoding rely on single public endpoints with no fallback.<br>- Key pitfalls: known-token lists are only ~30 tokens per chain, L2 gas estimates don't include L1 data-posting fees, and whale/activity scans are limited to recent blocks only.<br>- No credentials required; can optionally point at a private RPC via an env var for higher rate limits.<br>- Verify with the `stats` subcommand (prints current block/gas/ETH price) and an ENS resolution check. |
| hyperliquid | skill | blockchain | A stdlib-only Python client for querying Hyperliquid perp/spot market data, account positions, fills, and generating heuristic post-trade reviews via the public info API. | Python 3.8+ stdlib, Hyperliquid public /info REST endpoint, optional HYPERLIQUID_USER_ADDRESS in .env | none | N/A | optional-skills/blockchain/hyperliquid/SKILL.md | official | MIT (repo) | - | terminal tool, script under ~/.hermes/skills/blockchain/hyperliquid/scripts/, reads HERMES_HOME/.env for defaults | HIGH | OPTIONAL - relevant to users doing crypto perp/spot trading analysis | - Use for read-only Hyperliquid market data (markets, spot pairs, candles, funding, order book) and account review (positions, spot balances, fills, orders, a heuristic PnL/trade review).<br>- No order placement or signing capability - purely informational, safe to run without exposing keys.<br>- The `review` command combines fills with market context to surface fee drag, concentration, and counter-trend loss heuristics; the `export` command produces a normalized JSON dataset for backtesting prep.<br>- Key pitfalls: public endpoints are rate-limited and only expose recent rolling history, not a full archive; L2 book snapshots are point-in-time only.<br>- Optional `.env` defaults (API URL for testnet, default user address) simplify repeated calls.<br>- Verify with the `markets --limit 5` command, which should print top markets by 24h volume. |
| solana | skill | blockchain | A stdlib-only Python client for querying Solana wallet portfolios, SPL tokens, transactions, NFTs, and whale transfers enriched with CoinGecko USD pricing. | Python 3.8+ stdlib, public Solana RPC endpoint, CoinGecko free API | none | N/A | optional-skills/blockchain/solana/SKILL.md | official | MIT (repo) | - | terminal tool, script under ~/.hermes/skills/blockchain/solana/scripts/, SOLANA_RPC_URL env override | HIGH | OPTIONAL - relevant only to users tracking Solana on-chain activity | - Use for wallet portfolio value, token/NFT holdings, transaction inspection, recent activity, network stats, and whale-transfer detection on Solana.<br>- Single Python stdlib script driven by subcommands (wallet, tx, token, activity, nft, whales, stats, price); `--no-prices` skips CoinGecko calls for speed.<br>- NFT detection is a heuristic (amount=1, decimals=0) and misses compressed NFTs/Token-2022 assets.<br>- Key pitfalls: public RPC and CoinGecko both rate-limit aggressively; whale detection and transaction history only cover a recent window, not full archives.<br>- No credentials needed; a private RPC endpoint can be set via an env var for production use.<br>- Verify with the `stats` subcommand, which should print the current slot, TPS, and SOL price. |

## Communication

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| one-three-one-rule | skill | communication | Defines a structured "1-3-1" response format - one problem statement, three labeled options with trade-offs, and one recommended path with a definition of done and plan. | none (pure prompting/response-structuring pattern) | N/A | N/A | optional-skills/communication/one-three-one-rule/SKILL.md | official | MIT (repo) | - | none - purely a response-formatting convention, no Hermes tools required | HIGH | OPTIONAL - nice-to-have for decision-heavy consulting-style interactions, not universally needed | - Use when a task has multiple viable technical approaches and the user wants a structured recommendation rather than an open-ended discussion.<br>- Write one problem sentence describing the desired outcome without implementation detail, then present exactly three genuinely distinct options labeled A/B/C with pros and cons each.<br>- Pick one option as the explicit recommendation with reasoning tied to the user's stated context/priorities, not a hedge.<br>- Follow with a concrete "definition of done" (verifiable outcomes) and an implementation plan (concrete steps/commands) for the recommended option.<br>- Key pitfall: presenting near-duplicate options instead of genuinely different strategies, or hedging instead of committing to one recommendation.<br>- Success is verified structurally: exactly one problem sentence, exactly three options with pros/cons, one clear recommendation, and DoD/plan consistent with whichever option the user ultimately picks. |

## Creative

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ai-presenter-video | skill | creative | Orchestrates a full presenter-video pipeline (script, TTS narration, avatar lip-sync generation, captions, and loudness-normalized export) with consent gates and visual QA baked in. | ffmpeg, ffprobe, python3, Hermes tools (image_generate, text_to_speech, vision_analyze, STT/whisper), FAL video/image providers | API key | N/A | optional-skills/creative/ai-presenter-video/SKILL.md | official | MIT (repo) | - | HERMES_SKILL_DIR token, job.json state machine, image_generate/text_to_speech/vision_analyze tool names | MEDIUM | OPTIONAL - complex multi-step paid video pipeline, valuable but heavy/niche | - Use when a user wants a full narrated presenter-style video from a script/topic plus one approved presenter photo.<br>- Steps: init a job folder, manually review image/voice inputs, run a preflight consent gate, lock narration audio via TTS+ASR verification, generate the avatar clip (pilot first), edit on an ffmpeg timeline with captions, then run the finalize script for loudness-normalized master/share exports plus a contact sheet.<br>- Pitfalls: never upload the presenter image before remote-upload consent is granted; identity drifts across regenerated avatar clips, so prefer one continuous source; ffmpeg/ffprobe/jq/awk must be installed.<br>- Verified by inspecting the contact sheet and decoded output files, not just checking exit codes. |
| archify | skill | creative | Thin catalog stub that pulls a live, upstream-maintained Node CLI for turning JSON specs or Mermaid into validated, explorable interactive HTML architecture/workflow/sequence diagrams. | Node.js 18+, live GitHub fetch of tt-a1i/archify repo tree | none | N/A | optional-skills/creative/archify/SKILL.md | official | MIT (repo) | - | `hermes skills install official/creative/archify` catalog-stub install mechanism | HIGH | OPTIONAL - useful diagram tool, but this entry is a stub that fetches the real content live at install time | - Use when a validated, richly interactive HTML diagram (architecture/workflow/sequence/dataflow/state machine) with export options is needed, beyond a static SVG.<br>- It is a stub: the actual Node CLI, schemas, and renderers are fetched live from the upstream GitHub repo at install time rather than vendored.<br>- Main steps: install pulls roughly 200 files pinned to one commit SHA, then a Node CLI turns a JSON spec or pasted Mermaid into a diagram, validated by a 9-check pipeline.<br>- Pitfall: relies on a live network fetch from GitHub at install time; verification is upstream's own receipt system, not described locally. |
| ascii-art | skill | creative | Grab-bag of local CLIs and free public APIs for producing text banners, cowsay/boxes decorations, image-to-ASCII conversion, and curated ASCII art lookup. | pyfiglet (pip), cowsay, boxes, toilet, ascii-image-converter or jp2a, curl, public APIs (asciified.thelicato.io, ascii.co.uk, wttr.in, qrenco.de) | none | N/A | optional-skills/creative/ascii-art/SKILL.md | official | MIT (repo) | - | none beyond the generic terminal tool | HIGH | OPTIONAL - fun/novelty feature, low risk, broadly usable | - Use for banners, cowsay/boxes decoration, image-to-ASCII conversion, or fetching curated ASCII art/weather/QR-code novelties.<br>- Steps: pick the right of about 9 tool options (pyfiglet, a free REST API, cowsay, boxes, toilet, image converters, curated-art scraping, or LLM-generated Unicode art) based on the request type.<br>- Pitfall: several tools require separate OS package installs (cowsay, boxes, toilet, ascii-image-converter); fall back gracefully if unavailable.<br>- Verified simply by eyeballing the printed/generated output for legibility and correct character set. |
| audiocraft-audio-generation | skill | creative | Reference guide for running Meta's AudioCraft (MusicGen/AudioGen/EnCodec) models locally to generate music and sound effects from text prompts. | Python, audiocraft or transformers+torch+torchaudio packages, GPU recommended | none | N/A | optional-skills/creative/audiocraft-audio-generation/SKILL.md | official | MIT (repo) | - | none | HIGH | SKIP - heavy GPU/local ML setup, largely superseded by hosted generation tools, poor fit for a shared server | - Use when a user wants locally generated music or sound effects from a text description, with no external API dependency.<br>- Steps: install audiocraft or transformers+torch, load a MusicGen/AudioGen/EnCodec model variant, set generation parameters (duration, temperature, cfg), and save output audio.<br>- Pitfalls: significant GPU VRAM requirements scaling with model size; melody/style conditioning needs specific model variants, not the base one.<br>- Verified by confirming the saved WAV file plays and matches the requested duration/sample rate. |
| auteur | skill | creative | A director-style workflow with executable anti-slop gates for building distinctive, cinematic, or multi-screen web experiences instead of generic AI-templated pages. | Node 18+, Playwright+chromium, ffmpeg, Hermes tools (image_generate, terminal, vision_analyze, browser_exec) | none | N/A | optional-skills/creative/auteur/SKILL.md | official | MIT (repo) | - | image_generate, terminal, vision_analyze, browser_exec tool names; `auteur-allow` override convention | MEDIUM | OPTIONAL - powerful but heavyweight design pipeline requiring a Node/Playwright toolchain | - Use for a landing page, marketing site, or multi-screen product build that must avoid generic "AI slop" visual defaults.<br>- Main steps: route into build/direct/system/edit/recon/audit register, fill a "commit-sheet" of concrete design decisions before writing code, build against reference recipes, then run automated slopscan/motionqa/systemscan gates before delivery.<br>- Pitfalls: several QA scripts (Playwright-based) were not fully executed during porting and should be treated as unverified until run; never skip the commit-sheet or gates just because output "looks done."<br>- Verified by slopscan exiting 0, reviewed screenshot journeys at multiple viewports, and a numeric rubric (contrast, LCP, CLS). |
| baoyu-article-illustrator | skill | creative | Analyzes an article, plans illustration placements, and generates consistent type/style/palette images to insert back into the article. | Hermes tools (image_generate, vision_analyze, write_file/read_file, terminal/curl, clarify) | none | N/A | optional-skills/creative/baoyu-article-illustrator/SKILL.md | official | MIT (repo) | - | image_generate aspect_ratio enum mapping, clarify tool, write_file/read_file conventions | MEDIUM | OPTIONAL - useful content-illustration add-on | - Use when a user wants illustrations added to an existing article with type/style/palette consistency.<br>- Steps: analyze the article, confirm type/density/style/palette via one-at-a-time clarifying questions, write an outline and per-image prompt files, generate via image_generate, download the returned URLs locally, then insert images into the article.<br>- Pitfalls: image_generate returns a URL not a file, so always download via terminal/curl; it also takes no reference-image input, so any supplied reference must be described in text via vision_analyze first.<br>- Verified by confirming every image has a saved prompt file and the article's inserted image count matches the outline. |
| baoyu-comic | skill | creative | Turns source text into a multi-page knowledge/educational comic with configurable art style, tone, layout, and character consistency via text-described traits. | Hermes tools (image_generate, clarify, terminal/curl) | none | N/A | optional-skills/creative/baoyu-comic/SKILL.md | official | MIT (repo) | - | image_generate aspect_ratio mapping, clarify tool | MEDIUM | OPTIONAL - useful niche content-creation add-on | - Use when a user wants an original multi-page knowledge/educational comic in a chosen art style, tone, and layout.<br>- Steps: analyze source content, confirm style/tone/layout/preset via sequential clarify questions, generate a storyboard and character sheet, write per-page prompt files embedding character text descriptions, then generate and download each page image.<br>- Pitfalls: image_generate has no image input so character consistency comes from text descriptions, not the character PNG; always use absolute paths for curl -o downloads since persistent shell CWD can silently drift between batches.<br>- Verified by confirming every page has a prompt file, an existing non-empty PNG at the correct absolute path, and a completion report matching the page count. |
| comfyui | skill | creative | Full lifecycle and execution toolkit (setup, hardware check, workflow param injection, batch runs, monitoring) for running ComfyUI diffusion pipelines locally or on Comfy Cloud. | comfy-cli, ComfyUI server (local or Comfy Cloud), Python, optional websocket-client, GPU | API key | N/A | optional-skills/creative/comfyui/SKILL.md | official | MIT (repo) | wraps ComfyUI (GPL-3.0 license) | none beyond generic terminal/python scripts calling the ComfyUI REST API | HIGH | OPTIONAL - powerful generative pipeline but a heavy external system with a GPL dependency | - Use when a user wants to run Stable Diffusion/Flux/SD3/video/audio diffusion workflows, whether locally or via Comfy Cloud.<br>- Steps: run a hardware check to pick local vs cloud, install via comfy-cli, get a workflow in API-format JSON, inspect its parameters/dependencies with the bundled scripts, then submit/monitor/download via run_workflow.py or run_batch.py.<br>- Pitfalls: workflows must be exported in API format, not editor format; ComfyUI itself is GPL-3.0 licensed, a heavier dependency than the MIT-licensed skill wrapper; free-tier cloud accounts cannot call the execution API.<br>- Verified via the bundled health_check.py checklist (comfy-cli present, server reachable, checkpoints installed, smoke-test workflow runs to completion). |
| concept-diagrams | skill | creative | A flat, minimal SVG design system and template for producing clean, dark/light-mode-ready educational diagrams as a single standalone HTML file. | none (pure HTML/SVG/CSS, optional local static server) | none | N/A | optional-skills/creative/concept-diagrams/SKILL.md | official | MIT (repo) | - | skill_view() loader convention for templates/examples | HIGH | OPTIONAL - lightweight, zero-dependency, broadly useful diagram fallback | - Use for physics/chemistry/engineering/anatomy/process-style educational SVG diagrams when no more specialized diagram skill fits.<br>- Steps: choose a diagram type, lay out shapes/text under a strict flat-minimal design system (fixed color ramps, two font sizes, 0.5px strokes), paste the SVG into the provided HTML template, and save as a standalone file.<br>- Pitfalls: a 12-point validation checklist (arrow defs, text classes, no gradients/shadows, box-width formulas) must be checked before finalizing, or output looks inconsistent with the design system.<br>- Verified by opening the HTML file directly in a browser with no dependencies. |
| creative-ideation | skill | creative | A routed library of 20+ named ideation methods (TRIZ, SCAMPER, OuLiPo, etc.) that matches a user's creative situation to a specific brainstorming technique instead of generic listicle output. | none | none | N/A | optional-skills/creative/creative-ideation/SKILL.md | official | MIT (repo) | - | none | HIGH | OPTIONAL - broadly useful brainstorming method library with no dependencies | - Use for any open-ended "give me ideas" / "help me choose" / "I'm stuck" request across writing, art, engineering, business, or personal decisions.<br>- Steps: extract phase/domain/specificity signals from the request, apply override rules for weird/high-slop requests, route to one of about 22 named methods (TRIZ, SCAMPER, OuLiPo, biomimicry, etc.), then generate output in that method's own format rather than a generic bullet list.<br>- Pitfall: defaulting to a bare "Here are 5 ideas" list without routing defeats the purpose of the skill; must refuse the first few obvious ideas before delivering.<br>- Verified by confirming the named method is disclosed, every idea has a concrete mechanism and honest tradeoff, and at least one idea is genuinely buildable now. |
| draw-your-font | skill | creative | Converts photographed handwriting samples into an installable TTF/WOFF font via a local CLI, with the agent handling visual QA and letter labeling. | Node 18+/npx, npm package draw-your-font, Hermes vision_analyze tool | none | N/A | optional-skills/creative/draw-your-font/SKILL.md | official | MIT (repo) | - | skill loader path convention (~/.hermes/skills), vision_analyze | MEDIUM | OPTIONAL - niche fun feature (photo-to-font) | - Use when a user wants their own handwriting turned into an installable font from photographed writing samples.<br>- Steps: optionally print/fill a template sheet, segment photos into per-letter crops via the CLI, visually label each detected blob with vision_analyze, build the font, then judge the rendered preview/glyph sheet before delivering the TTF.<br>- Pitfalls: never hand-edit SVG paths yourself, only the CLI does geometry; rebuilds after label fixes are cheap, so iterate rather than re-shoot unless the source ink itself is bad.<br>- Verified by inspecting the preview/glyph PNGs for broken traces, wrong weight, or mislabeled letters before delivery. |
| dream-loop | skill | creative | An iterative build-screenshot-judge loop that pushes 3D/WebGL scenes toward AAA-game visual fidelity using generated concept art as the target. | Hermes tools (image_generate, browser_exec, vision_analyze, delegate_task), optional Blender | none | N/A | optional-skills/creative/dream-loop/SKILL.md | official | MIT (repo) | - | image_generate, vision_analyze, browser_exec, delegate_task tool names | MEDIUM | OPTIONAL - narrow, high-effort 3D/WebGL visual fidelity loop | - Use for building an especially visually impressive 3D/WebGL scene or game where fidelity matters more than functionality.<br>- Steps: generate photorealistic "in-engine screenshot" concept art, build toward it, screenshot the live build, self-audit, then have a fresh judge (via delegate_task) score it against a gated 5-tier ladder and issue actionable directives, iterating until an exit criterion is hit.<br>- Pitfalls: concept art can be overbaked (grainy/cluttered) or oversimplified (cartoonish), both wreck the loop; screenshots must match the concept's resolution/aspect for fair comparison; don't submit half-finished screenshots to the judge.<br>- Verified by an explicit exit criterion (score at least 8 with acceptable FPS) plus artifacts for every round (screenshot, self-assessment, judge verdict). |
| excalidraw | skill | creative | Hand-writes Excalidraw JSON element files (with element/format reference) for diagrams that open directly in the free excalidraw.com editor. | none (write_file only) | none | N/A | optional-skills/creative/excalidraw/SKILL.md | official | MIT (repo) | - | none | HIGH | OPTIONAL - lightweight, useful diagram generator with no dependencies | - Use for architecture diagrams, flowcharts, or concept maps that need a hand-drawn Excalidraw look with no accounts or rendering libraries.<br>- Steps: write an array of Excalidraw element JSON objects (rectangles/ellipses/diamonds/arrows/text) with container-bound labels, wrap in the standard envelope, and save as a `.excalidraw` file, optionally uploading for a shareable link.<br>- Pitfall: a `"label"` property placed directly on shapes is silently ignored, labels must use the boundElements/containerId binding pattern instead.<br>- Verified by opening the file at excalidraw.com and checking labels render correctly and z-order/spacing look clean. |
| heartmula | skill | creative | Detailed installation/patch/run guide for self-hosting the open-source HeartMuLa model family to generate full songs from lyrics and style tags. | git, Python 3.10 venv, PyTorch/CUDA, HuggingFace CLI, several GB of model checkpoints | none | N/A | optional-skills/creative/heartmula/SKILL.md | official | MIT (repo) | - | none | HIGH | SKIP - requires manual source-code patching and a fragile GPU-heavy install, too brittle for general use | - Use when a user wants a fully local, open-source Suno-style song generator from lyrics plus style tags.<br>- Steps: clone heartlib, create a Python 3.10 venv, apply two required source-code patches (RoPE cache reinit, HeartCodec size-mismatch tolerance) plus dependency upgrades, download several GB of model checkpoints, then run the generation script with lyrics/tags files.<br>- Pitfalls: CPU-only generation is extremely slow (30-60+ minutes per song) and needs significant RAM; RTX 5080 incompatibility and tag-following issues are known upstream bugs.<br>- Verified by confirming the generated MP3 exists at 48kHz stereo and roughly matches the requested duration (about 1x real-time factor). |
| hyperframes | skill | creative | Renders MP4/WebM videos deterministically from HTML+GSAP compositions captured frame-by-frame, covering captions, TTS, transitions, and audio-reactive visuals. | Node 22+, ffmpeg, npx hyperframes CLI, Puppeteer/chrome-headless-shell, optional espeak-ng | none | N/A | optional-skills/creative/hyperframes/SKILL.md | official | Apache-2.0 | - | terminal toolset requirement declared in frontmatter | HIGH | OPTIONAL - capable video-rendering pipeline, needs Node/ffmpeg/Chrome workers | - Use when a video needs to be rendered deterministically from an HTML/CSS/GSAP composition (motion graphics, captioned narration, audio-reactive visuals, scene transitions).<br>- Steps: scaffold a project, define a visual-identity DESIGN.md before writing any composition HTML, build the static "hero frame" first, add GSAP timelines, add transitions between scenes, then lint/validate/inspect before rendering to MP4/WebM via ffmpeg.<br>- Pitfalls: `preview` runs a long-lived Chrome-worker server that must be explicitly stopped or it pegs a CPU core indefinitely on GPU-less hosts; timelines must be built synchronously with finite repeat counts, never `repeat: -1`.<br>- Verified via lint+validate+inspect passing, confirming the output file's duration/audio via ffprobe, and reviewing an extracted mid-video frame. |
| impeccable | skill | creative | Catalog stub for an upstream-maintained, tool-agnostic frontend design language skill with 23 sub-commands and a deterministic anti-slop detector CLI. | delegated to the upstream repo pbakaus/impeccable (Hermes-native skill bundle, Node CLI detector) | none | N/A | optional-skills/creative/impeccable/SKILL.md | official | Apache-2.0 | - | `hermes skills install impeccable` catalog-stub mechanism | HIGH | SKIP - stub delegating entirely to an externally-maintained repo bundle, no substantive local content to reproduce | - Use for general frontend design critique/improvement guidance across 23 sub-commands (init, craft, critique, audit, polish, etc.) with a deterministic anti-pattern detector.<br>- It is a stub: the real skill bundle and its 61-rule `npx impeccable detect` CLI are fetched live from the upstream repo at install time.<br>- Main step: after install, invoke `/impeccable init` to begin; the detector needs no LLM or API key to run.<br>- No further local content to verify beyond confirming the upstream install succeeded. |
| ip-as-logo | skill | creative | Guides generation of extremely simplified, 32px-readable cute mascot/IP character marks using strict shape, color, and composition constraints. | Hermes image_generate tool, optional vision_analyze/delegate_task | none | N/A | optional-skills/creative/ip-as-logo/SKILL.md | official | MIT (repo) | - | image_generate aspect_ratio="square", delegate_task, `hermes tools` listing convention | MEDIUM | OPTIONAL - niche logo/mascot generation feature | - Use when a user wants a minimal, cute mascot/IP character logo readable at 32 by 32 pixels, not a detailed illustration.<br>- Steps: infer or ask about product context, propose three distinct design directions, generate two independent candidates per direction (six total) via image_generate under a strict complexity budget (4-7 shapes, 3 semantic colors, rounded shapes only), and deliver every result unfiltered.<br>- Pitfalls: the generation prompt must never call the output a "logo" or "icon" (triggers unwanted mockup/badge framing); do not inspect, rank, or auto-retry candidates, treat generation as a one-pass stochastic draw.<br>- Verified by confirming every candidate is labeled with its direction, corner assignment, and prompt/color mapping in the final report. |
| kanban-video-orchestrator | skill | creative | Meta-pipeline that scopes a video request, designs a specialist multi-agent team, and bootstraps Hermes kanban profiles/tasks to execute the production, without rendering anything itself. | Hermes kanban toolset, hermes profile/CLI commands, whichever rendering skills the team needs (ascii-video, manim-video, p5js, comfyui, etc.) | API key | N/A | optional-skills/creative/kanban-video-orchestrator/SKILL.md | official | MIT (repo) | - | hermes profile create --clone, hermes kanban create/watch/list, hermes dashboard, SOUL.md/TEAM.md conventions, ~/.hermes/.env key convention | LOW | OPTIONAL - meta-orchestration for complex multi-agent video pipelines, narrow/advanced use | - Use for video requests complex enough to need a multi-agent production team (narrative shorts, music videos), not single continuous or one-shot conversions.<br>- Steps: discover scope via adaptive questioning, write a structured brief, design a team of specialist Hermes profiles from a role library, generate and run a setup script that creates profiles/workspace/kanban task, then monitor and intervene as the director profile decomposes work.<br>- Pitfalls: every task must share one workspace directory and a project-specific tenant; verify required API keys exist before firing tasks to avoid wasting a task slot; don't over-decompose a short video into an oversized task graph.<br>- Verified by watching the kanban board (`hermes kanban watch/list`) and confirming each specialist's output before it's accepted. |
| meme-generation | skill | creative | Generates real meme PNG files by overlaying captions on about 100 popular templates (or an AI-generated scene) via a Pillow-based script. | Python, Pillow, imgflip template API, optional Hermes image_generate/vision_analyze | none | N/A | optional-skills/creative/meme-generation/SKILL.md | official | MIT (repo) | - | MEDIA: delivery convention, image_generate/vision_analyze tool names | MEDIUM | OPTIONAL - fun lightweight feature, broadly usable | - Use whenever a user wants an actual meme image (PNG) generated, not just meme text.<br>- Steps: match the topic to one of about 100 imgflip templates (10 curated with tuned text placement) or generate a custom AI scene via image_generate, then overlay short captions with the bundled Pillow script, optionally verifying custom-image legibility via vision_analyze.<br>- Pitfalls: captions must stay short (8-12 words) or the meme looks bad; caption count must match the template's field count; never generate hateful or personally targeted content.<br>- Verified by confirming a non-empty PNG exists at the output path with legible white-with-black-outline text. |
| mono-color | skill | creative | Produces one- or two-ink editorial print poster imagery from a catalog-driven recipe of substrate, palette, layout, and typography choices. | Hermes image_generate tool, bundled design-system JSON catalogs | none | N/A | optional-skills/creative/mono-color/SKILL.md | official | MIT (repo) | - | image_generate tool | MEDIUM | OPTIONAL - niche poster/print design generator | - Use for a monochrome/duotone editorial poster, risograph/zine-style print, or halftone photo treatment, not for any request merely mentioning a color.<br>- Steps: extract subject/intent/text/image-role/representation from the request, resolve a full recipe manifest against bundled JSON catalogs (colors, compositions, typography, rhythm, imperfections, carriers), compile a five-paragraph prompt, generate via image_generate, and inspect/regenerate once against a quality-gate checklist.<br>- Pitfalls: never use more than two printing inks; catalog values always override any prose description when they conflict; must preserve user-supplied text verbatim without translation.<br>- Verified against the quality-gate checklist covering ink count, empty-paper percentage, typographic scale jump, and structural originality versus any reference. |
| pixel-art | skill | creative | Converts photos into era-accurate retro pixel art (NES, Game Boy, PICO-8, etc.) and optionally animates them into short looping videos with particle effects. | Python 3.9+, Pillow, ffmpeg (for video) | none | N/A | optional-skills/creative/pixel-art/SKILL.md | official | MIT (repo) | - | clarify tool for style/scene selection | HIGH | OPTIONAL - fun niche image-style conversion feature | - Use when a user wants a photo converted to retro pixel art (NES, Game Boy, PICO-8, etc.) with optional short looping animation.<br>- Steps: confirm the style preset (and animation scene, if wanted) via at most two clarify calls, then run the pixel-art conversion script (contrast boost, posterize, downscale, dithered palette quantize, upscale) and optionally the video-overlay script for particle effects.<br>- Pitfalls: palette/preset keys are case-sensitive; very small source images collapse under large pixel blocks; animation particle density is tuned for roughly 640x480 canvases.<br>- Verified by checking the output PNG shows clear pixel blocks at the expected size/color count, and any MP4 opens cleanly via ffprobe. |
| pretext | skill | creative | Guides building single-file browser demos using the DOM-free `pretext` text-layout library for kinetic typography, text-flow-around-obstacle, and text-as-game-geometry effects. | esm.sh CDN (@chenglou/pretext), browser Canvas2D, Intl.Segmenter | none | N/A | optional-skills/creative/pretext/SKILL.md | official | MIT (repo) | - | none | HIGH | OPTIONAL - niche creative-coding/typography demo generator | - Use for browser-based creative-coding demos that need real prose to flow, shatter, or form geometry using the DOM-free @chenglou/pretext text-measurement library.<br>- Steps: pick a demo pattern (reflow-around-obstacle, text-as-game-geometry, particle shatter, kinetic type, etc.), start from a bundled HTML template, swap in real corpus text, tune font/palette/interaction, and serve locally to verify.<br>- Pitfalls: canvas font strings must exactly match rendered CSS or measurements drift; the expensive prepare() call must happen once and be cached, never per animation frame.<br>- Verified via a checklist covering single-file self-containment, real corpus text, matched font strings, at least one interaction, and no console errors when served locally. |
| simple-english | skill | creative | Rewrites or audits technical text against the ASD-STE100 Simplified Technical English controlled-language standard to eliminate ambiguity and AI-writing tells. | none (pure text rules) | none | N/A | optional-skills/creative/simple-english/SKILL.md | official | MIT (repo) | - | none beyond generic read_file/patch/write_file | HIGH | OPTIONAL - useful technical-writing style enforcer, narrow use case | - Use for rewriting or auditing technical documentation, runbooks, error messages, or agent instructions into unambiguous, AI-slop-free controlled English.<br>- Steps: choose pragmatic or strict mode, classify each passage as procedural or descriptive (different sentence-length limits and verb forms), apply the 53-rule catalog (approved words, banned modals like "should"/"could", one term per concept), then run a mandatory four-point self-check before delivering.<br>- Pitfalls: never invent rule numbers from memory (they're commonly hallucinated); never touch code blocks, identifiers, or quoted errors; don't apply it to marketing/brand copy, which it will flatten.<br>- Verified by the explicit self-check (sentence-length scan, contraction/passive-voice search, condition-before-command ordering, consistent verb choice) or the full checklist reference. |
| sketch | skill | creative | Produces 2-3 disposable, interactive HTML mockups exploring different design stances so a user can compare directions before committing to a build. | Hermes tools (browser_navigate, browser_vision), optional Tailwind CDN | none | N/A | optional-skills/creative/sketch/SKILL.md | official | MIT (repo) | - | browser_navigate/browser_vision tool names | MEDIUM | OPTIONAL - useful lightweight UI-mockup generator | - Use when a user wants to compare 2-3 disposable visual design directions for a screen before committing to a build, not a production component.<br>- Steps: intake feel/references/core-action one question at a time, build 2-3 self-contained interactive HTML variants that each take a genuinely different design stance, verify each visually with browser tools, then present a head-to-head comparison table with an opinionated recommendation.<br>- Pitfalls: variants that differ only in accent color waste the exercise; a frozen static mockup with no interaction is a weaker deliverable than a rough interactive one.<br>- Verified by loading each variant in a browser and using vision-based inspection to catch rendering bugs. |
| social-media-content-calendar | skill | creative | Plans a multi-platform social campaign end-to-end - briefs, drafts, review, and scheduling/handoff - while delegating platform-specific publishing to connector skills. | Hermes tools (read_file, web_extract, image_generate, humanizer skill), platform connector skills (e.g. xurl) | OAuth | N/A | optional-skills/creative/social-media-content-calendar/SKILL.md | official | MIT (repo) | - | xurl skill, humanizer skill, web_extract/image_generate tools | MEDIUM | OPTIONAL - useful marketing/planning workflow | - Use for planning a multi-platform social campaign end-to-end, not for a single one-off post.<br>- Steps: define campaign constraints, inventory verified source material, build a themed calendar across platforms, write platform-adapted briefs, draft copy/assets (delegating voice to the humanizer skill and images to image_generate), run an editorial/risk review, then schedule or hand off per-platform using connector skills.<br>- Pitfalls: identical copy pasted across every platform; publishing from "draft" or "needs review" state; conflating an approved asset with an actually scheduled/published post.<br>- Verified by confirming every post traces to a campaign objective, has a disposition and owner, and that published vs. handed-off status is accurately reported per slot. |
| system-atlas | skill | creative | Builds a single-source-of-truth data file that renders into both an interactive isometric architecture map and a generated Markdown text twin, with question tracking across feedback rounds. | Node.js (built-in fs/path/url only), static server for verification | none | N/A | optional-skills/creative/system-atlas/SKILL.md | official | MIT (repo) | - | delegate_task tool (optional), skill_view convention | HIGH | OPTIONAL - niche architecture-documentation tool | - Use for an evolving architecture that benefits from an explorable isometric map plus a generated text twin, especially across multiple feedback rounds with open questions to track.<br>- Steps: read inputs and discuss structure before drawing, build a data.mjs single source of truth, generate the atlas HTML and SYSTEM.md via a Node build script, apply progressive disclosure (revealing structures a few at a time via chapters), and track feedback as ID-tagged questions across rounds.<br>- Pitfalls: never hand-edit the generated atlas.html or SYSTEM.md; a stray render call inside a hover handler can silently break click handling; never delete a question ID, only resolve/route/drop it.<br>- Verified by the build script exiting 0, manually clicking through the rendered atlas at multiple viewport sizes, and confirming SYSTEM.md contains the decisions table and question index. |
| tldraw-offline | skill | creative | Drives the tldraw offline desktop app's local HTTP API to script canvas edits and embed durable, reactive/interactive JavaScript behavior into .tldraw files. | tldraw offline desktop app (local HTTP API on localhost:7236), curl/jq | API key | N/A | optional-skills/creative/tldraw-offline/SKILL.md | official | MIT (repo) | wraps tldraw offline desktop app (proprietary, license unclear) | none, pure local HTTP API, not Hermes-specific | HIGH | OPTIONAL - requires a locally running desktop app, narrow use case | - Use when the user has the tldraw offline desktop app open and wants canvas edits or durable, reactive/interactive behavior scripted into the .tldraw file.<br>- Steps: read the port/token from the app's server.json on every call (it doesn't persist across shell calls), use /exec for one-off layout edits or edit script/main.js for durable behavior that survives reload, and make scripts idempotent since they rerun on every load.<br>- Pitfalls: store.listen fires on the tick after a commit, not synchronously; event listeners must be torn down on the abort signal or duplicate handlers double-fire clicks; raw shape records built outside createShape need every schema prop filled in.<br>- Verified via /api/doc/:id/script-status reporting state applied, or reading back shapes/screenshots through the search API. |
| unreal-mcp | skill | creative | Automates the Unreal Engine editor (scene building, lighting, actors, cameras, rendering) through Epic's official experimental MCP plugin via tool discovery and serial tool calls. | Unreal Editor 5.8+ with Unreal MCP + AllToolsets plugins, Hermes MCP catalog entry | none | N/A | optional-skills/creative/unreal-mcp/SKILL.md | official | MIT (repo) | wraps Unreal Engine (proprietary EULA, commercial) | mcp_unreal_engine_* tool naming convention, `hermes mcp install`, ~/.hermes/config.yaml mcp_servers entry | LOW | OPTIONAL - narrow game-dev automation, needs a local Unreal Editor GUI running | - Use for any in-editor Unreal Engine task, level dressing, actor placement, lighting, materials, camera framing, or rendering, driven entirely through the MCP plugin rather than GUI clicking.<br>- Steps: ensure the Unreal Editor and its MCP+AllToolsets plugins are running before starting the Hermes session, discover the live tool surface (list_toolsets, describe_toolset, call_tool), then work in small single-purpose serial calls, verifying and saving after each milestone.<br>- Pitfalls: calls must never overlap (the server runs them serially on the game thread and parallel Hermes tool-calling will deadlock it); some tool calls silently no-op on property writes, so always read values back; the plugin is experimental and its data-handling falls under the Unreal Engine EULA.<br>- Verified by re-querying changed actors/properties, capturing and reviewing viewport screenshots, and confirming the level was saved after each milestone. |

## Data Science

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| jupyter-notebook | skill | data-science | Gives the agent a stateful Python REPL by driving a live JupyterLab kernel through a helper CLI, useful for iterative data-science exploration where variables must persist across calls. | uv, JupyterLab (uv tool install jupyterlab), hamelnb helper script (cloned from GitHub), a running Jupyter server, curl for REST session creation | none | N/A | optional-skills/data-science/jupyter-notebook/SKILL.md | official | MIT (repo) | - | contrasted explicitly against Hermes' own execute_code and terminal tools; uses ~/.hermes/cache/scratch for logs | MEDIUM | OPTIONAL - valuable for genuine iterative data-science/ML work, overkill for one-shot scripts | - Use instead of a stateless code-execution tool when you need variables/imports/objects to persist across multiple execution steps (data exploration, ML iteration, API probing).<br>- Set up by installing JupyterLab via uv, cloning the hamelnb helper repo, starting a headless Jupyter server with auth disabled on loopback, and creating a kernel session via the REST API.<br>- Drive execution, variable inspection, and cell editing through the helper script's subcommands (execute/variables/contents/edit), always with `--compact` to save tokens.<br>- Use `restart-run-all` only for clean end-to-end verification, not for routine iteration.<br>- Key pitfalls: first execution after server start may time out (just retry), packages must be installed into the Jupyter server's own Python environment, and websocket transport can silently break requiring a `--transport zmq` fallback.<br>- Success is verified by successful `execute` calls returning results and, for full verification, a clean `restart-run-all` pass. |

## DevOps

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| actual-setup | skill | devops | Configures actual.inc (Actual Computer) as a first-class Hermes inference provider, covering both the hosted encrypted relay and a fully local on-device daemon. | actual.inc account + ac_ API key (relay mode) or the locally installed/authorized Actual daemon (local mode), curl, hermes config CLI | API key | N/A | optional-skills/devops/actual-setup/SKILL.md | official | MIT (repo) | wraps actual.inc daemon (proprietary local binary, license unclear) | built-in `actual` provider id, ~/.hermes/.env (ACTUAL_API_KEY/ACTUAL_BASE_URL), hermes config set model.provider/model.default, hermes chat -Q verification | LOW | OPTIONAL - only for users choosing Actual Computer as their inference provider | - Use to add actual.inc as a Hermes LLM provider, either the hosted encrypted relay (needs an ac_ API key) or a fully local on-device daemon (needs human browser device authorization).<br>- For relay mode, store the key in .env, discover models via the relay's /v1/models endpoint, then set provider/model config and verify with a scripted chat reply.<br>- For local mode, wait for the human to authorize the daemon in a browser, download a specific quantization, load it by its installed name, then point Hermes at the loopback base URL (which auto-enables no-auth mode).<br>- Key pitfalls: small local models can overflow the context window under Hermes' full default toolset (restrict tools or use a larger-context model), and reasoning-effort levels must be clamped to what the backend accepts.<br>- Never invent a device-authorization email or approve on the user's behalf - always relay the browser URL and wait.<br>- Verify via a scripted chat call requesting an exact string reply, plus `hermes status` showing the correct key source. |
| docker-management | skill | devops | A reference procedure for managing Docker containers, images, volumes, networks, and Compose stacks via standard CLI commands, including troubleshooting and Dockerfile optimization tips. | Docker Engine + Docker Compose v2, terminal tool | none | N/A | optional-skills/devops/docker-management/SKILL.md | official | MIT (repo) | - | terminal tool only; otherwise pure Docker CLI knowledge | HIGH | DEFAULT - broadly useful baseline devops skill for any environment where Docker is present | - Use for container lifecycle management, image builds, Compose stacks, volume/network administration, disk cleanup, and debugging crashed containers.<br>- Follow a domain-first triage (lifecycle vs. interaction vs. image vs. compose vs. volumes/networks vs. troubleshooting) before picking commands.<br>- Always check `docker system df` before any cleanup, and never run `docker system prune -a --volumes` without explicit user confirmation since it destroys named-volume data.<br>- Diagnose common failures with a lookup table (port conflicts, exited containers, binding to 127.0.0.1 instead of 0.0.0.0, build cache misses).<br>- Key pitfalls captured: layer-order in Dockerfiles killing build cache, UID/GID mismatches on bind-mounted volumes, and services unable to reach each other over the wrong Compose network.<br>- Verify results per operation: `docker ps` for status, `docker logs` for cleanliness, `docker compose ps` for stack health, and `docker system df` before/after cleanup. |
| hermes-s6-container-supervision | skill | devops | Documents the s6-overlay process-supervision architecture inside the Hermes Docker image - boot scripts, static services, and per-profile gateway service registration - for anyone modifying or debugging it. | Linux + s6-overlay-based Hermes Docker image, docker CLI, internal hermes_cli.service_manager/container_boot modules | N/A | N/A | optional-skills/devops/hermes-s6-container-supervision/SKILL.md | official | MIT (repo) | - | extremely deep - S6ServiceManager, container_boot.reconcile_profile_gateways, docker/s6-rc.d/*, hermes_cli/gateway.py dispatch, profile SOUL.md marker | LOW | SKIP - internal Hermes container plumbing, not a portable end-user capability for a general reference catalog | - Load this only when modifying/debugging the Hermes Docker image's process supervision: adding a static service, changing per-profile gateway run scripts, or diagnosing why a gateway won't survive a container restart.<br>- Understand the architecture: s6-overlay's `/init` runs cont-init.d setup scripts (UID remap, profile reconciliation) then a static s6-rc bundle, while the container's CMD (not a supervised service) is the actual entrypoint via a wrapper script.<br>- Per-profile gateways are dynamically registered under `/run/service/` at boot by walking each profile's persisted `gateway_state.json` and recreating only those previously running.<br>- Key pitfall knowledge: s6's `/command/` binaries aren't on the `docker exec` PATH (must use absolute paths), and s6's halt mechanism cannot propagate a real exit code, which is why main Hermes runs unsupervised as the CMD rather than as an s6-rc service.<br>- Common failure modes documented: root-owned profile directories breaking the reconciler, crash-looping gateways from missing model/auth config, and containers always exiting 143 regardless of the real process exit code.<br>- Verified via `docker exec` calls to `s6-svstat`, `s6-svc`, and reading the `container-boot.log`, plus a dedicated docker test harness. |
| inference-sh-cli | skill | devops | Drives the inference.sh CLI (`infsh`) to run 150+ hosted AI apps (image/video generation, search, TTS, etc.) from the terminal without managing individual provider APIs. | infsh CLI (curl installer), inference.sh account/API key, terminal tool | API key | N/A | optional-skills/devops/inference-sh-cli/SKILL.md | official | MIT (repo) | wraps inference.sh CLI (commercial hosted SaaS) | terminal tool only; relies on `MEDIA:<url>` convention for inline media display | HIGH | OPTIONAL - useful if the user wants managed access to many hosted generative-AI models without per-provider API setup | - Use when the user wants image/video generation, AI-powered search, or other hosted AI apps without configuring individual provider APIs.<br>- Always search the app catalog first (`infsh app list --search <term>`) rather than guessing an app ID, since IDs and offerings change frequently.<br>- Run apps with `infsh app run <app-id> --input '<json>' --json` and parse the JSON output for result URLs.<br>- Local files referenced in the input JSON are automatically uploaded by the CLI.<br>- Key pitfalls: never assume an app ID without searching first, always request `--json` output, and warn users that video generation can take 30-120 seconds.<br>- Verify via `infsh me` (auth check) and a successful `--json` run returning a valid media URL. |
| pinggy-tunnel | skill | devops | Exposes a local service to the public internet via a zero-install Pinggy SSH reverse tunnel, with recipes for webhooks, MCP servers, and local LLM endpoints. | stock ssh client only (no daemon install), optionally a paid PINGGY_TOKEN for the Pro tier | none | N/A | optional-skills/devops/pinggy-tunnel/SKILL.md | official | MIT (repo) | - | terminal tool background mode + process tool for teardown, ~/.hermes/cache/scratch for logs/pid files | HIGH | OPTIONAL - handy utility for exposing local dev servers/webhooks/MCP endpoints on demand | - Use to give a locally-running service (dev server, webhook receiver, MCP HTTP endpoint, local LLM server) a public URL, when no tunnel daemon like ngrok/cloudflared is installed.<br>- Start the local origin first, then launch `ssh -p 443 -R0:localhost:<port> free@a.pinggy.io` in the background, redirecting output to a logfile.<br>- Parse the public `https://...pinggy.link` URL out of the logfile with grep rather than expecting a return value; gate access with Pinggy's username-keyword flags (basic auth, bearer token, IP whitelist) for anything non-trivial.<br>- Free tier tunnels expire after 60 minutes and get a new random URL on each restart; Pro tier (token-based) removes both limits.<br>- Key pitfalls: always quote the `+`-joined username string, always disable strict host-key checking for unattended runs, and remember `502` means the tunnel is up but the local origin isn't listening yet.<br>- Verify end-to-end by curling the returned public URL and confirming the expected local-origin response, then tear down via the saved PID or the terminal tool's process kill. |
| setup-wizard-generator | skill | devops | Generates a self-contained bash wizard script that walks a human step-by-step through a tedious manual setup procedure (credentials, OAuth apps, CI secrets), capturing and writing values as it goes. | bash, gh CLI (only if writing GitHub secrets/variables), a bundled bash library template | N/A | N/A | optional-skills/devops/setup-wizard-generator/SKILL.md | official | MIT (repo) | - | writes to .env / GitHub secrets conventions; otherwise a generic bash code-generation skill | HIGH | OPTIONAL - valuable specifically for onboarding/credential-provisioning tasks, not an everyday-use skill | - Use when a manual, human-only setup procedure (clicking through a dashboard, generating API keys, configuring OAuth apps) needs to be turned into a repeatable, guided script rather than re-explained every time.<br>- Scope every stage and captured value first by reading the repo's .env/README/CI workflows, then confirm the ordered stage list with the user before authoring.<br>- Copy the bundled template and add one `stage` per step using its helper functions (open_url, ask/ask_secret, write_env, set_secret, confirm), never editing the shared library section above the STAGES marker.<br>- Validate statically with `bash -n` (and shellcheck if available) rather than running it yourself, since it blocks on human browser interaction.<br>- Key pitfalls: inventing dashboard click-paths that may be stale, pushing CI secrets that no workflow actually references, and cramming multiple steps into one stage since each stage clears the screen.<br>- Success is verified by tracing every captured value to its declared destination and matching every `set_secret` name to an actual `secrets.*` reference in CI. |
| watchers | skill | devops | Provides three ready-made polling scripts (RSS/Atom, arbitrary JSON API, GitHub) with a shared watermark-based dedup helper, meant to be wired into a cron job for "notify me when X changes" automations. | Python (stdlib-based scripts), terminal tool, optionally GITHUB_TOKEN in .env for higher GitHub rate limits, cron/scheduling capability | none | N/A | optional-skills/devops/watchers/SKILL.md | official | MIT (repo) | - | $HERMES_HOME/watcher-state/ for state files, cron job wiring via the agent's own cron capability, terminal tool | HIGH | DEFAULT - a clean, reusable building block for any "watch and notify" automation request | - Use whenever the user wants to be notified only of NEW items from an RSS/Atom feed, a JSON API, or a GitHub repo's issues/pulls/releases/commits.<br>- Each script follows the same shape: load a watermark file of previously-seen IDs, fetch, diff, save the updated watermark, and print only new items (or nothing, silently, if there's nothing new).<br>- Wire a chosen script into a scheduled/cron agent task with instructions to summarize output only when the script actually prints something.<br>- The first run of any watcher always establishes a baseline and emits nothing - this is by design, not a bug.<br>- Key pitfalls: printing a "no new items" message breaks the silent-on-empty contract, and the watermark set is capped (default 500 IDs) so very high-churn feeds may need a higher cap.<br>- Verify by inspecting the JSON state file under `$HERMES_HOME/watcher-state/<name>.json` and confirming a forced re-run (after deleting that file) replays a fresh baseline. |

## Dogfood

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| adversarial-ux-test | skill | dogfood | Has the agent roleplay a hostile, non-technical "worst-case user" persona to browse a product and surface real UX friction, then filters findings through a pragmatism pass before creating tickets. | browser automation tools (navigate/screenshot), a target staging/deployed URL, no external API | none | N/A | optional-skills/dogfood/adversarial-ux-test/SKILL.md | official | MIT (repo) | - | browser tooling (navigate/screenshot/console-check), ticket-creation convention (label "ux-review") | HIGH | OPTIONAL - valuable pre-launch/pre-demo QA ritual, not needed for every project | - Use before demos, launches, or after shipping a feature batch to find genuine usability friction rather than bugs.<br>- Define a specific, low-tech-comfort persona with one core task and a concrete "give up" trigger, then fully inhabit that persona while browsing the live app and attempting their actual task (not a feature tour).<br>- Write the raw feedback in-character as a visceral "rant," including screenshots and console errors for every pain point.<br>- Step out of character and run a mandatory pragmatism filter, sorting each complaint into RED (real bug), YELLOW (low-priority but valid), WHITE (persona noise, skip), or GREEN (hidden feature request).<br>- Key pitfall: skipping the pragmatism filter and shipping raw persona complaints as tickets, or testing with pre-seeded admin accounts instead of a fresh cold-start signup.<br>- Success is verified by capped, well-labeled tickets (max 10) created only for RED/GREEN items, each with a persona quote, the underlying UX issue, and a suggested fix. |

## Email

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| agentmail | skill | email | Gives the agent its own email inbox via the AgentMail CLI for sending mail, receiving replies, handling OTP flows, and running inbound email loops. | Node.js (npm install -g agentmail-cli), AGENTMAIL_API_KEY, terminal tool | API key | N/A | optional-skills/email/agentmail/SKILL.md | official | MIT (repo) | - | terminal tool for CLI invocation; references native MCP/webhook/websocket integration docs bundled with the skill | HIGH | OPTIONAL - needed only when an agent requires its own outbound/inbound email address (not the user's personal mailbox) | - Use when the agent itself needs an owned email address for sending, receiving replies, completing OTP verification flows, or running an inbound-mail loop - not for accessing a human's existing mailbox.<br>- Install the CLI globally via npm and export an API key (or run the bundled self-signup flow if no key exists yet).<br>- Prefer the CLI for all operations, falling back to MCP only if the host expects MCP tools, and to raw REST only for operations the CLI lacks.<br>- Use `--format json` whenever another script or command needs to consume IDs from the output.<br>- Key pitfalls: never expose the API key in prompts/logs/URLs, use stable client IDs for retried create calls, and react only to `message.received` events (not the agent's own sent messages).<br>- Verify with `agentmail inboxes list --format json` returning a valid inbox list. |

## Finance

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 3-statement-model | skill | finance | Completes integrated income statement / balance sheet / cash flow Excel templates with formula-driven cross-statement linkages. | excel-author skill and its recalc.py helper, optional financial-data MCP server, web_search/web_extract/browser_navigate as fallback | none | N/A | optional-skills/finance/3-statement-model/SKILL.md | official | Apache-2.0 | - | excel-author skill's recalc.py script, native-mcp skill reference, web_search/web_extract/browser_navigate tools | MEDIUM | OPTIONAL - specialized IB financial-modeling workflow, valuable for a finance vertical but not general-purpose | - Use when a user needs a full 3-statement model built or completed in Excel with proper IS/BS/CF linkages.<br>- Map the template's tabs and input vs formula cells, populate historicals then projections, build the balance sheet and cash flow, and check in with the user after each statement rather than building end to end.<br>- Every derived cell must be a live formula referencing assumption cells, never a hardcoded computed number, so the model still works when assumptions change.<br>- Run the cross-statement checks (assets = liabilities + equity, cash flow ending cash ties to the balance sheet, retained earnings roll-forward) after each stage.<br>- Verifies success via a consolidated master check that reports pass/fail across all validation sections plus zero formula errors after recalculation. |
| comps-analysis | skill | finance | Builds a comparable-company valuation spreadsheet with operating and valuation multiples plus quartile/median statistics. | excel-author skill and its recalc.py helper, optional financial-data MCP server, web_search/web_extract against SEC EDGAR as fallback | none | N/A | optional-skills/finance/comps-analysis/SKILL.md | official | Apache-2.0 | - | excel-author skill's recalc.py script, native-mcp skill reference | MEDIUM | OPTIONAL - same finance-vertical rationale as the other valuation skills | - Use when comparing a peer group of public companies for valuation or benchmarking.<br>- Define the peer group, gather operating metrics (revenue, margins) with cited sources, build valuation multiples that reference the operating section rather than re-entering figures, and add max/quartile/median/min statistics rows.<br>- Keep the metric set small (roughly 5 operating + 5 valuation columns) and tailor it to the specific question being asked (undervalued vs efficient vs fastest-growing).<br>- Every hardcoded input cell needs a comment citing its source or stated assumption.<br>- Verifies success via sanity checks (margin ordering, multiple ranges by industry) and an error-free formula recalculation. |
| dcf-model | skill | finance | Produces a discounted-cash-flow equity valuation workbook with CAPM-based WACC, bear/base/bull scenarios, and three full-recalculation sensitivity tables. | excel-author skill and its recalc.py helper, optional financial-data MCP server, web_search/web_extract as fallback | none | N/A | optional-skills/finance/dcf-model/SKILL.md | official | Apache-2.0 | - | excel-author skill's recalc.py script, native-mcp skill reference | MEDIUM | OPTIONAL - specialized IB financial-modeling workflow, valuable for a finance vertical but not general-purpose | - Use for equity valuation via discounted cash flow, typically with bear/base/bull scenario cases.<br>- Gather historicals, project revenue and free cash flow, compute WACC via CAPM, discount projected cash flows plus a terminal value, then bridge enterprise value to an implied per-share price.<br>- Build three 5x5 (odd-dimension) sensitivity grids where the center cell equals the base case output exactly, using full DCF recalculation formulas in every cell rather than linear shortcuts.<br>- A key pitfall is letting terminal growth exceed WACC, which produces an invalid infinite valuation.<br>- Verifies success by running the recalculation script until it reports zero formula errors and confirming the sensitivity table's center cell matches the model's actual implied share price. |
| excel-author | skill | finance | Foundational headless-Excel authoring skill enforcing formulas-over-hardcodes, blue/black/green cell coloring, named ranges, and a recalculation step. | openpyxl (pip package), LibreOffice headless or a bundled recalc script for formula recalculation | N/A | N/A | optional-skills/finance/excel-author/SKILL.md | official | Apache-2.0 | - | Is itself referenced by the 3-statement-model, comps-analysis, dcf-model, lbo-model, merger-model, and pptx-author skills; ./out/ output-file convention | MEDIUM | OPTIONAL - foundational building block only needed if the finance-modeling family is included | - Use whenever a task needs to produce an auditable .xlsx file headlessly rather than driving a live Excel session.<br>- Core convention: blue font for hardcoded inputs, black for formulas, green for cross-sheet links; every calculation is a formula string, never a Python-computed value pasted in.<br>- Use named ranges for figures referenced across sheets or into a deck, and add a Checks tab that surfaces TRUE/FALSE balance and tie-out tests.<br>- Comment every hardcoded input cell with its source at the moment it's created, not deferred to the end.<br>- A key pitfall is that openpyxl only writes formula strings and does not compute them, so the workbook must be recalculated (via LibreOffice headless or a helper script) before any downstream tool reads computed values; verifies success via that recalculation reporting zero errors and the Checks tab returning TRUE. |
| lbo-model | skill | finance | Builds a leveraged-buyout Excel model (Sources & Uses, debt schedule, IRR/MOIC returns) by populating a template rather than building from scratch. | excel-author skill and its recalc.py helper, a user-supplied or bundled example LBO template file | none | N/A | optional-skills/finance/lbo-model/SKILL.md | official | Apache-2.0 | - | excel-author skill's recalc.py script, bundled examples/LBO_Model.xlsx template | MEDIUM | OPTIONAL - PE-specific niche | - Use for private-equity buyout modeling, always preferring an attached template over inventing a new layout.<br>- Work section by section (Sources & Uses, operating model, debt schedule, returns, sensitivity tables) and get user sign-off at each stage before moving on.<br>- Identify the plug item that balances Sources to Uses, and use beginning-not-ending debt balances for interest calculations to avoid circular references.<br>- Cash-flow signs must be correct for IRR/MOIC (investment negative, proceeds positive), and sensitivity tables need odd dimensions with the base case centered.<br>- Verifies success via section-by-section balance checks (assets=liabilities+equity, sources=uses) and a zero-error formula recalculation. |
| merger-model | skill | finance | Builds an M&A accretion/dilution analysis covering purchase price, sources & uses, pro forma EPS, and synergy/consideration-mix sensitivities. | excel-author skill conventions (implied); no other external services | N/A | N/A | optional-skills/finance/merger-model/SKILL.md | official | Apache-2.0 | - | Follows excel-author skill's cell-coloring/formula conventions | MEDIUM | OPTIONAL - M&A-specific niche | - Use when evaluating a potential acquisition's EPS impact or preparing a merger-consequences pitch section.<br>- Gather acquirer, target, and deal-term inputs, build the purchase-price and sources-and-uses tables, then compute pro forma EPS by layering synergies, foregone interest on cash used, new debt interest, and intangible amortization onto combined net income.<br>- Sensitize the result against synergy levels and cash/stock consideration mix, and calculate the minimum synergies needed for EPS neutrality (breakeven).<br>- A common pitfall is forgetting the tax effect on synergies/interest adjustments or ignoring that Year 1 synergies are usually only a partial phase-in of the run-rate figure.<br>- Verifies success by confirming GAAP and cash EPS views are both shown and the sensitivity tables recalc without formula errors. |
| polymarket | skill | finance | Queries Polymarket's public REST APIs (Gamma, CLOB, Data) for prediction-market questions, prices, orderbooks, and history with zero authentication. | HTTP access to gamma-api.polymarket.com, clob.polymarket.com, and data-api.polymarket.com | none | N/A | optional-skills/finance/polymarket/SKILL.md | official | MIT (repo) | - | None beyond a generic HTTP-capable tool | HIGH | OPTIONAL - handy read-only market-data lookup, not essential | - Use when a user asks about prediction-market odds, event probabilities, or Polymarket specifically.<br>- Search the Gamma API for the relevant event, parse its nested markets, and present outcome prices as percentages alongside volume.<br>- For deeper detail, use the market's clobTokenIds to query the CLOB API orderbook or its conditionId for price history.<br>- A key pitfall is that outcomePrices, outcomes, and clobTokenIds arrive as JSON strings nested inside the JSON response and must be parsed twice.<br>- Verifies success by confirming a returned market includes a clear question and a probability that reads sensibly as a percentage; the skill is strictly read-only and cannot place trades. |
| pptx-author | skill | finance | Headless PowerPoint deck authoring via python-pptx, tuned for finance decks where every number traces back to a source Excel workbook. | python-pptx (pip package), optional firm-branded .pptx template, openpyxl to read the source workbook's computed values | N/A | N/A | optional-skills/finance/pptx-author/SKILL.md | official | Apache-2.0 | - | Complements the excel-author skill and the broader built-in "powerpoint" skill; ./out/ output-file convention | MEDIUM | OPTIONAL - narrower than a general presentation-authoring skill | - Use when a deck must ship as a file artifact (pitch deck, IC memo, earnings note) rather than driving a live PowerPoint session.<br>- Load a firm template if one is mounted, keep one idea per slide title, and read numbers programmatically from the source workbook's named ranges instead of retyping them.<br>- Prefer embedding a PNG chart rendered from the model over native pptx chart objects when visual fidelity to the model must match exactly.<br>- A key pitfall is that openpyxl only sees a workbook's computed values after it has been recalculated by something (Excel, LibreOffice, or a recalc script) — reading it too early yields stale or blank values.<br>- Verifies success by confirming the output .pptx exists at the expected path and every headline number matches its cited workbook cell. |
| stocks | skill | finance | Provides stock quote, ticker search, price history, multi-ticker comparison, and crypto price lookups via Yahoo Finance using only the Python standard library. | Python 3.8+ stdlib, terminal tool access to run the bundled script, optional ALPHA_VANTAGE_KEY env var for enrichment | none | N/A | optional-skills/finance/stocks/SKILL.md | official | MIT (repo) | - | Invoked through the terminal tool; reads optional ALPHA_VANTAGE_KEY from environment | HIGH | OPTIONAL - convenient but relies on an unofficial API and isn't core | - Use for quick stock price, ticker lookup, historical performance, multi-ticker comparison, or crypto price questions.<br>- Run the bundled script's quote/search/history/compare/crypto subcommands through the terminal tool; all output is JSON on stdout.<br>- Optionally set an Alpha Vantage key to backfill market_cap/pe_ratio/52-week fields that Yahoo's unauthenticated endpoint sometimes returns as null.<br>- A key pitfall is that Yahoo Finance's endpoint is unofficial and can rate-limit or change without notice, so bulk requests should be spaced out.<br>- Verifies success by running a quote lookup and confirming the JSON response includes the requested symbol and a numeric price field; it is read-only with no order placement. |

## Gaming

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| minecraft-modpack-server | skill | gaming | Guides standing up a modded Minecraft server (NeoForge/Forge) from a server-pack zip, including JVM tuning, firewall setup, and automated backups. | Java (8/17/21 depending on Minecraft version), shell/terminal access, ufw firewall, cron | none | N/A | optional-skills/gaming/minecraft-modpack-server/SKILL.md | official | MIT (repo) | - | Uses the terminal tool for shell commands; gated to linux/macos platforms | LOW | OPTIONAL - legitimate niche capability but heavy local server-ops footprint | - Use when a user wants to host a modded Minecraft server from a downloaded server pack zip.<br>- Inspect the pack to determine mod loader and required Java version, install that Java version, run the pack's own install script, accept the EULA, then tune server.properties and JVM heap/garbage-collection flags to the expected player and mod count.<br>- Open the server port in the firewall, build a clean launch script (the pack's own launcher often auto-restarts, which is undesirable), and set up an hourly cron job that tars the world folder and prunes old backups.<br>- A common pitfall is forgetting allow-flight=true, which kicks players using mods with flight/jetpacks, and mismatching online-mode with enforce-secure-profile which rejects legitimate clients.<br>- Verifies success by confirming the server process is running, tailing the log for a "Done (Xs)!" ready message, and having a player successfully connect. |
| pokemon-player | skill | gaming | Drives a headless Game Boy emulator via a companion Python package so the agent can autonomously play a user-supplied Pokemon ROM using RAM-state reads plus vision. | pokemon-agent Python package (cloned from its GitHub repo) with the pyboy emulator, a user-supplied ROM file, a vision-analysis tool, an SSH reverse tunnel (localhost.run) for a live dashboard | none | N/A | optional-skills/gaming/pokemon-player/SKILL.md | official | MIT (repo) | - | Assumes a vision_analyze tool and a memory-note convention (a PKM: prefix) for tracking progress across turns | LOW | SKIP - entertainment novelty requiring a user-supplied ROM (copyright-sensitive) and a fragile local emulator/tunnel setup | - Use when a user wants to watch an AI play a Game Boy-era Pokemon title end to end.<br>- Setup clones the companion repo, creates a Python virtual environment, and starts a local game server against a ROM the user provides directly (the skill explicitly forbids ever downloading or sourcing a ROM itself), then exposes a live dashboard through an SSH reverse tunnel.<br>- The gameplay loop is observe (read RAM state and take a screenshot) then orient/decide then act in short 2-4-step bursts, then verify the result visually before proceeding, saving named game states every 15-20 turns and before any risky encounter.<br>- Key pitfalls include needing extra wait time after door/staircase map transitions, sidestepping before walking after exiting a building (otherwise you re-enter immediately), and RAM-based dialog-end detection being unreliable compared to a screenshot check.<br>- Verifies progress via periodically named save states plus structured memory notes recording the current objective, map knowledge, and milestones reached. |

## Health

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| fitness-nutrition | skill | health | Acts as a fitness coach and nutritionist using the free wger exercise database, USDA FoodData Central, and offline body-metric calculators (BMI/TDEE/1RM/macros/body fat). | curl, python (stdlib), wger public API (no auth), USDA FoodData Central API (works with a shared DEMO_KEY or an optional personal key) | none | N/A | optional-skills/health/fitness-nutrition/SKILL.md | official | MIT (repo) | - | Invoked via curl/python through the terminal tool; reads optional USDA_API_KEY from environment | HIGH | OPTIONAL - useful free-data vertical skill, not core | - Use for exercise lookups by muscle/category/equipment/name, food macro lookups, or body-metric calculations (BMI, TDEE, one-rep max, macro splits, body fat %).<br>- Query the wger API with language and status filters for English/approved-only exercises, query USDA FoodData Central for per-100g nutrient data, or run the bundled offline calculator script for pure math questions.<br>- A key pitfall is that wger returns all languages and unverified user submissions unless explicitly filtered, and USDA's shared DEMO_KEY is capped at 30 requests/hour so bulk lookups need pacing or a personal free key.<br>- Another pitfall is that USDA nutrition figures are per 100 grams and must be scaled to the user's actual serving size, and 1RM formulas lose accuracy above about 10 reps.<br>- Verifies success by confirming exercise results include muscle/equipment fields, nutrition results include kcal/protein/fat/carbs, and calculator outputs fall in physiologically sane ranges (e.g. TDEE between roughly 1500-3500 kcal for most adults). |
| neuroskill-bci | skill | health | Connects Hermes to a running NeuroSkill desktop app to read live EEG/BCI-derived focus, mood, sleep, and cognitive-load metrics from a consumer wearable (Muse or OpenBCI) and suggest interventions. | Node.js 20+, the NeuroSkill desktop application running locally, a paired BCI headset (Muse 2/S or OpenBCI) over Bluetooth, the npx neuroskill CLI | none | N/A | optional-skills/health/neuroskill-bci/SKILL.md | official | MIT (repo) | wraps NeuroSkill desktop app / NeuroLoop CLI (GPLv3) - the skill's own MIT-licensed instructions require installing and running a GPLv3-licensed local application and companion CLI | Shells out to the npx neuroskill CLI as the sole data source; no other Hermes tool coupling | LOW | SKIP - requires specific consumer BCI hardware most users won't own, wraps a GPLv3 app, and touches sensitive biometric/health data with an explicit non-medical disclaimer | - Use to give cognitively-aware responses (focus/relaxation/mood-informed suggestions) or to offer short neurofeedback/breathing protocols, only when the user is wearing a paired BCI headset with the desktop app running.<br>- Poll the CLI's status/session/sleep/compare/search-labels subcommands as JSON and translate the numeric scores into plain-language interpretation (e.g. "flow state territory") rather than reciting raw numbers.<br>- Only offer an intervention protocol when a defined threshold is crossed (e.g. sustained low focus, high drowsiness, negative affect), always ask before starting one, and never interrupt an active high-focus/flow session.<br>- A critical caveat baked into the skill itself: this is an explicitly non-medical, research-only tool and must never be used for diagnosis or treatment.<br>- Verifies reliability by checking the signal_quality and SNR fields before trusting any derived score, since poor electrode contact makes all scores return near zero. |

## MCP

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| fastmcp | skill | mcp | Guides building, testing, and deploying Python MCP servers with the FastMCP framework, from scaffolding a minimal tool surface through CLI validation to client installation and HTTP deployment. | Python, fastmcp pip package, optionally httpx for the API-wrapper template | none | N/A | optional-skills/mcp/fastmcp/SKILL.md | official | MIT (repo) | - | hands off to Hermes' own `native-mcp` skill for wiring a finished server into ~/.hermes/config.yaml | HIGH | DEFAULT - core enabling skill for anyone building custom MCP tool servers | - Use when creating a new Python MCP server (wrapping an API, database, or file workflow) or preparing one for client installation/HTTP deployment.<br>- Start from the narrowest viable server shape via a bundled scaffold script/templates (API wrapper, read-only SQLite, file processor), implementing `@mcp.tool` functions with concrete verb names before adding resources or prompts.<br>- Validate locally with the FastMCP CLI (`inspect`, `list`, `call`) against both stdio and HTTP transports before wiring the server into any client.<br>- Install into supported clients (Claude Code, Claude Desktop, Cursor) via `fastmcp install`, or hand off to Hermes' `native-mcp` skill for Hermes-specific config.<br>- Key pitfalls: vague tool names like `run`/`process`, skipping a real `fastmcp call` test per tool, and hardcoding auth instead of using environment variables.<br>- Verify via a full quality-bar checklist: clean import, successful `inspect`/`list`, at least one real call per tool, and documented environment variables. |
| mcp-oauth-remote-gateway | skill | mcp | A manual OAuth-with-PKCE procedure for connecting remote HTTP MCP servers to a headless Hermes gateway (container/VPS) where the built-in loopback-callback OAuth flow cannot work. | curl, python3 (execute_code/terminal), the target MCP server's OAuth 2.1 + Dynamic Client Registration support, Hermes internal token-storage file layout | OAuth | N/A | optional-skills/mcp/mcp-oauth-remote-gateway/SKILL.md | official | MIT (repo) | - | extremely deep - tools/mcp_oauth.py HermesTokenStorage file schema ($HERMES_HOME/mcp-tokens/), config.yaml mcp_servers.<name> auth:oauth, /reload-mcp, Hermes dashboard connectors UI | LOW | OPTIONAL - necessary only for remote/headless Hermes gateway deployments adding OAuth-gated MCP servers | - Use only when Hermes runs as a remote/headless gateway (not a local CLI) and needs to connect to a remote HTTP MCP server that requires real OAuth (not a static bearer token) - prefer the Hermes dashboard's browser-driven OAuth flow first if available.<br>- Manually discover OAuth metadata (via WWW-Authenticate header or well-known endpoints), perform Dynamic Client Registration, build a PKCE authorize URL, and have the human paste back the failed-callback URL's query string.<br>- Exchange the authorization code for tokens via a form-encoded POST, then write the access/refresh tokens and client info into the exact two JSON files Hermes' token storage expects, so `/reload-mcp` finds cached credentials.<br>- Always smoke-test the token with a manual MCP `initialize` POST before telling the user to reload, and use a browser-like User-Agent to avoid Cloudflare blocking bare urllib requests.<br>- Key pitfalls: a tripped internal circuit-breaker can mask a working token refresh (needs a full restart, not just reload); a healthy-looking refresh can still fail because the provider revoked the underlying session server-side, requiring full re-authorization.<br>- Verify recovery state with the bundled diagnostic script, which classifies the failure into TOKEN_OK / REFRESH_FIXED / SESSION_REVOKED / REFRESH_DEAD branches. |
| mcporter | skill | mcp | A Node-based CLI for discovering, calling, authenticating, and managing MCP servers/tools directly from the terminal, independent of any particular MCP client. | Node.js (via npx or npm install -g mcporter) | OAuth | N/A | optional-skills/mcp/mcporter/SKILL.md | official | MIT (repo) | - | none required beyond the terminal tool; useful alongside Hermes' native-mcp/fastmcp skills for ad-hoc testing | HIGH | OPTIONAL - convenient for ad-hoc MCP exploration/testing, not required for normal operation | - Use for ad-hoc discovery of MCP servers already configured on the machine, or to connect to any MCP server (HTTP URL or stdio command) without writing config first.<br>- List tools/schemas with `mcporter list <server> --schema`, then call a tool using key=value or function syntax, always adding `--output json` for machine-parseable results.<br>- Manage server config (`mcporter config add/remove/import`) and authenticate OAuth-gated servers with `mcporter auth <server>` (run under a PTY for the interactive browser flow).<br>- Use the daemon subcommands for persistent server connections across multiple calls, and code-generation subcommands to emit a CLI wrapper or TypeScript client/types for a server.<br>- Key pitfall: OAuth auth needs an interactive terminal/PTY; forgetting `--output json` makes output harder to parse programmatically.<br>- Verify with `mcporter list` returning the expected server/tool set and a successful test call against a known tool. |

## Migration

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| openclaw-migration | skill | migration | Guides an interactive, dry-run-first migration of a user's OpenClaw agent setup (persona, memory, skills, allowlists, messaging settings) into Hermes, with strict clarify-tool-driven decision flows and post-run reporting rules. | scripts/openclaw_to_hermes.py (bundled), an existing ~/.openclaw installation, Hermes clarify tool (optional), hermes claw migrate CLI | none | N/A | optional-skills/migration/openclaw-migration/SKILL.md | official | MIT (repo) | - | very deep: hermes claw migrate CLI, Hermes SOUL.md/memory format, ~/.hermes/skills/openclaw-imports/, command_allowlist, clarify tool protocol, hermes setup wizard auto-detection | LOW | SKIP - Hermes-specific inbound migration tool tied to both the source (OpenClaw) and destination (Hermes) formats | - Use when a user with an existing OpenClaw setup wants to move their persona, memory, skills, and compatible config into Hermes with minimal manual cleanup<br>- Always dry-run first, present a migration-mode choice (user-data only vs full-with-secrets) and conflict-resolution choices via the clarify tool with strict formatting rules, then execute the script with flags mapped exactly to the user's choices and report only what the script's JSON output actually shows<br>- Key pitfalls: never silently overwrite existing SOUL.md/skills or migrate secrets without explicit consent<br>- never claim an item was migrated/overwritten unless the report status says so<br>- don't end a turn with a vague what-would-you-like-to-do when a structured clarify call should be made instead<br>- Verifies success by inspecting the script's report.json/summary.md and confirming counts of migrated/conflicted/skipped items match the narrated summary |

## MLOps

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| accelerate | skill | mlops | Reference guide for HuggingFace Accelerate, a thin wrapper that lets a single PyTorch training script run unchanged across CPU, single-GPU, multi-GPU, multi-node, DeepSpeed, or FSDP setups. | accelerate, torch, transformers Python packages; optional deepspeed package; multi-GPU/node hardware for distributed runs | none | N/A | optional-skills/mlops/accelerate/SKILL.md | official | MIT (repo) | - | None - pure library/CLI reference with no Hermes tool or config coupling | HIGH | SKIP - GPU/distributed-training infrastructure skill, too specialized for a general default catalog | - Use when a user wants to scale an existing PyTorch training loop from one device to multi-GPU/multi-node/TPU without rewriting it<br>- Core steps: wrap model, optimizer, and dataloader with Accelerator.prepare(), swap loss.backward() for accelerator.backward(), then launch with the `accelerate launch` CLI (optionally after an interactive `accelerate config`)<br>- Mixed precision (fp16/bf16/fp8), gradient accumulation via an accumulate() context manager, and DeepSpeed ZeRO or FSDP sharding are all enabled through Accelerator constructor options or plugin objects rather than manual device code<br>- Common pitfalls: manually calling .to('cuda') after prepare() breaks automatic placement, skipping the accumulate() context breaks gradient accumulation math, and distributed checkpoint save/load must go through accelerator.save_state/load_state guarded by an is_main_process check<br>- Verify success by confirming the script runs unmodified across device counts and that multi-GPU wall-clock time drops roughly in proportion to GPU count |
| chroma | skill | mlops | Reference guide for Chroma, an open-source embedding database used for local/self-hosted RAG and semantic search. | chromadb Python package (or the chromadb/@chroma-core npm packages), optional sentence-transformers/OpenAI/HuggingFace embedding function, optional local Chroma server process | none | N/A | optional-skills/mlops/chroma/SKILL.md | official | MIT (repo) | - | None - generic vector-database reference; no Hermes-specific tool names or config keys | HIGH | OPTIONAL - lightweight, self-hosted vector store that's broadly useful for local RAG prototyping | - Use when a user needs a simple, self-hosted vector database for retrieval-augmented generation or semantic search without standing up a managed cloud service<br>- Core steps: create a client (in-memory, persistent-on-disk, or HTTP server), create a collection, add documents with ids/metadata, then query by text (auto-embedded) or by precomputed embedding vectors, optionally filtering on metadata<br>- Embedding generation is pluggable - defaults to a local sentence-transformers model but can be swapped for OpenAI or HuggingFace embedding functions, and it plugs directly into LangChain/LlamaIndex retriever abstractions<br>- Pitfalls: using the plain in-memory client loses all data on process restart (use PersistentClient or server mode instead); duplicate/careless IDs silently overwrite documents; skipping metadata makes later filtering impossible<br>- Verify success by running a query and confirming the returned documents, distances, and metadata match the expected nearest matches |
| clip | skill | mlops | Reference guide for OpenAI's CLIP model, used for zero-shot image classification, image-text similarity, and semantic image search. | transformers or the official OpenAI CLIP package, torch, pillow, optional GPU for speed | none | N/A | optional-skills/mlops/clip/SKILL.md | official | MIT (repo) | - | None - pure computer-vision library reference | HIGH | SKIP - specialized vision-model workflow, narrow relevance for a general default catalog | - Use for zero-shot image classification, image-to-text/text-to-image similarity search, or lightweight content-moderation tasks without any task-specific fine-tuning<br>- Core steps: load a CLIP checkpoint (e.g. ViT-B/32) and its preprocessing function, encode an image and a set of candidate text labels into embeddings, normalize both, then compute cosine similarity or a softmax over labels to pick the best match<br>- For search over a corpus, embed all images once, cache the embeddings, then compare a query text embedding against the cached image embeddings (optionally storing them in a vector DB like Chroma/FAISS for scale)<br>- Pitfalls: vague or single-word text labels hurt zero-shot accuracy versus descriptive phrases; forgetting to L2-normalize embeddings breaks cosine-similarity math; CLIP is weak at counting objects and precise spatial relationships<br>- Verify success by checking that predicted label probabilities favor the expected class, or that a known matching image/text pair scores higher similarity than mismatched pairs |
| evaluating-llms-harness | skill | mlops | Reference guide for EleutherAI's lm-evaluation-harness, the industry-standard tool for benchmarking LLMs on 60+ academic tasks like MMLU, GSM8K, and HumanEval. | lm-eval Python package, transformers, optional vllm for faster inference, GPU recommended (works on CPU but slow) | none | N/A | optional-skills/mlops/evaluation/evaluating-llms-harness/SKILL.md | official | MIT (repo) | - | None - CLI benchmarking tool reference with no Hermes integration | HIGH | SKIP - GPU-heavy academic model benchmarking, out of scope for a general default catalog | - Use to measure and compare LLM quality on standardized academic benchmarks (MMLU, GSM8K, HellaSwag, HumanEval, TruthfulQA, etc.), or to track quality across training checkpoints<br>- Core steps: install lm-eval, choose a benchmark suite and few-shot count, run the `lm_eval` CLI against a HuggingFace, vLLM, or API-backed model, and save results as JSON for later comparison<br>- Swap the HF backend for the vLLM backend to get roughly 5-10x faster evaluation runs on the same benchmark suite<br>- Pitfalls: benchmarks that execute generated code (HumanEval, MBPP) require an explicit unsafe-code confirmation flag or they refuse to run; mismatched few-shot counts or subtly different task-name variants produce scores that don't match published leaderboards; a full MMLU run can take hours on a single GPU<br>- Verify success by comparing the output JSON's accuracy/exact-match metrics against known reference scores or prior runs of the same model |
| weights-and-biases | skill | mlops | Reference guide for Weights and Biases (W&B), a hosted ML experiment-tracking, hyperparameter-sweep, and model-registry platform. | wandb Python package, a Weights and Biases account and API key (or self-hosted/offline mode) | API key | N/A | optional-skills/mlops/evaluation/weights-and-biases/SKILL.md | official | MIT (repo) | - | None - SDK/service reference, no Hermes-specific coupling | HIGH | SKIP - training-workflow experiment tracking tied to a specific SaaS account, not a general default need | - Use to log metrics, hyperparameters, and artifacts from ML training runs into a shareable, real-time dashboard, or to run automated hyperparameter sweeps<br>- Core steps: install wandb, authenticate via `wandb login`/API key, call wandb.init() with a project name and config dict, log scalar/media/table metrics each step with wandb.log(), and call wandb.finish() when done<br>- Hyperparameter sweeps are defined as a config dict (grid, random, or Bayesian search) and driven with wandb.sweep()/wandb.agent(); models/datasets can be versioned as Artifacts and promoted through a model registry<br>- Built-in integrations exist for HuggingFace Trainer (report_to="wandb"), PyTorch Lightning (WandbLogger), and Keras (WandbMetricsLogger/WandbModelCheckpoint callbacks)<br>- Pitfalls: forgetting wandb.finish() can leave a run marked incomplete; unstable networks need WANDB_MODE=offline plus a later `wandb sync`; generic run names make later comparison across many runs painful<br>- Verify success by checking the run's dashboard URL shows the expected logged metrics, config, and any uploaded artifacts |
| faiss | skill | mlops | Reference guide for FAISS, Meta's C++/Python library for extremely fast, large-scale (millions-to-billions of vectors) similarity search. | faiss-cpu or faiss-gpu Python package, numpy | none | N/A | optional-skills/mlops/faiss/SKILL.md | official | MIT (repo) | - | None - pure vector-search library reference | HIGH | SKIP - large-scale/GPU vector-search infrastructure, heavier and more niche than a basic RAG need | - Use when similarity search must scale to millions or billions of vectors and/or needs GPU acceleration, with no requirement for metadata filtering<br>- Core steps: pick an index type (Flat for small/exact, IVF for fast approximate search at 10K-1M scale, HNSW for best quality/speed tradeoff, PQ for memory-constrained huge datasets), train it if the type requires training, add vectors, then call search() for top-k neighbors<br>- Indexes can be persisted with write_index/read_index and optionally transferred to one or many GPUs for a further 10-100x speedup; LangChain and LlamaIndex both ship FAISS vector-store wrappers<br>- Pitfalls: FAISS has no built-in metadata filtering (pure vector math only); training IVF/PQ indexes is expensive and should be cached/reused rather than repeated; picking the wrong index type for the dataset size wastes either speed or accuracy<br>- Verify success by confirming search() returns the expected distances/indices for known query vectors and by benchmarking latency against the target dataset size |
| flash-attention | skill | mlops | Reference guide for Flash Attention, an IO-aware attention algorithm that gives 2-4x speedup and large memory savings for transformer training/inference on compatible NVIDIA GPUs. | PyTorch 2.2+ (native scaled_dot_product_attention) or the separate flash-attn pip package; NVIDIA Ampere or newer GPU with CUDA 12+ (FlashAttention-3/FP8 needs a from-source Hopper build) | none | N/A | optional-skills/mlops/flash-attention/SKILL.md | official | MIT (repo) | - | None - low-level attention-kernel optimization reference | HIGH | SKIP - GPU-hardware-gated training/inference optimization, not broadly applicable | - Use when training or serving transformers with sequences longer than roughly 512 tokens on a compatible NVIDIA GPU and memory or speed is a bottleneck<br>- Simplest path: rely on PyTorch's native scaled_dot_product_attention, which auto-selects a Flash Attention kernel on PyTorch 2.2+; for advanced features like multi-query attention or sliding-window attention, install the separate flash-attn pip package and call its function directly<br>- FlashAttention-3 with FP8 forward-only support on H100/H800 GPUs is a distinct beta build compiled from the project's Hopper-specific source directory - it is not included in the standard pip package and exposes its own module<br>- Pitfalls: only float16/bfloat16 tensors are supported, not float32; sequences under ~512 tokens see little to no benefit; older GPUs like V100 (Volta) are unsupported<br>- Verify success by benchmarking latency/memory against standard attention and confirming the maximum output difference against a standard-attention baseline stays very small |
| guidance | skill | mlops | Reference guide for Microsoft's Guidance library, which constrains LLM token generation with regex/grammars/choice lists to guarantee valid structured output on local models. | guidance Python package, a local backend (transformers or llama-cpp-python) for actual constrained generation, or a remote OpenAI-compatible client for unconstrained chat only | none (API key only if optionally using a remote OpenAI-compatible backend for unconstrained chat) | N/A | optional-skills/mlops/guidance/SKILL.md | official | MIT (repo) | - | None - prompting/generation-control library reference | HIGH | SKIP - overlaps with instructor/outlines in this batch and needs a local model backend to deliver its real value | - Use when an LLM's output must strictly match a regex, a fixed set of choices, or a JSON/grammar structure, and a locally-hosted model (Transformers or llama.cpp) is available for token-level control<br>- Core steps: load a local backend, build the prompt using chat-style context managers (system/user/assistant), then compose it with gen() for regex-constrained free text, select() for a fixed choice list, or guidance.json() with a Pydantic schema for structured JSON<br>- Reusable, even stateful, multi-step generation logic (e.g. chain-of-thought or ReAct tool-use loops) can be wrapped in functions using the @guidance decorator<br>- Pitfalls: remote API backends like OpenAI cannot do true token-level constraints - only unconstrained gen()/chat works there; overly strict regexes can make generation slow or fail outright; token healing is automatic and doesn't need manual workaround<br>- Verify success by checking regex-constrained outputs match the pattern exactly and that JSON/Pydantic-constrained outputs parse without any error |
| huggingface-tokenizers | skill | mlops | Reference guide for HuggingFace's Rust-backed `tokenizers` library, used for very fast text tokenization and training custom BPE/WordPiece/Unigram vocabularies. | tokenizers, transformers, datasets Python packages | none | N/A | optional-skills/mlops/huggingface-tokenizers/SKILL.md | official | MIT (repo) | - | None - low-level NLP tokenizer reference | HIGH | SKIP - specialized tokenizer-training reference, narrow audience for a default catalog | - Use when tokenizing very large text corpora quickly, or when training a custom BPE, WordPiece, or Unigram tokenizer from scratch with token-to-text alignment tracking<br>- Core steps: either load a pretrained tokenizer via Tokenizer.from_pretrained(), or assemble one from a model type (BPE/WordPiece/Unigram), a pre-tokenizer, an optional normalizer, and a trainer, then call train() or train_from_iterator() on the corpus and save the result to a JSON file<br>- The full processing pipeline runs normalization to pre-tokenization to model application to post-processing (adding special tokens like [CLS]/[SEP]), with configurable padding/truncation and multiprocessing for large corpora<br>- Pitfalls: picking the wrong algorithm for a target pretrained model family causes mismatches (BERT expects WordPiece, GPT expects BPE, T5/ALBERT expect Unigram); training on very large corpora can take significant time and memory<br>- Verify success by confirming encode/decode round-trips reproduce the original text and that per-token character offsets correctly map back into the source string |
| llama-cpp | skill | mlops | Reference guide for running local GGUF model inference via llama.cpp, plus a URL-first workflow for discovering and choosing GGUF quantizations on the Hugging Face Hub. | llama-cpp-python (or the llama.cpp CLI/server binaries), a local GGUF model file, network access to the Hugging Face Hub for model discovery | none (public Hub models); optional API key/token only for gated or private Hub repos | N/A | optional-skills/mlops/inference/llama-cpp/SKILL.md | official | MIT (repo) | - | None - CLI/library reference plus a generic Hugging Face Hub URL-discovery workflow, no Hermes tool coupling | HIGH | OPTIONAL - broadly useful, comparatively lightweight path to running local LLMs on CPU/consumer GPUs | - Use for running an LLM locally on CPU, Apple Silicon, CUDA, ROCm, or Intel GPU from a quantized GGUF file, or for figuring out which GGUF quantization fits a given Hugging Face repo and the user's hardware<br>- Discovery workflow: search the Hub for llama.cpp-compatible repos, open the repo's local-app view to get an exact recommended command and quant, then cross-check exact filenames and byte sizes using the Hub's recursive file-tree API<br>- Running workflow: install llama.cpp via a package manager or from source, then launch `llama-cli`/`llama-server` either directly against a `repo:quant` shorthand on the Hub or against an already-downloaded/exact GGUF file; the server exposes an OpenAI-compatible chat completions endpoint<br>- Quant guidance: default to Q4_K_M for general chat, move up to Q5_K_M/Q6_K for code or technical work when memory allows, and only drop to Q3/Q2/IQ variants under tight RAM budgets - always preserve a repo's native quant label rather than renaming it<br>- Verify success by curling the server's OpenAI-compatible endpoint (or running llama-cli directly) and confirming a coherent completion comes back from the loaded model |
| outlines | skill | mlops | Reference guide for the Outlines library (v1.x API), which generates guaranteed-valid structured output (JSON/Pydantic/regex/enum) from local or API-based LLMs via token-level constraints. | outlines package, plus one backend: transformers, llama-cpp-python, vllm, or an openai client | none for local backends; API key only when using the OpenAI backend | N/A | optional-skills/mlops/inference/outlines/SKILL.md | official | MIT (repo) | - | None - structured-generation library reference | HIGH | SKIP - overlaps with instructor/guidance in this batch, best value requires a local model backend | - Use when an LLM call must return guaranteed-valid structured data - a Pydantic-modeled JSON object, a regex-matching string, a fixed enum/Literal choice, or a plain numeric type - from either a local or API-hosted model<br>- Core steps: wrap the model with the matching v1 factory function (from_transformers, from_llamacpp, from_vllm, or from_openai), then call the resulting model object directly with a prompt plus the desired output type; JSON/Pydantic results come back as a JSON string that must be parsed with model_validate_json<br>- Constraints are enforced by compiling the output type into a token-level automaton that filters invalid tokens during decoding, so structured generation runs at close to the same speed as unconstrained generation<br>- Pitfalls: this guide targets the current v1 API only - older pre-1.0 helper functions like outlines.generate.json/choice/regex were removed and will raise errors; forgetting to parse the returned JSON string back into the Pydantic model is a common mistake<br>- Verify success by confirming the returned string parses cleanly into the target Pydantic model or matches the given regex/enum with no manual retry logic needed |
| serving-llms-vllm | skill | mlops | Reference guide for vLLM, a high-throughput LLM inference server built on PagedAttention and continuous batching, exposing an OpenAI-compatible API. | vllm, torch, transformers Python packages; NVIDIA GPU(s) (also supports AMD ROCm, Intel GPUs, TPUs); optional Docker for deployment | none | N/A | optional-skills/mlops/inference/serving-llms-vllm/SKILL.md | official | MIT (repo) | - | None - production inference-server reference, no Hermes coupling | HIGH | SKIP - production GPU-serving infrastructure, out of scope for a general default agent catalog | - Use when deploying a production-grade, high-throughput OpenAI-compatible LLM API, or running large offline batch-inference jobs, especially with constrained GPU memory<br>- Core steps: install vllm, then either drive offline batch generation via the LLM class + SamplingParams, or launch `vllm serve <model>` for an HTTP server; tune GPU memory utilization, max context length, tensor-parallel size, and quantization (AWQ/GPTQ/FP8) based on model size and hardware<br>- Production rollout involves load-testing for target latency/throughput before going live, monitoring the built-in Prometheus metrics endpoint, and deploying via the official Docker image for reproducibility<br>- Pitfalls: tensor-parallel size should be a power of two for best performance; low throughput usually means GPU utilization or max concurrent sequences is too low; slow first-token latency is fixed with prefix caching or chunked prefill; the deprecated `--speculative-model` flag was replaced by a JSON `--speculative-config` object<br>- Verify success by confirming time-to-first-token and requests-per-second meet targets, GPU utilization stays high, and there are no out-of-memory errors in server logs |
| instructor | skill | mlops | Reference guide for Instructor, a library that turns any LLM API call into a validated, typed Pydantic object with automatic retry on validation failure. | instructor package, pydantic, and a provider SDK (openai or anthropic) or a local OpenAI-compatible endpoint such as Ollama | API key (for OpenAI/Anthropic); none when pointed at a local OpenAI-compatible server | N/A | optional-skills/mlops/instructor/SKILL.md | official | MIT (repo) | - | None - generic structured-output wrapper reference, not tied to Hermes's own tool-calling path | HIGH | OPTIONAL - lightweight, broadly-useful pattern for getting validated typed data out of any LLM call | - Use when you want an LLM response to come back as a fully validated, typed Python object instead of raw text or hand-parsed JSON<br>- Core steps: wrap a provider client with instructor (e.g. instructor.from_anthropic/from_openai), then pass a `response_model` Pydantic class into the normal chat/completion call; nested models, enums, and both field-level and whole-model validators are all supported out of the box<br>- On a Pydantic ValidationError, Instructor automatically resends the call with the validation error appended so the model can self-correct, up to a configurable max_retries<br>- It also supports streaming partial objects or iterables of objects as they're generated, and can talk to local OpenAI-compatible servers like Ollama by switching to JSON mode<br>- Pitfalls: vague Pydantic field descriptions lead to poor extraction accuracy; not every provider mode is universally supported, so falling back to Mode.JSON is sometimes required; unbounded retries on an unfixable input waste API calls<br>- Verify success by catching ValidationError after retries are exhausted, and otherwise confirming the returned object's fields and constraints fully match the declared schema |
| lambda-labs | skill | mlops | Reference guide for provisioning and managing on-demand GPU cloud instances and multi-node Slurm clusters on the Lambda Labs commercial cloud. | lambda-cloud-client Python package or raw REST/curl calls, a Lambda Labs account with billing enabled, an API key, and an SSH key | API key (plus an SSH key for instance access) | N/A | optional-skills/mlops/lambda-labs/SKILL.md | official | MIT (repo) | - | None - vendor cloud-provider CLI/API/SSH reference | LOW | SKIP - tied to one commercial, billed GPU cloud vendor's account and API, not portable as a generic capability | - Use when a user needs to provision, manage, or SSH into dedicated Lambda Labs GPU instances (or large Slurm-based multi-node clusters) for ML training or inference<br>- Setup steps: create a Lambda account with a payment method, generate an API key, and add an SSH key, then launch instances either from the web console or via the Python/REST API by specifying region, instance type, SSH key(s), and an optional persistent filesystem<br>- Instances ship with "Lambda Stack" pre-installed (PyTorch, TensorFlow, CUDA, NCCL); persistent filesystems mounted under a fixed NFS path should be used for data/checkpoints that must survive instance termination, and torchrun/DDP or Slurm handle multi-GPU/multi-node training<br>- Pitfalls: local instance disk is ephemeral and wiped on termination unless data lives on an attached persistent filesystem; there is no auto-stop, so idle instances keep billing until manually terminated; filesystems must be created in the same region as the instance and attached at launch time<br>- Verify success by confirming SSH access works, `nvidia-smi`/`torch.cuda.is_available()` report the expected GPU count/type, and checkpoints land under the persistent filesystem path |
| llava | skill | mlops | Reference guide for LLaVA, an open-source vision-language chat model for visual question answering, image captioning, and multi-turn image dialogue. | LLaVA GitHub repo (git clone + pip install -e), transformers, torch, pillow, a LLaVA/Vicuna model checkpoint, GPU strongly recommended | none (public Hugging Face checkpoints); a token may be needed for gated checkpoints | N/A | optional-skills/mlops/llava/SKILL.md | official | MIT (repo) | wraps LLaVA/Vicuna checkpoints that are typically distributed as deltas requiring Meta's base LLaMA weights, which historically carried a non-commercial, research-only license (license unclear/non-permissive depending on the exact checkpoint used) | None - heavyweight local vision-language model reference | HIGH | SKIP - heavy GPU vision-language model with a non-trivial custom repo setup and possible base-model licensing caveats | - Use for open-source vision-language chat tasks: visual question answering, detailed image captioning, multi-turn dialogue grounded in an image, or basic scene/document understanding<br>- Core steps: clone and pip-install the LLaVA repo, load a pretrained checkpoint (7B/13B/34B) plus its image processor, build a conversation using LLaVA's template with an embedded image-token placeholder, then generate a response conditioned on the processed image and prompt<br>- Multi-turn chat works by appending each prior response back into the conversation object before adding the next user turn; a CLI tool and a Gradio web UI are both provided for interactive use<br>- Pitfalls: CPU inference is impractically slow, so a GPU with meaningful VRAM is effectively required (4-bit/8-bit quantization can cut memory needs roughly 2-4x); the model can hallucinate details and is weak at precise spatial reasoning or object counting; some checkpoints depend on separately-licensed base LLaMA/Vicuna weights that must be obtained and merged separately<br>- Verify success by comparing generated answers to expected ground truth on sample images, or by checking standard benchmark scores (e.g. VQAv2, GQA) when evaluating rigorously |
| modal | skill | mlops | Reference guide for Modal, a Python-native serverless GPU cloud for running ML training jobs, batch processing, and auto-scaling model APIs without managing infrastructure. | modal Python package (>=1.0), a Modal cloud account authenticated via `modal setup`, billed GPU/CPU usage | OAuth (browser-based device login via `modal setup`) | N/A | optional-skills/mlops/modal/SKILL.md | official | MIT (repo) | - | None - vendor serverless-compute API reference | LOW | SKIP - tied to one commercial, billed serverless GPU vendor's account and Python SDK, not portable as a generic capability | - Use when deploying ML training/inference jobs, auto-scaling model APIs, or scheduled batch jobs to Modal's serverless GPU cloud without provisioning or managing servers<br>- Core steps: install modal and authenticate via the browser-based `modal setup` flow, define an App with Python decorators specifying GPU type/container image/volumes on functions or classes, then run/serve/deploy the script with the modal CLI, using @modal.enter() to load models once per container and @modal.method() for callable methods<br>- Supports container images built through a Python API (pip/apt installs), persistent Volumes for model/data caching, Secrets for credentials, multiple web-endpoint styles (FastAPI, full ASGI/WSGI apps, raw HTTP servers), dynamic request batching, and cron/interval-based scheduling<br>- Pitfalls: Modal 1.0 renamed several autoscaler parameters (container_idle_timeout to scaledown_window, concurrency_limit to max_containers, keep_warm to min_containers) and moved concurrent-input handling to a separate @modal.concurrent decorator, so code following older tutorials will fail; cold-start latency is mitigated by keeping containers warm and loading models in lifecycle hooks rather than per-call<br>- Verify success by running functions locally first via `.local()` before deploying remotely, then checking deployed app logs and confirming GPU-backed functions return the expected results |
| huggingface-hub | skill | mlops | Reference guide for the official `hf` CLI, the modern replacement for `huggingface-cli`, covering search, download, upload, and management of Hugging Face Hub models, datasets, and Spaces. | `hf` CLI (installed via an install script), a Hugging Face account and token for authenticated operations (upload, private/gated repos) | API key (HF_TOKEN, only required for authenticated operations; public reads need none) | N/A | optional-skills/mlops/models/huggingface-hub/SKILL.md | official | MIT (repo) | - | None - standalone CLI reference; complements other Hub-referencing skills in this batch (e.g. llama-cpp) but has no direct Hermes tool/config coupling | HIGH | OPTIONAL - broadly useful, lightweight reference for a very commonly needed capability (finding/downloading/uploading Hub assets) | - Use whenever a task involves finding, downloading, uploading, or managing models/datasets/Spaces on the Hugging Face Hub, and replace any reference to the deprecated `huggingface-cli` with the current `hf` command<br>- Core commands: `hf download`/`hf upload` for repo files, `hf auth login`/`whoami` for token-based authentication, and `hf repos create/delete/duplicate/move` for repository lifecycle management<br>- Also covers dataset/model listing and info commands, running SQL queries over dataset parquet files via DuckDB, managing pull requests/discussions, and infrastructure operations like Inference Endpoints, Jobs, and Spaces dev-mode<br>- Supports automation-friendly output via a `--format json` flag and cache-management subcommands (list/prune/verify) for controlling local disk usage<br>- Pitfalls: falling back to the deprecated `huggingface-cli` instead of `hf`; using the deprecated `hf upload-large-folder` command instead of the now-unified `hf upload`<br>- Verify success by checking command exit status, and for downloads/uploads, confirming the expected files appear in the local cache or on the Hub repo page |
| segment-anything-model | skill | mlops | Reference guide for Meta's Segment Anything Model (SAM), used for zero-shot image segmentation from point, box, or automatic prompts. | segment-anything package (or transformers' SamModel/SamProcessor), a SAM checkpoint (ViT-B/L/H, up to 2.4GB), torch, opencv-python or pillow, GPU recommended | none | N/A | optional-skills/mlops/models/segment-anything-model/SKILL.md | official | MIT (repo) | - | None - pure computer-vision library reference | HIGH | SKIP - specialized image-segmentation model with large checkpoint downloads, narrow relevance for a default catalog | - Use for zero-shot image segmentation: extracting object masks from point clicks, bounding boxes, or fully automatic whole-image mask generation, without any task-specific training<br>- Core steps: download a SAM checkpoint (ViT-B smallest/fastest through ViT-H largest/most accurate), load it via SamPredictor (or HuggingFace's SamModel/SamProcessor), call set_image() once to compute embeddings, then predict masks from point/box prompts, iteratively refining by feeding the previous mask back in as an additional input<br>- For prompt-free full-image segmentation, use SamAutomaticMaskGenerator, which returns every detected mask along with IoU/stability scores and a bounding box, filterable by quality thresholds<br>- The model can be exported to ONNX for lightweight or browser-based deployment, and predicted masks can be converted to COCO RLE format for standard dataset interchange<br>- Pitfalls: larger checkpoints need multiple GB of VRAM - use ViT-B or half/8-bit precision on constrained hardware; automatic mask generation with a high points-per-side setting can be slow; a single ambiguous point prompt can yield a low-quality mask, so combining a box with points usually helps<br>- Verify success by checking the predicted_iou/stability_score of returned masks against quality thresholds, or visually confirming the segmentation overlay matches the intended object boundary |
| nemo-curator | skill | mlops | GPU-accelerated pipeline for cleaning, deduplicating, filtering and redacting PII from LLM pretraining data at terabyte scale using NVIDIA's Curator toolkit. | nemo-curator, cudf/dask/RAPIDS GPU stack, NVIDIA GPU(s), optional multi-GPU/Dask cluster | none | N/A | optional-skills/mlops/nemo-curator/SKILL.md | official | MIT (repo) | - | none - pure library/CLI reference guide, only points to internal references/ files | MEDIUM | SKIP - heavy GPU data-curation infra, niche pretraining use case | - Use when preparing large web-scale text/image/video/audio corpora for LLM pretraining and need dedup/quality filtering faster than CPU tools.<br>- Steps: install the right extra for the target GPU/modality, then compose a pipeline of stages (quality filters, exact/fuzzy/semantic dedup, PII redaction, classifier filtering) and run it through the Ray-based executor.<br>- Pitfall: the library had a major 1.x rewrite from an object-oriented dataset API to a Ray pipeline/stage architecture, so older tutorials/snippets don't run as-is.<br>- Pitfall: fuzzy/semantic dedup and GPU clustering need enough VRAM and multi-GPU orchestration (Dask-CUDA); underestimating this causes OOM.<br>- Verifies success by comparing dataset size/quality metrics before and after (dedup ratio, filtered doc counts) and spot-checking curated output. |
| obliteratus | skill | mlops | CLI-driven toolkit that strips refusal/safety guardrails from open-weight LLMs via mechanistic-interpretability weight surgery (diff-in-means, SVD, and related techniques). | obliteratus CLI (installed from GitHub), torch, transformers, bitsandbytes, accelerate, safetensors, GPU (or Apple MLX/CPU for tiny models), optional HuggingFace token for uploads | none (optional API key for HF upload) | N/A | optional-skills/mlops/obliteratus/SKILL.md | official | MIT (repo) | wraps OBLITERATUS itself, which is AGPL-3.0 licensed - the skill mandates CLI-only/subprocess invocation, never importing it as a Python library, to avoid copyleft contamination of the host project | references Hermes's skill-file-viewing convention (skill_view) to load supplementary reference/template files, and names sibling skills (vllm, gguf, huggingface-tokenizers) as complementary | HIGH | SKIP - narrow, ethically sensitive model-modification use case with heavy GPU requirements | - Use only when a user explicitly wants to remove refusal behavior from an open-weight model, not for general fine-tuning.<br>- Steps: check hardware/VRAM tier, pick a compute-appropriate model, choose an abliteration method (default advanced SVD-based; specialized ones for MoE, reasoning models, or stubborn refusals), then run the obliterate command and test the output model.<br>- Critical constraint: because the underlying tool is AGPL-3.0, it must only be invoked as an external CLI/subprocess, never imported as a Python library, to keep the host codebase's permissive license intact.<br>- Pitfall: overly aggressive settings can degrade coherence (perplexity spikes) while under-aggressive settings leave refusals intact; small (under 1B) and MoE models need special handling.<br>- Verifies success via built-in metrics: refusal rate, perplexity delta, KL divergence, and qualitative coherence checks after each run. |
| peft | skill | mlops | Reference guide for parameter-efficient fine-tuning of LLMs (LoRA, QLoRA, and related adapter methods) that trains under 1% of model weights to fit large models on modest GPUs. | peft, transformers, torch, bitsandbytes (for quantization); NVIDIA GPU; optional HuggingFace Hub token to push adapters/models | none (optional API key for Hub push) | N/A | optional-skills/mlops/peft/SKILL.md | official | MIT (repo) | - | none - pure library reference guide | HIGH | SKIP - GPU-bound model fine-tuning workflow, out of scope for a general default agent | - Use when a user wants to fine-tune a 1B-70B+ language model on limited GPU memory rather than doing full-parameter fine-tuning.<br>- Core workflow: load a base model, wrap it with a LoRA (or QLoRA, with 4-bit quantization) adapter config specifying rank/alpha/target modules, train with a standard Trainer/SFTTrainer loop, then save just the small adapter.<br>- Adapters can later be merged back into the base model for deployment, or kept separate and hot-swapped for multi-task serving.<br>- Pitfall: wrong target-module names per architecture, mismatched rank/alpha ratios, and OOM during training are the most common failure points; each has documented workarounds (gradient checkpointing, smaller batch plus accumulation, lower rank).<br>- Verifies success by tracking trainable-parameter percentage, held-out loss/quality metrics (e.g. MMLU), and confirming the adapter activates correctly at inference. |
| pinecone | skill | mlops | Guide to using Pinecone, a fully-managed serverless vector database, for production RAG and semantic-search applications. | pinecone Python client, Pinecone cloud account and API key; optional LangChain/LlamaIndex integration libraries | API key | N/A | optional-skills/mlops/pinecone/SKILL.md | official | MIT (repo) | - | none - pure API/library reference guide | HIGH | OPTIONAL - lightweight, broadly useful for teams wanting a managed vector store for RAG, but requires a paid/free-tier external account so not sane as a default-on skill | - Use when a project needs a managed, auto-scaling vector database instead of self-hosting one, typically for production RAG or semantic search.<br>- Steps: create an index with a chosen embedding dimension and distance metric, upsert vectors with metadata (batched), then query with top-k and optional metadata filters or namespaces.<br>- Supports hybrid dense+sparse retrieval, but weighting between dense and sparse scores must be done by manually rescaling the query vectors before sending them, not via a query-time parameter.<br>- Pitfall: dimension mismatches between the embedding model and the index, and forgetting to batch upserts (hurts throughput and cost).<br>- Verifies success by checking index stats (vector counts, namespaces) and confirming query results/scores match expectations for known test vectors. |
| pytorch-fsdp | skill | mlops | Auto-generated reference for PyTorch's Fully Sharded Data Parallel (FSDP/FSDP2) API used to train very large models by sharding parameters, gradients, and optimizer state across GPUs. | torch>=2.0, transformers, multi-GPU (ideally multi-node) NVIDIA hardware | none | N/A | optional-skills/mlops/pytorch-fsdp/SKILL.md | official | MIT (repo) | - | none - generic scraped documentation skill with a references/ folder to consult for detailed patterns | MEDIUM | SKIP - distributed-training infrastructure topic, not relevant to a general-purpose default agent | - Use when a user needs to train or fine-tune a model too large to fit on one GPU, using PyTorch-native sharding rather than a third-party framework.<br>- The skill itself is thin (auto-generated placeholder text) and defers almost all real content to a large common-patterns reference file that must be loaded separately for actual code.<br>- Typical flow: wrap the model with FSDP, choose a sharding strategy and mixed-precision policy, optionally enable CPU offloading, and launch via torchrun across ranks.<br>- Pitfall: sharding strategy and wrap-policy choices interact with memory and speed in non-obvious ways, and checkpoint formats differ from single-GPU training.<br>- Verification is implicit: successful multi-GPU training runs without OOM and reproduces expected loss curves comparable to non-sharded runs. |
| pytorch-lightning | skill | mlops | Introduces PyTorch Lightning, a high-level training framework that removes device and distributed-training boilerplate from PyTorch training loops. | lightning, torch, transformers; GPU/TPU optional (also works on CPU) | none | N/A | optional-skills/mlops/pytorch-lightning/SKILL.md | official | MIT (repo) | - | none - pure framework reference guide | HIGH | SKIP - training-loop engineering framework, out of scope for a general default agent, though lighter-weight than most in this batch | - Use when a user is writing custom PyTorch training code and wants built-in support for multi-GPU/TPU, mixed precision, checkpointing, and logging without rewriting the loop by hand.<br>- Core pattern: subclass LightningModule to define training/validation/test steps and the optimizer, then hand it to a Trainer that manages devices, precision, and distributed strategy (DDP/FSDP/DeepSpeed) via simple flags.<br>- Callbacks (ModelCheckpoint, EarlyStopping, LearningRateMonitor) plug into the Trainer for common monitoring needs instead of manual bookkeeping.<br>- Pitfall: forgetting to pass a validation dataloader silently skips validation; distributed strategy behavior needs explicit accelerator/devices settings rather than relying on auto-detection.<br>- Verifies success via logged metrics (loss curves, validation accuracy) automatically surfaced to TensorBoard/other loggers, and by testing on CPU/single GPU before scaling out. |
| qdrant | skill | mlops | Reference guide for Qdrant, an open-source Rust-based vector search engine for production RAG, semantic search, and recommendation systems. | qdrant-client Python package; a running Qdrant instance (self-hosted via Docker or Qdrant Cloud) | none for self-hosted; API key for Qdrant Cloud | N/A | optional-skills/mlops/qdrant/SKILL.md | official | MIT (repo) | - | none - pure API/library reference guide | HIGH | OPTIONAL - lightweight, broadly useful self-hostable vector store for RAG-oriented agents, though it is one of several equivalent vector-DB choices | - Use when building a RAG or semantic-search feature that needs a self-hostable (or cloud) vector database with rich metadata filtering and hybrid dense/sparse search.<br>- Steps: run a Qdrant server (Docker or cloud), create a collection with a vector size/distance metric, upsert points (vector plus payload), then query with query_points (the current API - search/search_batch are removed in the 1.14+ client).<br>- Supports named multi-vectors, sparse vectors (BM25/SPLADE-style), payload indexing for fast filtered search, and scalar/product/binary quantization for memory savings at scale.<br>- Pitfall: filtering on unindexed payload fields is slow; a payload index must be created; also watch for the client API's move away from the older search/search_batch method names.<br>- Verifies success via collection stats (point/vector counts) and by validating query results/scores against known nearest neighbors. |
| dspy | skill | mlops | Covers DSPy, Stanford NLP's declarative framework for programming (not manually prompting) language models with automatically-optimized prompts. | dspy library plus an LM provider SDK (openai, anthropic) or a local model server (e.g. Ollama); provider API key for hosted LMs | API key (for hosted LM providers); none for local models | N/A | optional-skills/mlops/research/dspy/SKILL.md | official | MIT (repo) | - | none - pure framework/library reference guide | HIGH | OPTIONAL - useful for teams building systematic, data-optimized LLM pipelines (RAG, classifiers, agents), though it is a specialized programming paradigm rather than an everyday default | - Use when a user wants to define LM tasks declaratively (signatures/modules) and let an optimizer tune prompts/few-shot examples from data, instead of hand-crafting prompts.<br>- Core building blocks: Signatures describe input/output fields, Modules (Predict, ChainOfThought, ReAct, ProgramOfThought) implement the task, and optimizers (BootstrapFewShot, MIPRO, BootstrapFinetune) improve them against a metric and training examples.<br>- Complex behavior is built by composing modules into a custom Module subclass's forward() method (e.g., multi-hop RAG, self-consistency, reranking).<br>- Pitfall: needs representative labeled/training examples for optimizers to be effective; without them DSPy is just a more structured prompting layer.<br>- Verifies success by running an Evaluate harness against a metric function and comparing optimized vs. unoptimized module scores on a held-out set. |
| saelens | skill | mlops | Guide to SAELens, the primary library for training and analyzing sparse autoencoders (SAEs) that decompose polysemantic model activations into interpretable features. | sae-lens, transformer-lens, torch; GPU recommended; optional Weights and Biases account for training logs | none (optional API key for W&B) | N/A | optional-skills/mlops/saelens/SKILL.md | official | MIT (repo) | - | none - pure research-library reference guide | MEDIUM | SKIP - specialized mechanistic-interpretability research tool, not relevant to general-purpose agent use | - Use when a user is doing interpretability research: wanting to discover what concepts/features a model's internal activations encode, or to steer/ablate model behavior via those features.<br>- Two main workflows: load a pretrained SAE paired with a TransformerLens model to encode/decode activations and inspect top-activating features, or train a new SAE on a target model layer with a configured architecture (Standard/Gated/TopK/JumpReLU) and sparsity penalty.<br>- Feature vectors from the SAE decoder can be added back into the residual stream to steer generation, or used for logit attribution to explain specific outputs.<br>- Pitfall: too little L1 warm-up kills features early (dead features); wrong sparsity/capacity settings hurt reconstruction quality or interpretability; the library underwent a v6 config rewrite that nests options and removed several legacy flat parameters.<br>- Verifies success via standard SAE metrics: L0 (average active features), cross-entropy loss recovered, dead-feature ratio, and explained variance. |
| simpo | skill | mlops | Reference-free preference-alignment method (SimPO) presented as a simpler, often better-performing alternative to DPO for aligning LLMs with human preferences. | torch, transformers, datasets, trl, accelerate, flash-attn; multi-GPU (A100/H100) hardware; alignment-handbook training scripts | none | N/A | optional-skills/mlops/simpo/SKILL.md | official | MIT (repo) | - | none - pure training-method reference guide | MEDIUM | SKIP - GPU-heavy preference-alignment training pipeline, not relevant to a general default agent | - Use when a user has a chosen/rejected preference dataset and wants to align a model without training or hosting a separate reference model (unlike DPO).<br>- Workflow: set up the alignment-handbook environment with Flash Attention, write a YAML training config specifying the SimPO hyperparameters (beta, gamma/beta ratio, loss type), then launch via accelerate with a DeepSpeed ZeRO-3 config.<br>- Key knobs: beta controls reward scaling, gamma_beta_ratio sets a target margin between chosen/rejected, and an optional SFT-loss weight can be added to reduce capability drift.<br>- Pitfall: too-high learning rate or beta causes loss divergence; too little margin/beta yields poor preference separation; OOM is common on smaller GPUs without ZeRO-3/gradient checkpointing.<br>- Verification is done by comparing win-rate/quality benchmarks of the aligned model against the base model, tracking preference-loss curves during training. |
| slime | skill | mlops | Framework guide for slime, a Megatron-LM plus SGLang based reinforcement-learning post-training system (used to train the GLM model family) supporting GRPO-style RL at scale. | sglang-router, ray, torch, transformers, Megatron-LM, SGLang, multi-node/multi-GPU cluster, Docker | none | N/A | optional-skills/mlops/slime/SKILL.md | official | MIT (repo) | - | none - pure infra/framework reference guide | LOW | SKIP - large-scale distributed RL post-training infrastructure, entirely out of scope for a general agent catalog | - Use only for large-scale RL post-training (e.g., GRPO) of LLMs where a team already runs Megatron-LM for training and SGLang for rollout generation.<br>- Core architecture: a shared data buffer feeds prompts to a Megatron-based actor/critic trainer and an SGLang rollout engine, with periodic weight syncing between the two.<br>- Supports synchronous and asynchronous training modes, plus a customizable multi-turn/tool-use generation function for agentic RL training.<br>- Pitfall: rollout batch size, samples-per-prompt, and global batch size must satisfy an exact arithmetic constraint; SGLang engine crashes and weight-sync timeouts are common failure modes with documented flag-based mitigations.<br>- Verifies success via TensorBoard reward curves increasing over rollouts and per-task evaluation metrics (e.g. accuracy on held-out eval sets like AIME/GSM8K). |
| stable-diffusion | skill | mlops | Practical guide to text-to-image generation, image-to-image, inpainting, and ControlNet workflows using HuggingFace's Diffusers library and the Stable Diffusion model family. | diffusers, transformers, accelerate, torch, optional xformers; NVIDIA GPU; HuggingFace model downloads (some gated models need a token) | none (public models); API key/token for gated HuggingFace models | N/A | optional-skills/mlops/stable-diffusion/SKILL.md | official | MIT (repo) | some checkpoints/gated models use restrictive OpenRAIL-style community licenses (non-commercial/use-restricted), separate from the Apache-2.0 Diffusers library itself | none - pure library/API reference guide | MEDIUM | SKIP - GPU-heavy image-generation workflow, not suited to a general-purpose default agent | - Use when a user wants to generate, transform, inpaint, or control-guide images from text using Stable Diffusion variants (1.5, SDXL, SD3, Flux).<br>- Basic flow: load a pipeline for the chosen model/task (text2img, img2img, inpaint, ControlNet), configure generation parameters (steps, guidance scale, resolution, seed), and call the pipeline to produce images.<br>- Advanced features include swapping schedulers for speed/quality tradeoffs, loading LoRA style adapters, and applying spatial conditioning via ControlNet models (edges, pose, depth).<br>- Pitfall: GPU memory pressure is common with larger models/resolutions; mitigations include CPU offloading, attention/VAE slicing, xFormers, and lower precision (fp16/bf16).<br>- Verification is visual/qualitative (inspecting output images) plus checking for common failure signatures like black/noisy output (usually a VAE/dtype mismatch). |
| tensorrt-llm | skill | mlops | Guide to NVIDIA's TensorRT-LLM library for compiling and serving LLMs with maximum inference throughput and low latency on NVIDIA GPUs. | tensorrt-llm, torch, NVIDIA CUDA GPU (A100/H100/GB200), NGC Docker registry or pip install, trtllm-serve CLI | none | N/A | optional-skills/mlops/tensorrt-llm/SKILL.md | official | MIT (repo) | - | none - pure inference-serving reference guide | LOW | SKIP - specialized, NVIDIA-only high-performance inference infrastructure, irrelevant to a general default agent | - Use when deploying an LLM for maximum throughput/low latency specifically on NVIDIA GPU infrastructure, as an alternative to vLLM or llama.cpp.<br>- Basic flow: install via the NGC Docker image or pip, then either call the Python LLM/SamplingParams API directly or launch the trtllm-serve HTTP server with tensor-parallelism/batch-size flags for production serving.<br>- Key performance features: in-flight batching, paged KV cache, FP8/INT4/FP4 quantization, CUDA graphs, and tensor/pipeline/expert parallelism for multi-GPU and multi-node scaling.<br>- Pitfall: strict CUDA/TensorRT/Python version requirements make environment setup brittle; picking the wrong parallelism degree for the model size wastes GPU memory or throughput.<br>- Verification is throughput/latency benchmarking (tokens/sec, per-token latency) compared against baseline frameworks like plain PyTorch or vLLM. |
| torchtitan | skill | mlops | PyTorch's official framework for pretraining LLMs from scratch at scale using composable 4D parallelism (FSDP2, tensor, pipeline, and context parallelism). | torch>=2.6, torchtitan, torchao; multi-GPU/multi-node NVIDIA cluster (ideally H100s); HuggingFace token for tokenizer download; SLURM for multi-node jobs | API key (HuggingFace token for asset download) | N/A | optional-skills/mlops/torchtitan/SKILL.md | official | MIT (repo) | example workflows download a gated Meta Llama tokenizer/checkpoint that requires accepting Meta's own community license, separate from this skill's MIT license | none - pure distributed-pretraining framework reference guide | LOW | SKIP - large-scale LLM pretraining, entirely out of scope for a general-purpose default agent | - Use only for pretraining an LLM from scratch (not fine-tuning) at scales from single-node 8B models up to 405B+ models across hundreds of GPUs.<br>- Configuration is done through a Python config registry (selected by name) rather than hand-written TOML files, with individual fields overridable on the command line.<br>- Scaling knobs include FSDP2 sharding degree, tensor/pipeline/context parallelism degrees, activation checkpointing mode, and Float8 quantization (for H100 speedups, requires torch.compile).<br>- Pitfall: large pipeline-parallel or 4D-parallel runs require creating a seed checkpoint first for consistent initialization; changing parallelism degrees between runs requires distributed-checkpoint resharding rather than naive loading.<br>- Verifies success via tokens/sec/GPU throughput benchmarks and TensorBoard-logged loss curves, compared against published baseline numbers for known model/parallelism configurations. |
| axolotl | skill | mlops | Reference guide for Axolotl, a YAML-configuration-driven framework for fine-tuning LLMs with LoRA/QLoRA and preference-optimization methods (DPO, KTO, ORPO, GRPO). | axolotl, torch, transformers, datasets, peft, accelerate, deepspeed; GPU (single or multi-node) | none | N/A | optional-skills/mlops/training/axolotl/SKILL.md | official | MIT (repo) | - | none - auto-generated scraped documentation skill with supplementary references/ files (api, dataset-formats, other) to consult for detail | MEDIUM | SKIP - GPU fine-tuning framework, out of scope for a general default agent | - Use when a user wants to fine-tune one of 100+ supported open models via a declarative YAML config rather than writing training code by hand.<br>- Typical flow: pick or write a YAML config specifying the base model, adapter type (LoRA/QLoRA/full), dataset format, and optional preference-optimization method (DPO/KTO/ORPO/GRPO), then run Axolotl's training entrypoint against it.<br>- Supports advanced parallelism (FSDP2 configuration blocks), context/sequence parallelism for long sequences, and compressed checkpoint saving compatible with vLLM.<br>- Pitfall: this specific skill file is mostly auto-generated boilerplate with scattered example snippets rather than a coherent guide; real usage requires consulting the dataset-formats and api reference files for correct YAML schema.<br>- Verification is training-loss/eval-metric tracking plus test inference against the fine-tuned checkpoint to confirm the adapter or merged model behaves as intended. |
| trl-fine-tuning | skill | mlops | Guide to HuggingFace TRL for post-training LLMs with SFT, DPO, reward-model training, and online RL methods (GRPO, RLOO) as part of an RLHF pipeline. | trl, transformers, datasets, peft, accelerate, torch; GPU (single or multi-GPU) | none | N/A | optional-skills/mlops/training/trl-fine-tuning/SKILL.md | official | MIT (repo) | - | none - pure library/framework reference guide | HIGH | SKIP - GPU-bound RLHF/fine-tuning pipeline, out of scope for a general default agent | - Use when a user wants to align or instruction-tune an LLM using HuggingFace's TRL trainers rather than building an RL loop from scratch.<br>- Full RLHF pipeline: SFT on instruction data, then train a reward model on chosen/rejected pairs, then run an online RL trainer (RLOO or GRPO - PPO was removed in TRL 1.x) against that reward signal.<br>- Simpler alternative: DPO trains directly on preference pairs without a separate reward model or online rollouts.<br>- Pitfall: assuming PPOTrainer/PPOConfig still exist (removed in TRL 1.x) is a common outdated-code trap; OOM during DPO/RL training is common and mitigated via smaller batches, gradient accumulation, and gradient checkpointing.<br>- Verifies success by monitoring per-method loss/reward curves and evaluating the final model's generations qualitatively or against the reward model/held-out preference set. |
| unsloth | skill | mlops | Reference pointer to Unsloth, a library that speeds up LoRA/QLoRA fine-tuning of LLMs by 2-5x while using significantly less GPU memory. | unsloth, torch, transformers, trl, datasets, peft; NVIDIA GPU | none | N/A | optional-skills/mlops/training/unsloth/SKILL.md | official | MIT (repo) | - | none - auto-generated scraped documentation skill; content is minimal and defers to a single references/llms-txt.md file | MEDIUM | SKIP - GPU fine-tuning acceleration library, out of scope for a general default agent | - Use when a user wants faster, lower-memory LoRA/QLoRA fine-tuning of popular open models (Llama, Mistral, Gemma, Qwen) than standard HuggingFace PEFT/TRL provides.<br>- This particular skill file is essentially a stub/placeholder (no real workflow, quick-reference, or examples) that only points to one raw documentation dump file for details.<br>- A faithful rewrite would need to source Unsloth's actual quick-start pattern (load a 4-bit model via its fast model-loading wrapper, attach LoRA adapters, train with TRL's SFTTrainer, export merged or GGUF weights).<br>- Pitfall: because the shipped skill has almost no substantive content, relying on it as-is would give the agent little more than the library name and dependency list.<br>- Verification would follow standard fine-tuning checks: trainable-parameter counts, training/eval loss, and a manual generation test on the fine-tuned model. |
| whisper | skill | mlops | Guide to using OpenAI's Whisper model family for multilingual speech-to-text transcription and translation. | openai-whisper, transformers, torch, ffmpeg (external CLI for audio conversion); optional faster-whisper for streaming | none | N/A | optional-skills/mlops/whisper/SKILL.md | official | MIT (repo) | - | none - pure library/CLI reference guide | HIGH | OPTIONAL - broadly useful, lightweight-enough (tiny/base models run on CPU) audio-transcription capability, unlike the GPU-training-heavy skills in this batch | - Use when a user needs to transcribe or translate speech from audio/video files into text, in any of 99 supported languages.<br>- Steps: install openai-whisper plus ffmpeg, pick a model size trading speed vs. accuracy (tiny through large, plus the faster turbo variant), then call transcribe() (or the whisper CLI) on the audio file, optionally specifying language, task (transcribe vs. translate-to-English), or an initial prompt for domain vocabulary.<br>- Output can include segment- or word-level timestamps and be exported as plain text, SRT/VTT subtitles, or JSON.<br>- Pitfall: the base library only supports batch (offline) transcription, hallucinates on unclear audio, and degrades on long-form (over 30 min) recordings; real-time/streaming needs faster-whisper instead, and there is no built-in speaker diarization.<br>- Verification is straightforward: compare the output transcript against known/reference text or spot-check timestamps against the source audio. |

## Payments

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| mpp-agent | skill | payments | Wraps Machine Payments Protocol wallet CLIs (mppx, Tempo, Privy, AgentCash) so the agent can autonomously pay per-request against HTTP 402 "Payment Required" APIs. | Node.js 20+, one of the mppx / Tempo Wallet / Privy Agent / AgentCash CLIs (npm-distributed), a funded wallet or mppx account, optionally the stripe-link-cli skill for Stripe-flavored 402 challenges | API key | N/A | optional-skills/payments/mpp-agent/SKILL.md | official | MIT (repo) | - | Cross-references the stripe-link-cli skill for method="stripe" challenges | MEDIUM | OPTIONAL - real value for agentic-commerce use cases but moves real money, keep opt-in | - Use when a merchant API responds with HTTP 402 and the user actually wants it paid, not just reported.<br>- Probe the challenge with a plain HTTP request to confirm it is really an MPP 402, pick the wallet client that matches the method the challenge advertises (Stripe challenges route to stripe-link-cli, others to mppx/Tempo/Privy/AgentCash), then pay and verify the returned receipt.<br>- A key safety point: wallet private keys must never be read into the agent's own reasoning context, since each CLI manages its own credential store or ephemeral session key.<br>- A notable pitfall: this skill covers no sandbox/test-mode by default - it pays live 402 challenges with real funds, so it should stay off unless the user explicitly wants agentic spend enabled; zero-amount challenges are valid and don't need a funded wallet.<br>- Verifies success via the wallet CLI listing an existing funded account and the payment call returning the merchant's real response rather than a 402 retry. |
| stripe-link-cli | skill | payments | Wraps Stripe's official Link CLI so the agent can complete real purchases using one-time virtual cards or Shared Payment Tokens, gated by mandatory in-app user approval per spend. | Node.js 20+, the @stripe/link-cli npm package, a US-based Stripe Link account with at least one saved payment method, the Link mobile/web app to approve each spend | OAuth | N/A | optional-skills/payments/stripe-link-cli/SKILL.md | official | MIT (repo) | - | - | MEDIUM | OPTIONAL - real purchasing capability, well-gated by mandatory human approval, keep opt-in | - Use when the user asks the agent to buy something or wants an HTTP 402 challenge paid via their real Stripe Link wallet.<br>- Check or establish device-code login, pick the credential type based on the merchant surface (card for a checkout form, shared_payment_token for a 402 challenge advertising method="stripe"), then create an itemized spend request that pings the user's Link app and blocks until they approve or deny it.<br>- Retrieve the resulting one-time card to a local file rather than printing it, hand that file to a checkout flow, then delete it once the purchase completes.<br>- Notable risk: this moves real money via real cards; every spend requires an explicit in-app human approval (a genuine built-in safeguard) but there is no sandbox/test mode, and the skill is US-only.<br>- Verifies success via auth status showing logged in and the spend-request's approval outcome being captured before the credential is ever used. |
| stripe-projects | skill | payments | Wraps the Stripe Projects CLI plugin to provision third-party SaaS resources (databases, SMS numbers, hosting, etc.) and sync their credentials into a project's .env file. | Stripe CLI plus its "projects" plugin, a Stripe account, and per-provider accounts that get created/billed as services are added (Neon, Twilio, Vercel, etc.) | OAuth | N/A | optional-skills/payments/stripe-projects/SKILL.md | official | MIT (repo) | - | - | MEDIUM | OPTIONAL - real infra billing, useful for dev-infra automation, keep opt-in | - Use when a user wants to spin up a new managed service (a database, an SMS number, hosting, etc.) for their project and have its credentials wired into the codebase automatically.<br>- Initialize the project's encrypted credential vault, browse the provider catalog, add a provider/service (which may prompt for a billing tier), and confirm the resource and its .env keys landed correctly.<br>- Manage the resource's lifecycle afterward via upgrade/remove/rotate subcommands as needed.<br>- Notable risk: provisioning triggers real billing on the user's connected provider accounts with no described sandbox, .env writes are plaintext real secrets that must stay gitignored, and removing a service does not always destroy the underlying paid resource on the provider's side.<br>- Verifies success via the CLI's list command showing the new provider and its populated .env keys. |

## Productivity

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| canvas | skill | productivity | Read-only wrapper around the Canvas LMS REST API for listing a student's or educator's courses and assignments. | Canvas personal access token and base URL (env vars), a bundled Python CLI script | API key | N/A | optional-skills/productivity/canvas/SKILL.md | official | MIT (repo) | - | Reads CANVAS_API_TOKEN / CANVAS_BASE_URL from the Hermes .env | HIGH | OPTIONAL - narrow student/educator audience | - Use when a user wants their Canvas course list or assignment due dates pulled programmatically.<br>- Setup requires generating a personal access token in the Canvas account settings and storing it plus the institution's base URL as environment variables, then calling the bundled script's list_courses and list_assignments subcommands.<br>- The script auto-handles Canvas's Link-header pagination so callers don't have to.<br>- A key pitfall is a 401 response meaning an invalid or expired token (must be regenerated in Canvas), and Canvas's own rate limit is roughly 700 requests per 10 minutes.<br>- Verifies success by confirming list_courses returns data without a 401/403 error, since the skill is strictly read-only and never modifies courses or assignments. |
| decision-questionnaire | skill | productivity | Turns a decision the user can't answer alone into a structured Markdown questionnaire document to hand off to whoever holds the missing knowledge. | None - a pure document-generation skill with no external APIs or tools | N/A | N/A | optional-skills/productivity/decision-questionnaire/SKILL.md | official | MIT (repo) | - | Writes a plain Markdown file to the working directory; no config/tool coupling | HIGH | OPTIONAL - broadly useful, low-risk, but still a specific workflow not everyone needs | - Use when a decision is blocked on facts or judgment only a specific person holds, and the user keeps deferring it pending that person's input.<br>- Interview the user only about "the send" (who's receiving it and what specific answers are needed back) rather than grilling them on the subject matter itself, since that's precisely what they can't answer.<br>- Write a single Markdown file with a purpose statement, a short orienting context paragraph, and questions grouped by theme, ordered most-important-first, each with a single idea and an answer stub beneath it.<br>- Common pitfalls include compound (and/or) questions, dumping excessive background context, and burying the most critical question below less important ones.<br>- Verifies success by confirming every information gap identified in the intake is covered by at least one question and the file's absolute path is reported back to the user. |
| here-now | skill | productivity | Publishes static sites/files to a `{slug}.here.now` URL and manages private cloud "Drive" file storage via the here.now hosting service. | curl, file, jq; optional HERENOW_API_KEY and HERENOW_DRIVE_TOKEN; bundled publish.sh and drive.sh scripts | API key | N/A | optional-skills/productivity/here-now/SKILL.md | official | MIT (repo) | - | Requires the terminal toolset; reads/writes ~/.herenow/credentials and a local .herenow/state.json cache | MEDIUM | OPTIONAL - convenient instant hosting, third-party service dependency | - Use when a user wants to quickly publish a static site/file or needs private cloud storage for agent-generated files.<br>- Publishing runs a create-then-upload-then-finalize flow via the bundled script and returns a live URL; without a saved API key the site is anonymous and expires in 24 hours unless the user claims it, while an authenticated key makes it permanent.<br>- Drives provide private cloud folders that can be shared with scoped, time-limited tokens for agent-to-agent handoff, distinct from the public site-hosting side.<br>- A critical rule baked into the skill is to always fetch the live https://here.now/docs before answering capability questions, since the local instructions can go stale and live API behavior wins on any conflict.<br>- Verifies success via the script's reported siteUrl and auth_mode, which determines whether the user needs to be warned about the 24-hour anonymous-site expiry. |
| live-dashboard | skill | productivity | Builds a self-contained, cron-refreshed HTML status dashboard from arbitrary live sources (email, websites, files) that alerts the user only on material change. | At least one source connector (email/calendar skill, web-extraction/browser tool, or local file reads), a cron-scheduling tool, optional desktop-preview integration | N/A | N/A | optional-skills/productivity/live-dashboard/SKILL.md | official | MIT (repo) | - | Assumes a Hermes-home dashboards/ directory alongside config.yaml, a cronjob tool for scheduling, and an optional desktop_preview tool for GUI sessions | MEDIUM | OPTIONAL - powerful but requires cron plus other connector skills configured first | - Use when a user wants one persistent, living status page tracking some process (applications, deals, shipments) across multiple sources with periodic refresh, as opposed to a one-off status question.<br>- Setup pins down exactly what fields come from which source, does one live foreground read per source up front so auth/permission failures surface immediately rather than on an unattended run, then writes a JSON state file plus a self-contained regenerated HTML page and schedules a recurring refresh job.<br>- Each scheduled tick re-reads every source, diffs against the stored state, marks a failed read stale without destroying the last known-good value, and only messages the user when something material changed or needs attention.<br>- Key pitfalls include building the page before verifying sources, overwriting good data with an empty/error read, and sending a "still watching, nothing changed" message on every tick instead of staying silent.<br>- Verifies success by confirming the JSON state and rendered HTML always agree and every value carries a retrieval timestamp. |
| memento-flashcards | skill | productivity | Local, file-based spaced-repetition flashcard system with agent-graded free-text review and YouTube-transcript quiz generation. | Bundled Python scripts for card storage and YouTube transcript fetching; optional youtube-transcript-api pip package for the quiz feature | none | N/A | optional-skills/productivity/memento-flashcards/SKILL.md | official | MIT (repo) | - | Requires the terminal toolset; stores all cards in a local JSON file under the skill's data directory | HIGH | OPTIONAL - specialized study/learning tool | - Use when a user wants to save facts as flashcards, review cards due today, or generate a quiz from a YouTube video.<br>- Card creation follows a graded-intent rule: an explicit flashcard request creates the card immediately, an incidental factual statement triggers a yes/no confirmation first, and anything else (coding tasks, normal conversation) doesn't trigger the skill at all.<br>- The review flow shows exactly one question at a time, grades the user's free-text answer as correct/partial/incorrect, always reveals the right answer and the next review date before moving on, and schedules the next review using a simple fixed-interval rule that auto-retires a card after three consecutive "easy" ratings.<br>- A key pitfall is that the underlying JSON file must never be hand-edited directly, since only the script's atomic-write commands prevent corruption; also some YouTube videos have no available transcript.<br>- Verifies success via a stats subcommand reporting card counts by status, and by confirming every review or quiz answer receives visible feedback before the next question appears. |
| property-listings | skill | productivity | Formats researched property/rental listings as structured JSON "card" fences that render as browsable, photo-gallery cards inside the Hermes desktop UI. | The Hermes desktop app's built-in renderer for `listing` fenced code blocks; web search/extraction or browser tools to gather the underlying listing data | none | N/A | optional-skills/productivity/property-listings/SKILL.md | official | MIT (repo) | - | Depends entirely on the Hermes desktop client recognizing a `listing` fenced-code-block language as a special card renderer | LOW | SKIP - purely a Hermes-desktop rendering convention, not reusable without matching UI support | - Use only when presenting or re-ranking property/rental search results inside a Hermes desktop conversation, not as a search or valuation tool itself; outside the desktop app, plain Markdown with links should be used instead.<br>- Gather verified address, price, specs, photos, and a canonical source link for each property, deduplicate portal mirrors of the same listing, and emit one JSON object per property inside a fenced code block tagged as the `listing` language.<br>- The schema defines specific fields (address, price, beds/baths, size, note, facts, catches, images, links) and unknown values should be omitted rather than guessed or invented.<br>- A key pitfall is exceeding the renderer's hard caps (roughly 24 listings, 40 images per property, 12 facts/catches/links) or emitting malformed JSON, both of which fall back to a plain, less useful code block.<br>- Verifies success by confirming the desktop UI actually renders the address, price, photo gallery, and links as a native card rather than raw text. |
| shop | skill | productivity | Wraps Shopify's official "shop" CLI (Shop.app) to search its global product catalog, run agent-initiated checkouts with delegated payment, and manage order tracking/returns/reorders. | curl, node, the @shopify/shop-cli npm package; optional Shop.app account sign-in (device-code flow) required for checkout, orders, and spending-budget features | OAuth | N/A | optional-skills/productivity/shop/SKILL.md | official | MIT (repo) | - | Defines strict per-channel message-formatting rules (WhatsApp/iMessage/Telegram) for how product cards must be sent | MEDIUM | OPTIONAL - real purchasing capability, appropriately gated, keep opt-in | - Use for product search, checkout, order tracking, returns, or reorders through Shop.app's catalog rather than a general web search.<br>- The mandated flow is: offer (never force) sign-in before showing any product results, search broadly then narrow, present each product as its own separate message followed by one summary message, optionally offer an AI photo-based try-on visualization for visual goods, and complete checkout only after explicitly confirming purchase intent, item, price, and shipping details.<br>- A hard safety rule is that every checkout warning (final sale, age-restricted, etc.) must be shown to the user verbatim before completing, and a fresh idempotency key is required per distinct purchase intent.<br>- Notable risk: this is a real-money checkout skill; unattended purchases require the user to explicitly set a spending budget first rather than any sandbox/test mode, and prohibited categories (alcohol, weapons, adult content, etc.) must be silently filtered from results.<br>- Verifies success via auth status for sign-in state and by inspecting the checkout response's status and payment fields before ever passing `--confirm`. |
| shopify | skill | productivity | Direct curl-based wrapper for merchants to query and mutate their own Shopify store via the Admin and Storefront GraphQL APIs (products, orders, inventory, customers, metafields, bulk exports, webhooks). | curl, jq, a Shopify custom-app Admin API access token and store domain (env vars) | API key | N/A | optional-skills/productivity/shopify/SKILL.md | official | MIT (repo) | - | Reads SHOPIFY_ACCESS_TOKEN / SHOPIFY_STORE_DOMAIN / SHOPIFY_API_VERSION from the Hermes .env | HIGH | OPTIONAL - merchant-admin-only integration, requires the user to already run a Shopify store | - Use when a merchant wants to manage their own store's products, orders, inventory, customers, or metafields programmatically rather than through the Shopify admin UI.<br>- The core pattern is a reusable curl+jq GraphQL POST helper against the Admin API endpoint with the store's access-token header, always checking both the top-level errors array and each mutation's userErrors field since HTTP 200 does not guarantee success.<br>- Covers product/variant CRUD, order and customer queries, inventory adjust/set, metafields, large bulk-operation exports via polling, and webhook subscriptions verified by HMAC.<br>- Key pitfalls: the REST Admin API is frozen and GraphQL should be used exclusively, a 403 usually means a missing API scope requiring the app to be reconfigured and reinstalled, and orders are silently capped to a recent window without the read_all_orders scope.<br>- Notable risk: mutations are real and can create products, cancel orders, or issue refunds with no staging environment, so the skill requires explicit user confirmation before any destructive or financial mutation; verifies success via a simple shop-name query succeeding and mutation responses returning a populated resource with an empty userErrors array. |
| siyuan | skill | productivity | Curl-based CRUD wrapper over the SiYuan personal-knowledge-base app's local kernel API for searching, reading, creating, and editing notes/blocks. | curl, jq, a running SiYuan desktop or Docker instance, a local API token from SiYuan's settings | API key | N/A | optional-skills/productivity/siyuan/SKILL.md | official | MIT (repo) | - | Reads SIYUAN_TOKEN / SIYUAN_URL from the Hermes .env; optional alternative install path via a native MCP server config in config.yaml | HIGH | OPTIONAL - narrow PKM-tool audience | - Use when a user wants to search, read, or edit notes in their self-hosted SiYuan knowledge base.<br>- Every call is a POST with a JSON body and a `code:0`-means-success envelope, covering full-text and raw-SQL search (SELECT only), reading block/child content and human-readable paths, creating notebooks and documents, appending/updating/deleting blocks, and setting custom attributes (which must be prefixed `custom-`).<br>- A key pitfall is that every endpoint, including reads, must be called via POST, and only SELECT statements should ever be sent to the SQL query endpoint since the app has no separate read-only mode.<br>- Block and notebook IDs follow a strict timestamp-plus-suffix pattern that should be validated before use to catch malformed references early.<br>- Verifies success by checking `code == 0` in every response and, for search calls, that the returned blocks actually contain the expected content. |
| telephony | skill | productivity | Provisions and manages a persistent Twilio phone number for SMS/MMS and direct calls, plus routes outbound AI-driven phone calls through Bland.ai or Vapi. | A bundled Python helper script; a Twilio account for owning a number/SMS/calls; optionally a Bland.ai account (fastest AI-call setup) and/or a Vapi account (higher-quality AI calls, needs a Twilio number imported) | API key | N/A | optional-skills/productivity/telephony/SKILL.md | official | MIT (repo) | - | Persists provider credentials and the owned number into the Hermes .env, plus a separate telephony_state.json checkpoint file under the Hermes home directory | MEDIUM | OPTIONAL - real-world calling/texting capability, correctly kept opt-in | - Use when a user wants Hermes to own a reusable phone number, send/receive SMS, place a direct text-to-speech call, or make a fully AI-driven outbound call.<br>- A decision tree picks the right provider: Twilio for owning a number and texting, Bland.ai for the quickest one-key AI-call setup, Twilio plus Vapi for the best conversational voice quality, and a direct Twilio call with a hosted audio URL for delivering a prerecorded custom message.<br>- State persists across sessions so a later session can run a diagnose command to see already-configured providers, the remembered default number, and the last inbound-SMS-polling checkpoint.<br>- Mandatory safety rules baked into the skill: always confirm with the user before dialing or texting, never call emergency numbers, and never persist a third-party phone number into Hermes's long-term memory.<br>- Verifies readiness via the diagnose subcommand and confirms end-to-end success with a real test SMS send or call placement, since there is no sandbox/test mode - calls and texts incur real carrier charges and reach real people. |

## Research

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| bioinformatics | skill | research | Acts as an index/gateway that clones two external open-source skill libraries (bioSkills, ClawBio) on demand to answer genomics and computational-biology questions. | git, pip/Bioconductor/conda toolchains, external repos GPTomics/bioSkills and ClawBio/ClawBio, various bio CLI tools (samtools, bcftools, blast, minimap2, bedtools) | none | N/A | optional-skills/research/bioinformatics/SKILL.md | official | MIT (repo) | - | none beyond generic terminal tool and cache dir (~/.hermes/cache/scratch) | HIGH | OPTIONAL - niche domain-specific gateway, valuable only for bioinformatics workloads | - Use when a user asks about genomics, sequencing, variant calling, or other computational-biology tasks.<br>- Shallow-clone the referenced bioSkills or ClawBio repos into a scratch cache, then read the specific sub-skill's docs/scripts as reference material.<br>- Pitfall: fetched material isn't in native skill format and assumes bio tooling (conda/samtools/etc.) already installed; large genomic files can consume significant disk.<br>- Success is verified by the ability to open and follow the correct SKILL.md/README under the cloned repo for the requested domain. |
| blogwatcher | skill | research | Wraps the external blogwatcher-cli Go tool to track many blogs/RSS feeds with persistent read/unread state. | blogwatcher-cli binary (Go install/Docker/binary release), optional cronjob tool, web_extract tool | none | N/A | optional-skills/research/blogwatcher/SKILL.md | official | MIT (repo) | - | cronjob tool's monitor field, web_extract tool, terminal tool | MEDIUM | OPTIONAL - useful once a user tracks many feeds with read state; lighter zero-install rss-feeds skill covers occasional use | - Use when the user wants durable, many-feed blog/RSS tracking with read/unread status rather than a one-off feed read.<br>- Install blogwatcher-cli via Go/Docker/binary, add blogs, scan, then list/mark articles as read.<br>- Pair with the cronjob tool's monitor field so ticks only wake the agent when new articles appear.<br>- Pitfall: default SQLite DB is lost on Docker restart unless a volume/env var pins its path; feed article bodies still need web_extract for full text.<br>- Verified by scan output showing new-article counts and the articles command listing them with URLs. |
| darwinian-evolver | skill | research | Thin wrapper around Imbue's open-source evolutionary search loop for optimizing prompts, regex, SQL, or small code snippets against a scorer. | git, uv/Python 3.11+, imbue-ai/darwinian_evolver repo, an LLM API key (Anthropic/OpenRouter/OpenAI) | API key | N/A | optional-skills/research/darwinian-evolver/SKILL.md | official | MIT (repo) | wraps imbue-ai/darwinian_evolver (AGPL-3.0) via subprocess only | none beyond terminal tool and cache directories (~/.hermes/cache) | MEDIUM | OPTIONAL - narrow research/optimization use case, meaningful cost per run | - Use when a user wants to auto-improve a prompt, regex, SQL query, or short code snippet against a measurable scorer, not for subjective or differentiable targets.<br>- Install upstream via git clone plus uv sync, then either run the built-in parrot example or define a custom Organism/Evaluator/Mutator and drive the loop.<br>- Key pitfall: the initial organism must be marked viable even at score 0, and the CLI's run() is a generator that must be iterated; upstream is AGPL so it must only be invoked via subprocess/CLI, never imported into Hermes code.<br>- Concurrency defaults (10/10) are aggressive and hit rate limits; start low.<br>- Verified via the shipped smoke test that checks the visualizer HTML exists and --help runs cleanly. |
| domain-intel | skill | research | Pure-Python, zero-dependency passive domain reconnaissance tool for subdomains, SSL, WHOIS, DNS, and availability checks. | Python 3 stdlib only (socket, ssl, urllib); network access to crt.sh, WHOIS servers (TCP 43), Google DNS-over-HTTPS | none | N/A | optional-skills/research/domain-intel/SKILL.md | official | MIT (repo) | - | bundled scripts/domain_intel.py invoked via terminal tool; complements web_search/web_extract | HIGH | DEFAULT - zero-dependency, no keys, immediately useful for infrastructure OSINT questions | - Use for subdomain discovery, TLS cert inspection, WHOIS lookups, DNS records, or passive domain-availability checks, not for general what-does-this-company-do queries (use web_search instead).<br>- Run the bundled script's subcommands (subdomains/ssl/whois/dns/available/bulk) via terminal; all output is structured JSON.<br>- Pitfall: WHOIS uses TCP port 43 which may be blocked on restrictive networks; GDPR redaction on some registries; crt.sh can be slow for high-traffic domains; the availability check is heuristic, not authoritative.<br>- Verified by inspecting the JSON output for expected fields per command. |
| drug-discovery | skill | research | Curl/Python recipes for querying free public pharma APIs (ChEMBL, PubChem, OpenFDA, OpenTargets) to research bioactive compounds, drug-likeness, and interactions. | curl, python (stdlib json/urllib), public APIs ChEMBL/PubChem/OpenFDA/OpenTargets (no auth) | none | N/A | optional-skills/research/drug-discovery/SKILL.md | official | MIT (repo) | - | none beyond terminal tool; purely curl+python one-liners | HIGH | OPTIONAL - valuable for pharma/chemistry research niche, no setup cost since APIs are free and keyless | - Use for pharmaceutical/cheminformatics questions: compound search, Lipinski/Veber drug-likeness scoring, drug interaction/adverse-event lookups, or gene-disease association research.<br>- Run the provided curl+python snippets against ChEMBL, PubChem, OpenFDA, or OpenTargets endpoints and parse the JSON output.<br>- Always state raw property values before applying rule sets (Ro5, Veber), and cite the specific API used.<br>- Pitfall: ChEMBL rate-limits on batch queries (add sleep); FDA adverse-event data reflects reports, not causation; not a substitute for professional medical/pharmacist advice.<br>- Verified by successful JSON parses returning expected fields (SMILES, MW, LogP, etc.). |
| duckduckgo-search | skill | research | Free, keyless web/news/image/video search via the ddgs CLI or Python package as a fallback when Hermes's built-in web_search is unavailable. | ddgs Python package/CLI (pip install ddgs) | none | N/A | optional-skills/research/duckduckgo-search/SKILL.md | official | MIT (repo) | - | explicit fallback_for_toolsets: [web]; relies on terminal vs execute_code runtime distinction, web_extract for full content | HIGH | DEFAULT - keyless fallback search path worth keeping enabled when FIRECRAWL_API_KEY absent | - Use as a substitute for Hermes's native web_search when Firecrawl isn't configured, or when DuckDuckGo results are specifically requested.<br>- Prefer the ddgs CLI via terminal over importing the Python DDGS class in execute_code, since the sandbox may not have the package installed.<br>- Follow up any search with web_extract to get full page content since ddgs only returns snippets.<br>- Pitfall: max_results must be passed as a keyword argument; DuckDuckGo may rate-limit or block cloud IPs; terminal and execute_code are separate runtimes so installing in one doesn't guarantee the other has it.<br>- Verified by successful text/news/image/video search calls returning title/href/body fields. |
| gitnexus-explorer | skill | research | Sets up and serves a GitNexus-style interactive codebase knowledge-graph web UI, tunneled via Cloudflare for remote sharing. | Node.js 18+, git, cloudflared, npm packages from the GitNexus repo, a Node proxy script | none | N/A | optional-skills/research/gitnexus-explorer/SKILL.md | official | MIT (repo) | - | none beyond terminal tool; writes/patches files inside the cloned GitNexus repo | MEDIUM | OPTIONAL - niche visualization tool, heavier setup (clone+build+patch+tunnel) for a specific need | - Use when a user wants a visual, shareable knowledge-graph explorer of a codebase's symbols/call chains rather than raw CLI output.<br>- Clone and build GitNexus, patch its web UI for same-origin API calls, index the target repo, run a Node proxy alongside the GitNexus backend, then optionally tunnel with cloudflared.<br>- Pitfall: browser rendering caps out around 5k files (30k+ crashes the tab); must use --config /dev/null with cloudflared to avoid conflicting with named tunnel configs; a production build (not the Vite dev server) is required for remote hosts.<br>- Verified by curling the local proxy's /api/repos endpoint and receiving the indexed repo list. |
| osint-investigation | skill | research | Stdlib-only investigative framework that cross-references public-records sources (SEC, USAspending, lobbying, sanctions, offshore leaks, property, courts, archives, news) with confidence-scored entity resolution. | Python stdlib only; public APIs/data sources (SEC EDGAR, USAspending, Senate LDA, OFAC SDN, ICIJ, NYC ACRIS, OpenCorporates, CourtListener, Wayback, Wikipedia, GDELT); optional API tokens (OPENCORPORATES_API_TOKEN, SENATE_LDA_TOKEN, COURTLISTENER_TOKEN, SEC_USER_AGENT) | none | N/A | optional-skills/research/osint-investigation/SKILL.md | official | MIT (repo) | - | bundled fetch_*.py/entity_resolution.py/timing_analysis.py/build_findings.py scripts run via terminal tool; HERMES_OSINT_CACHE cache dir | HIGH | OPTIONAL - specialized investigative-journalism/due-diligence use case, but well-built and dependency-free | - Use for follow-the-money investigations: corporate due diligence, sanctions screening, lobbying-to-contract correlation, property/litigation history, or narrative research via news/archives.<br>- Fetch normalized CSVs per source, cross-reference entities with tiered confidence (exact/fuzzy/token_overlap), optionally run a permutation timing-correlation test, then compile an evidence-chain findings.json.<br>- Every claim must cite a specific record; entity matches are leads, not facts, and statistical significance isn't proof of wrongdoing.<br>- Pitfall: excludes US federal campaign finance (FEC) by design; some sources rate-limit or redact (GDPR); no fuzzy-matching library beyond token-bag matching.<br>- Verified by inspecting output CSVs/findings.json for populated evidence arrays and correct source citations. |
| parallel-cli | skill | research | Optional third-party CLI wrapper for Parallel's paid web search/extraction/deep-research/enrichment/entity-discovery/monitoring stack, used only when Parallel is explicitly wanted. | parallel-cli binary (brew/npm/pip/installer), PARALLEL_API_KEY or interactive/device login, paid Parallel account beyond free tier | API key | N/A | optional-skills/research/parallel-cli/SKILL.md | official | MIT (repo) | - | recommends saving large outputs to ~/.hermes/cache/scratch; explicitly deprioritized vs native web_search/web_extract | MEDIUM | OPTIONAL - only when user specifically wants Parallel's vendor workflows; native tools cover ordinary lookups | - Use only when the user explicitly names Parallel or needs its richer async research/enrichment/FindAll/monitor capabilities beyond simple search.<br>- Install via brew/npm/pip, authenticate via login or PARALLEL_API_KEY, then drive search/extract/research/enrich/findall/monitor subcommands with --json.<br>- For long research jobs, launch with --no-wait, capture the run ID, and poll/status later instead of blocking.<br>- Pitfall: it overlaps with and is not a default replacement for Hermes's native web_search/web_extract; auth may need a PTY/browser; costs real money beyond the free tier.<br>- Verified by checking exit codes (0=success) and that cited URLs come only from CLI output, not invented. |
| pinecone-research | skill | research | Shows how to use Pinecone as a vector-store RAG/long-term-memory backend for agent conversations via LangChain. | pinecone-client, langchain-pinecone, langchain-openai packages; PINECONE_API_KEY; an embeddings provider (e.g. OpenAI) | API key | N/A | optional-skills/research/pinecone-research/SKILL.md | official | MIT (repo) | - | none - generic Python/LangChain pattern, not wired into Hermes's own memory system | HIGH | OPTIONAL - overlaps with Hermes's built-in session/memory storage; only relevant if a user specifically wants Pinecone-backed RAG | - Use when a user wants to prototype or build agent RAG/long-term memory specifically on Pinecone rather than Hermes's native SQLite/FTS5 memory.<br>- Install pinecone-client, langchain-pinecone, and langchain-openai, create/connect a serverless index, then build a PineconeVectorStore from documents and query via a retriever.<br>- Namespace vectors per session/user for multi-tenant isolation, batch upserts in 100-200 chunks, and prune stale namespaces to control cost.<br>- Pitfall: requires an external paid vector DB and an embeddings API key; free tier caps at 1 index / 100K vectors (1536-dim).<br>- Verified by a successful similarity_search/retriever call returning relevant stored documents. |
| qmd | skill | research | Sets up and drives QMD, a local hybrid (BM25 + vector + LLM reranking) search engine over markdown notes, docs, and transcripts, optionally exposed to Hermes via MCP. | Node.js >=22, npm/bun, @tobilu/qmd package (auto-downloads ~2GB of local GGUF models), optional Homebrew SQLite on macOS | none | N/A | optional-skills/research/qmd/SKILL.md | official | MIT (repo) | - | MCP server registration in ~/.hermes/config.yaml (mcp_servers.qmd), otherwise plain terminal CLI calls | MEDIUM | OPTIONAL - strong for personal knowledge-base search, but heavy local-model footprint (~2GB, RAM) makes it a per-user opt-in | - Use when a user wants to search their own notes/docs/transcripts semantically or by keyword rather than grep or built-in search_files.<br>- Install qmd, add collections with descriptive context, generate embeddings, then query via search (BM25)/vsearch (semantic)/query (hybrid+rerank).<br>- Prefer registering it as an MCP server (stdio or HTTP daemon) so Hermes gets native qmd tools instead of shelling out each time.<br>- Pitfall: first run downloads ~2GB of GGUF models; cold start is ~19s unless a warm HTTP daemon is kept running; macOS needs Homebrew SQLite for extension loading.<br>- Verified via qmd status showing healthy collections and successful search/query results. |
| research-paper-writing | skill | research | End-to-end iterative pipeline for producing NeurIPS/ICML/ICLR/ACL/AAAI/COLM-style ML research papers, from experiment design through submission and post-acceptance deliverables. | semanticscholar, arxiv, habanero, requests, scipy, numpy, matplotlib, SciencePlots packages; LaTeX toolchain (chktex, latexmk/pdflatex, bibtex); git; optional Exa MCP server | none | N/A | optional-skills/research/research-paper-writing/SKILL.md | official | MIT (repo) | - | relies on Hermes's arxiv skill, web_search/web_extract, delegate_task subagents, todo tool, terminal/file toolsets | HIGH | OPTIONAL - heavyweight, valuable specifically for ML research-paper authorship workflows | - Use when starting, running experiments for, writing, reviewing, or submitting an ML/AI research paper, including non-empirical (theory/survey/negative-result) papers.<br>- Follow the phased loop: project setup and contribution framing, literature review with mandatory programmatic citation verification, experiment design mapped to specific claims, execution/monitoring with crash-safe incremental saving, statistical analysis, drafting, ensemble self-review with a meta-reviewer, and submission/anonymization/camera-ready prep.<br>- Never hallucinate citations; always fetch bibliographic data programmatically and mark unverifiable ones as citation-needed.<br>- Pitfall: skipping git discipline (commit per experiment batch) loses the exploration tree needed for an honest Methods-section narrative; reviewers penalize missing baselines/ablations and careless negative-result framing.<br>- Verified through statistical significance checks, claim-verification passes tracing every number to a result file, and pre-submission checklists (anonymization, formatting, compilation). |
| rss-feeds | skill | research | Zero-install, stdlib-only reader/discoverer for RSS 2.0/RDF/Atom/JSON feeds that normalizes entries and finds a feed behind an ordinary page URL. | Python 3.10+ stdlib only; network access to feed hosts | none | N/A | optional-skills/research/rss-feeds/SKILL.md | official | MIT (repo) | - | bundled scripts/feed.py invoked via terminal; pairs with cronjob_manage for recurring digests, web_extract for full article text | HIGH | DEFAULT - lightweight, dependency-free, immediately useful and safer/cheaper than scraping | - Use for what's-new-on-X questions, changelog/release tracking, or building a cron-based digest; install the heavier blogwatcher skill only when many feeds need persistent read/unread state.<br>- Run feed.py read on a URL or page (auto-discovers the feed) or feed.py discover to list candidate feeds; bound results with --limit or --since.<br>- Pass each entry's link (not the feed URL) to web_extract for full text and to citations.<br>- Pitfall: a 200 response with HTML instead of a feed can mean no discoverable feed exists; Reddit feeds share Reddit's roughly one-request-per-minute anonymous throttle; Cloudflare-fronted feeds may 403 non-browser clients.<br>- Verified by running the script against a known feed and confirming it prints entries with the right format tag. |
| scrapling | skill | research | Web-scraping framework skill providing HTTP, JS-rendering, and stealth/Cloudflare-bypass fetchers plus a spider framework, for cases the built-in web_extract can't handle. | scrapling Python package (pip install scrapling[all]), scrapling install (browser binaries), optional Playwright page automation | none | N/A | optional-skills/research/scrapling/SKILL.md | official | MIT (repo) | - | positioned as an escalation path from built-in web_extract; otherwise standalone Python/CLI usage | MEDIUM | OPTIONAL - escalation tool for anti-bot/Cloudflare sites where the built-in extractor fails; carries scraping-legality caveats | - Use when web_extract fails to get needed content: static HTML uses Fetcher, JS-rendered pages use DynamicFetcher, and Cloudflare/anti-bot pages use StealthyFetcher; use the Spider class for multi-page crawls.<br>- Install with pip install scrapling[all] then run scrapling install to fetch browser binaries; use the CLI or the Python API depending on need.<br>- Pitfall: forgetting scrapling install breaks the browser-based fetchers; DynamicFetcher/StealthyFetcher timeouts are in milliseconds while Fetcher's is in seconds; Cloudflare bypass adds 5-15s per fetch.<br>- Always check robots.txt/ToS first; the skill is explicitly scoped to educational/research use.<br>- Verified by successful CSS/XPath selector extraction returning expected page content. |
| searxng-search | skill | research | Free, keyless meta-search across 70+ engines via a self-hosted or public SearXNG instance, used as a fallback when Hermes's built-in web search is unconfigured. | a reachable SearXNG instance (self-hosted via Docker/pip, or a public instance) and the SEARXNG_URL environment variable; curl or requests | none | N/A | optional-skills/research/searxng-search/SKILL.md | official | MIT (repo) | - | explicit fallback_for_toolsets: [web]; keyed off SEARXNG_URL env var rather than a Hermes config field | MEDIUM | OPTIONAL - solid fallback/self-hosted alternative to duckduckgo-search, but requires infra (self-hosted instance) for reliability | - Use when the built-in web-search toolset (Firecrawl) is unconfigured and the user wants a privacy-respecting meta-search, or specifically wants SearXNG.<br>- Check SEARXNG_URL is set and the instance responds, then curl the search endpoint with format=json (or use requests) and parse title/url/content.<br>- Follow up with web_extract on the best URL for full content, since SearXNG only returns snippets.<br>- Pitfall: public instances can be down, rate-limited, or block queries; must explicitly request format=json and set a timeout; self-hosting (Docker or pip) is the reliable option.<br>- Verified by a successful JSON response containing a non-empty results array. |

## Security

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1password | skill | security | Sets up and drives the 1Password CLI (op) to read, inject, and run commands with secrets instead of using plaintext env vars. | 1Password account, op CLI binary, one of desktop-app integration / service account token (OP_SERVICE_ACCOUNT_TOKEN) / Connect server, tmux (desktop-app flow only) | API key / OAuth | N/A | optional-skills/security/1password/SKILL.md | official | MIT (repo) | wraps 1Password CLI (op) - proprietary vendor tool, not open-source licensed | writes OP_SERVICE_ACCOUNT_TOKEN into ${HERMES_HOME}/.env; setup.collect_secrets frontmatter integrates with Hermes's own secret-prompt flow | MEDIUM | OPTIONAL - valuable when a user already standardizes on 1Password for secrets; not a default dependency | - Use when a user wants secrets read from or injected via 1Password rather than plaintext .env files.<br>- Install the op CLI, then authenticate via service account token (best for Hermes/headless), desktop-app integration, or a self-hosted Connect server.<br>- For desktop-app auth in non-interactive Hermes terminal calls, run signin/read operations inside a dedicated tmux session so auth context persists across calls.<br>- Pitfall: never print raw secret values back to the user unless explicitly requested; service accounts need CLI v2.18.0+; headless/CI environments should avoid interactive op signin.<br>- Verified by op whoami succeeding and an op read call returning the expected secret reference. |
| godmode | skill | security | Provides API-level LLM jailbreak techniques (system-prompt templates, input obfuscation, multi-model racing) and Hermes config automation to bypass provider safety filters. | OpenRouter/provider API keys for multi-model racing; Hermes config.yaml/prefill.json write access; bundled Python scripts (parseltongue.py, godmode_race.py, load_godmode.py) | API key | N/A | optional-skills/security/godmode/SKILL.md | official | MIT (repo) | wraps G0DM0D3 and L1B3RT4S (elder-plinius) - both AGPL-3.0 upstream | directly edits ~/.hermes/config.yaml (agent.system_prompt, prefill_messages_file) and writes ~/.hermes/prefill.json; reads HERMES_HOME env var; calls out execute_code sandbox quirks | LOW | SKIP - directly undermines model safety alignment and mutates persistent agent config to bypass provider guardrails; not appropriate to carry into a new platform's default catalog | - Documents three attack modes: fixed jailbreak system-prompt templates per model family, an input-obfuscation encoder for trigger words, and a multi-model race-and-pick-least-filtered-response utility.<br>- Ships an auto-jailbreak routine that detects the configured model, tries ranked strategies against a canary query, and permanently writes the winning system prompt/prefill into Hermes's config.<br>- Includes a refusal/hedge pattern-matching scorer to detect when a jailbreak attempt succeeded or failed.<br>- Finding noted in the skill itself: provider-side patches make techniques perishable (e.g. one boundary technique no longer works on newer Claude versions); prefill priming is described as the most durable technique.<br>- Verification in the skill is a canary-query refusal score threshold, not a safety or correctness check. |
| oss-forensics | skill | security | A 7-phase multi-agent framework for investigating GitHub open-source supply-chain compromises using GH Archive/BigQuery, Wayback Machine, GitHub API, and local git forensics with strict evidence-citation discipline. | git, curl, GitHub REST API (GITHUB_TOKEN for higher rate limits), Google BigQuery/gcloud auth for GH Archive queries, Wayback Machine CDX API, bundled evidence-store.py script, delegate_task subagents | API key | N/A | optional-skills/security/oss-forensics/SKILL.md | official | MIT (repo) | - | uses delegate_task for parallel specialist investigators, terminal/web/file toolsets, bundled evidence-store.py CLI and templates | HIGH | DEFAULT - well-scoped defensive-security workflow with strong anti-hallucination/evidence discipline, useful for any supply-chain investigation | - Use when asked to investigate a GitHub repo for possible compromise: force-pushes, deleted PRs/issues, maintainer takeover, dependency confusion, or CI/CD injection.<br>- Initialize an evidence store, extract IOCs from the request, then delegate parallel single-source investigators (local git, GitHub API, Wayback Machine, GH Archive/BigQuery, IOC enrichment) that must not mix data sources.<br>- Consolidate and cross-reference evidence, form hypotheses citing specific evidence IDs, mechanically validate each hypothesis (reject any with fabricated/missing citations), then produce a structured forensic report.<br>- Pitfall: never execute code found in the investigated repo locally; redact discovered secrets; respect GitHub/BigQuery rate limits (dry-run BigQuery cost first).<br>- Verified by every report claim tracing to a specific evidence ID and a final evidence-store count check. |
| sherlock | skill | security | Wraps the Sherlock Project CLI to search for a given username across 400+ social platforms for OSINT/reconnaissance. | sherlock (sherlock-project) CLI via pip/pipx/Docker; network access to social platforms | none | N/A | optional-skills/security/sherlock/SKILL.md | official | MIT (repo) | - | none beyond terminal tool invocation of the sherlock binary | HIGH | OPTIONAL - useful narrow OSINT capability, ethical-use caveats apply | - Use when asked to find social-media accounts tied to a specific username, or check username availability/registration across platforms.<br>- Verify sherlock is installed, extract the exact username from the request without unnecessary clarification, then run the CLI with print-found and a timeout flag.<br>- Present found accounts as categorized links plus the default output file location.<br>- Pitfall: some sites produce false positives due to response-structure quirks; aggressive searches can trigger rate limits; the --tor option requires a running Tor daemon.<br>- Verified by output containing matched-account lines with valid URLs and, if requested, the saved results file. |
| unbroker | skill | security | Autonomous, consent-gated pipeline that scans data-broker/people-search sites for a person's exposed PII and drives opt-out/deletion requests, escalating only CAPTCHA/ID/phone-gated steps to a human digest. | Python (stdlib), bundled scripts/pdd.py CLI; optional Browserbase (cloud browser), SMTP/IMAP email credentials or browser-driven webmail via CDP, Google Sheets (google-workspace skill), scrapling skill, age encryption binary; Hermes's web_extract/browser_navigate/cronjob tools | none | N/A | optional-skills/security/unbroker/SKILL.md | official | MIT (repo) | - | reads credentials from $HERMES_HOME/.env, stores dossiers under $HERMES_HOME/unbroker, integrates with the cronjob tool for recurring re-scans, and explicitly requires terminal (not execute_code, which scrubs secrets/output) | LOW | OPTIONAL - powerful and well-engineered but high-stakes (handles real PII/consent); appropriate only where privacy-removal is an explicit product feature | - Use when a user asks to remove their (or a consented third party's) personal data from data-broker/people-search sites, or to set up recurring privacy monitoring.<br>- Run setup/intake (recording explicit consent) then loop the engine's next-action command repeatedly, executing every returned action (scan, poll email verification, submit opt-outs parents-first for ownership clusters, re-verify removals) until the queue is empty.<br>- Human-only steps (CAPTCHA/gov-ID/phone/fax) are queued into a single end-of-run digest rather than interrupting the run.<br>- Pitfall: never disclose more than the broker's own declared fields; no consent means no action; one major broker cluster's delete flow wipes its own suppression and must be treated specially; a case is only marked fully removed after a verifying re-scan, not just a submission confirmation.<br>- Verified via the bundled test suite and a dry run showing setup, intake, and next producing an ordered action queue. |
| web-pentest | skill | security | A phased, evidence/proof-required web-application penetration-testing workflow (recon through exploitation and reporting) with hard scope/authorization guardrails. | terminal tools (nmap, whatweb, curl), browser tool, delegate_task subagents, bundled recon-scan.sh and templates; explicit written user authorization before any active scan | N/A | N/A | optional-skills/security/web-pentest/SKILL.md | official | MIT (repo) | - | uses delegate_task for per-vulnerability-class analysis subagents, terminal/web/browser/file toolsets, and calls out a Hermes-specific auxiliary-client history-replay leakage risk for captured secrets | MEDIUM | DEFAULT - a disciplined, safety-gated authorized-pentest workflow appropriate for a security-tooling catalog | - Use only for an authorized web-app penetration test; requires the user to explicitly type an acknowledgement and a maintained scope allowlist before any active request.<br>- Proceed through pre-recon (optional static code analysis), read-only recon, per-vulnerability-class staged analysis via subagents, then proof-based exploitation that promotes findings through identified, partial, confirmed, and critical levels with bypass-exhaustion before dismissing as a false positive.<br>- Every finding needs reproducible evidence (request/response, repro curl, impact); redact captured credentials to the last six characters in chat, full values only in gitignored evidence files.<br>- Pitfall: destructive payloads and cloud-metadata probing require explicit prior approval; stop immediately if authorization is revoked or scope is exceeded (e.g. discovering an unrelated exposed database) rather than pivoting.<br>- Verified by the final report only listing confirmed/critical findings with CVSS scores, sourced strictly from recorded evidence. |

## Smart Home

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| openhue | skill | smart-home | Wraps the OpenHue CLI to control Philips Hue lights, rooms, and scenes (on/off, brightness, color, temperature) from the terminal over the local network. | openhue CLI binary (prebuilt release tarball or Homebrew), a Philips Hue Bridge on the same local network, one-time physical bridge-button pairing | none | N/A | optional-skills/smart-home/openhue/SKILL.md | official | MIT (repo) | - | mentions pairing well with Hermes cron jobs for scheduled lighting | LOW | OPTIONAL - useful only for a local/home-network Hermes deployment with real Hue hardware present | - Use for voice/chat-driven control of Philips Hue lights, rooms, zones, or scenes, turning them on/off, dimming, setting color/temperature, or applying quick presets like bedtime/work/movie mode<br>- Requires installing the openhue-cli binary and one-time bridge pairing (press bridge button on first run) since the bridge must be reachable on the local network<br>- Key pitfalls: light/room names are case-sensitive (list them first)<br>- colors only work on color-capable bulbs<br>- the bridge must be on the same LAN as the machine running Hermes<br>- Verifies success by listing lights and confirming the target lights respond to a subsequent on/off or brightness command |

## Social Media

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| reddit-reading | skill | social-media | Reads Reddit subreddits, search results, threads, and user activity from a server/headless IP where the normal Reddit web/API/browser routes are blocked, using either anonymous RSS/Atom feeds or optional free OAuth app credentials, read-only, no login. | bundled scripts/reddit.py, Python stdlib/HTTP, optional free Reddit script app credentials (REDDIT_CLIENT_ID/REDDIT_CLIENT_SECRET) in .env | none | N/A | optional-skills/social-media/reddit-reading/SKILL.md | official | MIT (repo) | - | ~/.hermes/.env convention, references other Hermes skills (grounded-citations, blocked-page-recovery) for citation/fallback handling | HIGH | DEFAULT - solves a common real problem (Reddit blocking datacenter IPs) with no required credentials | - Use to read subreddit listings, search Reddit, summarize a shared thread, or check a user's activity from a server/headless environment where normal Reddit fetching returns 403s<br>- Check backend/rate-limit status with a doctor subcommand, then use sub/search/thread/user subcommands, preferring one targeted call over several broad listings given the roughly 1-request-per-minute anonymous throttle<br>- Key pitfalls: don't fall back to the reddit.com .json endpoints or spoofed user agents (still blocked)<br>- don't retry-loop on 429s since the script already waits out the window<br>- anonymous thread feeds only include top-level comments with no scores<br>- Verifies success via the doctor command reporting a healthy backend (or OAuth active with credentials) and a test subreddit call returning a valid permalink |

## Software Development

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ast-grep | skill | software-development | Provides a Python helper and workflow around the ast-grep (sg) tool for AST-aware structural code search and codemod-style rewrites across 25 languages. | ast-grep/sg binary, bundled scripts/ast_grep_helper.py, install.sh/install.ps1 | none | N/A | optional-skills/software-development/ast-grep/SKILL.md | official | MIT (repo) | - | recommended to run through Hermes's terminal tool; explicitly complements (not replaces) Hermes's patch tool | HIGH | DEFAULT - solid, safe default for any find/rewrite-by-code-shape task | - Use when a task depends on code structure (function/class/call shape) rather than raw text/regex; switch to search_files/rg for text-shaped queries.<br>- Use the bundled helper's search/replace/scan/validate subcommands: validate patterns offline first, dry-run rewrites, then apply with a two-pass approach since raw JSON output and update-all are mutually exclusive.<br>- Patterns are code (using meta-variables), not regex; trying to use regex syntax fails silently.<br>- Pitfall: the sg binary name can collide with util-linux's setgroups on Linux (prefer the ast-grep binary name); stdin usage requires explicit language flag.<br>- Verified by the helper's dry-run summary (match/file counts) before any apply, and a git diff review after applying. |
| code-wiki | skill | software-development | Generates a structured wiki (overview, architecture, per-module docs, Mermaid diagrams, getting-started) for any codebase using only built-in Hermes file/terminal tools. | git (for repo SHA/clone), optional pygount for language stats; Hermes's terminal/read_file/search_files/write_file tools | none | N/A | optional-skills/software-development/code-wiki/SKILL.md | official | MIT (repo) | - | writes output under ~/.hermes/wikis/<repo-name>/ by default; uses Hermes's read_file/search_files/write_file/terminal tool names directly | MEDIUM | DEFAULT - low-risk, useful onboarding/documentation generator with clear anti-fabrication discipline | - Use when asked to document an unfamiliar codebase or generate architecture diagrams, not for single-file/function explanations or strategic why narratives.<br>- Resolve the target repo, scan its structure with bounded depth, pick 8-10 modules, then write README/architecture/per-module docs, Mermaid class and sequence diagrams, and a getting-started guide.<br>- Cap scope deliberately (max depth 3, 10 modules) to control token cost on large repos, and flag cost estimates before an exhaustive run.<br>- Pitfall: every diagram node/claimed call must trace to real source and never be fabricated; Mermaid diagrams over roughly 50 nodes don't render legibly and must be split.<br>- Verified by checking Mermaid fence balance, expected output files present, module count matching what was scoped, and spot-checking a few source links resolve. |
| grill-me | skill | software-development | An adversarial "grill the plan" interview technique that models a proposed plan as a decision tree and questions the user in dependency-ordered rounds before any code is written. | none beyond reasoning; optionally search_files/read_file/terminal for codebase fact-finding, delegate_task for heavy exploration | none | N/A | optional-skills/software-development/grill-me/SKILL.md | official | MIT (repo) | - | uses Hermes's search_files/read_file/terminal/delegate_task tool names for fact-finding rather than asking the user | HIGH | DEFAULT - low-cost, high-value pre-implementation alignment technique for any complex plan | - Use before implementing complex or risky work (auth flows, schema migrations, payments) or whenever a plan seems vague or under-specified.<br>- Model the plan as a design tree; each round asks only the frontier of questions whose prerequisites are already settled, each paired with a recommended answer.<br>- Look up any answerable-from-the-codebase fact yourself instead of asking the user; only decisions are the user's to make.<br>- Pitfall: asking questions whose answers depend on other still-open questions turns them into guesses; don't write code during the interrogation.<br>- Verified by an explicit synthesis step listing all decisions and open items and getting the user's confirmed alignment before implementation starts. |
| pr-lens | skill | software-development | Renders code diffs or architecture as animated, structured SVG diagrams (not a review/findings tool) via a third-party CLI, optionally attached to a GitHub PR. | Node.js/npx (runs a pinned pr-lens CLI package), gh CLI (optional, for PR attachment, needs a recent gh version), optional third-party canvas-publishing service | none | N/A | optional-skills/software-development/pr-lens/SKILL.md | official | MIT (repo) | - | none beyond terminal tool invocation of npx and gh | HIGH | OPTIONAL - a nice-to-have visualization layer for PRs/architecture explanations, not core review tooling | - Use when asked to diagram/visualize a code change or system architecture, or to attach an architecture/data-flow diagram to a PR; it is explicitly not a bug/security review tool.<br>- Author a single graph document (lanes, nodes, edges, flows) describing the diff or codebase, validate it, then render to SVGs via the CLI.<br>- Optionally push to the third-party canvas service (opt-in only) for a shareable link, keeping the write-capable edit link private.<br>- Pitfall: never commit the generated output directory (gitignored/rebuilt on demand); PR attachment needs a recent gh CLI version; always run validate since several integrity checks are parser-only, not schema-enforced.<br>- Verified by validate and render both exiting cleanly and producing the expected SVG/manifest files. |
| rest-graphql-debug | skill | software-development | A layered methodology (connectivity, TLS, auth, request format, response parsing, semantics) plus curl/Python snippets for debugging REST and GraphQL API failures. | curl, Python requests library, terminal and execute_code tools, optional web_extract for vendor docs | N/A | N/A | optional-skills/software-development/rest-graphql-debug/SKILL.md | official | MIT (repo) | - | explicitly maps steps to Hermes's terminal/execute_code/web_extract/delegate_task tools | HIGH | DEFAULT - broadly useful, low-risk debugging methodology for any API-integration task | - Use whenever an API call returns an unexpected status/body, auth fails unexpectedly, or an integration works in a GUI client but not in code.<br>- Walk the fixed layer order (connectivity, timeouts, TLS, auth, request format, response parsing, semantics) without skipping steps, since a 200 can still hide broken data.<br>- Use the HTTP status-code playbook (401/403/404/409/422/429/5xx) to narrow root cause, and always check GraphQL's errors field even on HTTP 200.<br>- Pitfall: default content-type/body mismatches cause silent 400/415s; the requests library has no default timeout and will hang without an explicit tuple.<br>- Verified via a captured correlation/request ID, a redacted repro curl command, and where relevant an added regression test that fails before the fix and passes after. |
| subagent-driven-development | skill | software-development | A workflow for executing implementation plans by dispatching a fresh delegate_task subagent per task, followed by mandatory two-stage (spec-compliance then quality) review before moving to the next task. | Hermes's delegate_task and todo tools; an existing implementation plan (e.g. from a plan skill) | none | N/A | optional-skills/software-development/subagent-driven-development/SKILL.md | official | MIT (repo) | - | built entirely around Hermes's delegate_task/todo tool signatures and toolsets parameter | MEDIUM | DEFAULT - a disciplined, quality-focused orchestration pattern for any multi-task implementation plan | - Use when executing a multi-task implementation plan where tasks are mostly independent and quality/spec-compliance matter, not for exploratory single-shot work.<br>- Read the plan once, extract full task text into a todo list, then for each task dispatch an implementer subagent (with full context inlined, following TDD), a spec-compliance reviewer, and a code-quality reviewer in strict order, fixing and re-reviewing until both pass.<br>- Keep tasks small (2-5 minutes of focused work each) and never dispatch multiple implementers against the same files concurrently.<br>- Pitfall: skipping either review stage, doing quality review before spec review, or letting the implementer's self-report substitute for actual review all defeat the pattern's purpose.<br>- Verified by a final integration-review subagent plus a full test-suite run and a diff review before the closing commit. |

## Web Development

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| cloudflare-temporary-deploy | skill | web-development | Deploys a Cloudflare Worker to a live throwaway workers.dev URL with zero account setup using wrangler's temporary-deploy mode, for fast write-deploy-verify loops. | Node 18+/npm/npx, wrangler CLI >=4.102.0, no existing Cloudflare credentials present in the shell | none | N/A | optional-skills/web-development/cloudflare-temporary-deploy/SKILL.md | official | MIT (repo) | - | run via terminal tool; bundled scripts/parse_deploy_output.py parses wrangler's stdout into JSON | HIGH | DEFAULT - low-friction, zero-account way to demo agent-written Workers with a real live URL | - Use when a user wants agent-written code shipped to a live URL instantly, without Cloudflare signup/OAuth, especially in autonomous/background sessions where a browser login would block.<br>- Scaffold a minimal Worker, run the temporary-deploy command, parse the live/claim URLs, then curl the live URL to verify before reporting success.<br>- Redeploying within 60 minutes reuses the same cached temporary account/URL; hand the claim URL to the user as a secret they must open within 60 minutes to keep the deployment.<br>- Pitfall: the temporary flag is a hidden feature absent from --help and requires a recent wrangler version; it hard-errors if any Cloudflare auth/env var is present; edge caching can briefly serve a stale body right after redeploy.<br>- Verified by curl returning the expected body and a redeploy reporting the account was reused with an unchanged URL. |
| har-derived-api-client | skill | web-development | Captures a site's network traffic into a HAR file via a real (local or CDP-connected) browser session, then derives that site's private JSON API so it can be called directly over plain HTTP instead of re-driving a browser every time. | Playwright + a browser binary (capture step only), requests/httpx/curl (replay step), a reachable CDP endpoint for the cloud-browser capture path | none | N/A | optional-skills/web-development/har-derived-api-client/SKILL.md | official | MIT (repo) | - | explicitly covers every Hermes browser backend (local browser_navigate, cloud backends, CDP connect) and picks the capture script accordingly | MEDIUM | DEFAULT - a smart cost/latency optimization whenever an agent would otherwise repeatedly browser-drive the same site | - Use when about to loop browser automation for the same query/page repeatedly, or when a site clearly fetches JSON but exposes no documented public API.<br>- Pick the correct capture script based on whether the browser was launched locally or connected to over CDP/cloud, drive the needed UI interaction while recording, then derive endpoint method/params/headers/body from the HAR.<br>- Recreate the request with a plain HTTP client, carrying forward the captured User-Agent (and cookies/auth token if present); this is the single most common cause of derived clients failing when the browser worked.<br>- Pitfall: server-rendered pages produce no XHR to derive; debounced/typeahead requests need an explicit pause during capture; captured HAR files contain live secrets and should be treated as sensitive and deleted after use.<br>- Verified end-to-end by replaying the derived client outside the browser and confirming it returns the same data the browser captured. |
| page-agent | skill | web-development | Documents how to embed an open-source in-page natural-language GUI copilot that reads a webpage's DOM as text and executes instructions against it, into a web app. | Node 22.13+/npm 10+, an OpenAI-compatible LLM endpoint (Qwen/OpenAI/Ollama/OpenRouter), the page-agent npm package or CDN bundle | API key | N/A | optional-skills/web-development/page-agent/SKILL.md | official | MIT (repo) | - | none - a pure web-app integration guide unrelated to Hermes's own browser-automation direction (explicitly the opposite use case) | HIGH | OPTIONAL - a specific third-party product integration, valuable only when a user wants an in-app copilot rather than agent-driven browsing | - Use when a user wants to add an in-page AI copilot to their own web app that end-users type natural-language instructions into, not when the goal is Hermes itself driving a browser.<br>- Try it instantly via the CDN demo script for evaluation, or install the npm package and wire it up against any OpenAI-compatible endpoint for production use.<br>- For serious development/testing, clone the monorepo, configure environment variables, and use the local dev-bundle bookmarklet to test the copilot on arbitrary sites.<br>- Pitfall: never ship the demo CDN or a client-side API key to real users; proxy LLM calls through your own backend, and never share the dev bundle since it inlines local secrets.<br>- Verified by seeing the floating panel appear and confirming a network request to the configured LLM endpoint fires when an instruction is typed. |
| publish-site | skill | web-development | A disciplined static-site publishing workflow (build, preview, git-tag version, deploy via a GitHub Pages/Cloudflare Pages/Netlify provider ladder, verify, rollback) for shipping agent-built sites to infrastructure the user owns. | git, gh CLI (GitHub Pages), wrangler CLI or a Cloudflare API token (Cloudflare Pages), netlify-cli (Netlify), optional cloudflared for shareable local previews | OAuth / API key | N/A | optional-skills/web-development/publish-site/SKILL.md | official | MIT (repo) | - | none beyond terminal tool invocations; complements the cloudflare-temporary-deploy skill (which it explicitly defers to for account-free serverless deploys) | HIGH | DEFAULT - solid, safety-conscious default for put-this-site-online requests with real rollback discipline | - Use when a user wants a built site/dashboard/prototype published to a real, user-owned URL, not a throwaway serverless demo.<br>- Build and preview locally (optionally tunnel for remote sign-off), commit and git-tag before every deploy, then deploy through the provider ladder: GitHub Pages by default, Cloudflare Pages for custom domains/Functions, Netlify as fallback.<br>- Rollback is just redeploying a previous git tag, never hand-editing live output; never commit secrets since anything in a static bundle or repo is public.<br>- Pitfall: SPA client-side routes 404 on GitHub Pages unless the index page is duplicated as a 404 page; GitHub Pages project sites serve under a subpath that breaks absolute asset URLs; deploying source instead of the build output directory yields a broken site.<br>- Verified by curling the live URL for a 200 status and expected content, checking a deep SPA route doesn't 404, and confirming the deploy git tag exists. |
| scrollcraft | skill | web-development | A structured, interview-driven methodology and toolset for building premium scroll-driven "scrollytelling" landing pages with varied animation devices, a curated feeling curve, and screenshot-based verification. | Node.js, ffmpeg (full build, for asset encoding), Playwright/playwright-core (for screenshot verification), optional image-generation tool or a paid asset-generation API key, a token-driven CSS/JS engine bundled with the skill | none | N/A | optional-skills/web-development/scrollcraft/SKILL.md | official | MIT (repo) | - | uses Hermes's write_file, image_generate, vision_analyze tools and a workspace/registry convention plus optional browser_exec for verification | MEDIUM | OPTIONAL - a heavyweight, opinionated design workflow valuable specifically for premium scroll-based landing pages, not general web dev | - Use when a user wants a scrollytelling, Apple-style landing page experience rather than a standard static page; always start with a mandatory user interview (vibe, journey, feeling curve, signature move) written to a brief file before building anything.<br>- Pick one of eight mutually-exclusive page grammars, invent one bespoke signature interaction, and run a fingerprint gate against a registry of past builds to force genuine variety.<br>- Generate or source assets (user footage/photos preferred, else an image-generation tool, optionally a paid API for AI stills/clips) and encode video for scrub-seeking with a dense keyframe interval.<br>- Hard ship-blockers include no default clay-diorama aesthetic, no repeated animation device back-to-back, no scroll-hint cues or section counters, and never editing the shared engine per project.<br>- Verified by a scripted multi-position screenshot pass (desktop/mobile/reduced-motion) plus a manual feel-check diff against the original brief, appending the finished build's fingerprint row to the registry. |

## Yuanbao

| id | type | category | one_line_summary | depends_on | auth_type | transport | source | tier | license | license_flag | hermes_coupling | portability | suggested_use | rewrite_brief |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| yuanbao | skill | yuanbao | Enables an agent on the Yuanbao (元宝) messaging platform to @mention users and send direct messages within groups by combining plain-text replies with three group-lookup/DM tools. | Yuanbao gateway integration tools (yb_query_group_info, yb_query_group_members, yb_send_dm) | bot token | N/A | optional-skills/yuanbao/SKILL.md | official | MIT (repo) | - | Yuanbao-specific gateway tools (yb_query_group_info, yb_query_group_members, yb_send_dm), Hermes gateway message-reply-as-send convention, chat_id/group_code parsing convention, explicitly avoids Hermes' generic send_message tool for Yuanbao DMs | LOW | SKIP - single-platform (Yuanbao/China messaging) gateway integration, not relevant outside that specific messaging platform | - Use when running as a bot in a Yuanbao (元宝) group chat and the user asks to @mention someone or send a private direct message<br>- To mention: look up the exact nickname via the group-members tool with mention=true, then simply include @nickname in the plain-text reply, which the gateway auto-converts into a real mention<br>- to DM: call the dedicated DM tool with the group code and target name/message<br>- Key pitfalls: never guess a nickname instead of looking it up first<br>- never claim the agent cannot send messages or mention users since replying normally already sends the message<br>- don't use the generic send_message tool for Yuanbao DMs<br>- Verifies success implicitly by the reply text being delivered with a working mention, or by the DM tool call succeeding without a multiple-candidates clarification needed |

